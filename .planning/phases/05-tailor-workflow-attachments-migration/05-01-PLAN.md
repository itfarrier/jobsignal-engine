---
phase: 05-tailor-workflow-attachments-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/03-tailor.json
autonomous: false
requirements:
  - TAIL-01
  - TAIL-02
  - TAIL-03
user_setup: []

must_haves:
  truths:
    - "Tailor workflow reads Profile from NocoDB via HTTP GET + Unwrap Profile"
    - "Tailor workflow reads Pipeline records (Status=Evaluated, Fit Tier=High, Tailored CV Text blank) via HTTP GET + Unwrap High Fit Jobs with D-04 (is,blank) filter"
    - "Build Tailoring Prompt references Unwrap nodes and threads string UUID record.id as _recordId"
    - "DOCX renders via cv-renderer sidecar unchanged, then Convert to File produces binary data property"
    - "Upload DOCX posts multipart file to NocoDB v2 /api/v2/storage/upload with continueOnFail true (D-06)"
    - "Update Pipeline Record PATCH writes Tailored CV Text, CV Tailoring Cost, and Tailored CV attachment array when upload succeeds"
    - "When upload fails, PATCH still saves Tailored CV Text and CV Tailoring Cost without attachment (D-06)"
    - "CV attachment is visible, previewable, and downloadable in NocoDB Pipeline record UI (TAIL-03)"
  artifacts:
    - path: "workflows/03-tailor.json"
      provides: "Tailor workflow migrated from Airtable to NocoDB with attachment upload"
      contains: "Unwrap High Fit Jobs"
      min_lines: 800
  key_links:
    - from: "Unwrap High Fit Jobs (Code node)"
      to: "Get High Fit Jobs (HTTP GET with where filter)"
      via: "$input.first().json.records"
      pattern: "id: record.id"
    - from: "Build Tailoring Prompt (Code node)"
      to: "Unwrap Profile + Unwrap High Fit Jobs"
      via: "$('Unwrap Profile') and $('Unwrap High Fit Jobs')"
      pattern: "_recordId.*job.json.id"
    - from: "Convert to File"
      to: "Render DOCX"
      via: "docx_base64 field"
      pattern: "sourceProperty.*docx_base64"
    - from: "Upload DOCX"
      to: "Convert to File"
      via: "formBinaryData inputDataFieldName data"
      pattern: "formBinaryData"
    - from: "Update Pipeline Record (HTTP PATCH)"
      to: "Upload DOCX + Parse Tailored CV"
      via: "conditional jsonBody IIFE"
      pattern: "Tailored CV.*upload"

---

<objective>
Migrate the Tailor workflow (Workflow 3) from Airtable to NocoDB: replace 2 Airtable read nodes, 1 Airtable update node, and 1 Airtable content upload with NocoDB HTTP Request + Unwrap nodes, add Convert to File for base64→binary, and adapt DOCX upload to NocoDB v2 storage API with v3 PATCH for attachment fields.

Purpose: Complete the database backend migration for CV tailoring — the pipeline must read Profile and high-fit untailored jobs from NocoDB, generate identical tailored CV markdown and DOCX files, upload attachments via NocoDB storage, and persist results back to Pipeline records. AI tailoring logic, Render DOCX sidecar, Loop Over Jobs, Wait 2s, and schedule trigger remain unchanged.

Output: `workflows/03-tailor.json` — modified in-place with 2 HTTP GET + 2 Unwrap Code nodes, 1 Convert to File node, 1 Upload DOCX HTTP POST (multipart), 1 Update Pipeline Record HTTP PATCH (conditional attachment per D-06), and updated Code node references.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tailor-workflow-attachments-migration/05-CONTEXT.md
@.planning/phases/05-tailor-workflow-attachments-migration/05-RESEARCH.md
@.planning/phases/05-tailor-workflow-attachments-migration/05-PATTERNS.md
@.planning/phases/05-tailor-workflow-attachments-migration/05-VALIDATION.md
@workflows/03-tailor.json
@workflows/02-evaluator.json
@workflows/01a-scanner-greenhouse.json
@scripts/nocodb-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Airtable GET reads with NocoDB HTTP GET + Unwrap nodes + update Build Tailoring Prompt</name>
  <files>workflows/03-tailor.json</files>
  <read_first>
    - workflows/03-tailor.json — nodes "Get High Fit Jobs" (Airtable, lines 35-68), "Get Profile" (Airtable, lines 70-103), "Build Tailoring Prompt" (Code, lines 105-117), connections block (lines 678-811)
    - workflows/02-evaluator.json lines 65-118 — reference for HTTP GET + Unwrap pattern (Get New Jobs, Unwrap New Jobs)
    - workflows/01a-scanner-greenhouse.json lines 222-270 — reference for Get Profile + Unwrap Profile
    - .planning/phases/05-tailor-workflow-attachments-migration/05-PATTERNS.md lines 36-205 — exact replacement JSON and Code edits
    - .planning/phases/05-tailor-workflow-attachments-migration/05-CONTEXT.md D-04 — (is,blank) filter syntax
  </read_first>
  <action>
    Make structural changes to workflows/03-tailor.json nodes array and connections block. Reference Phase 4 evaluator pattern; apply D-04 filter exactly.

    A) Replace "Get High Fit Jobs" Airtable node with HTTP Request GET:
       - type: n8n-nodes-base.httpRequest, typeVersion: 4.2
       - method: GET
       - url: ={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?where=(Status,eq,Evaluated)~and("Fit Tier",eq,High)~and("Tailored CV Text",is,blank)
       - Per D-04: use (is,blank) operator for Tailored CV Text — catches empty string and NULL
       - authentication: genericCredentialType, genericAuthType: httpHeaderAuth
       - sendHeaders: true, header Content-Type: application/json
       - retryOnFail: true, maxTries: 3, waitBetweenTries: 5000, alwaysOutputData: true
       - credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
       - Keep same node id and position [2784, 64]
       - Remove airtableTokenApi credential

    B) Insert new "Unwrap High Fit Jobs" Code node after Get High Fit Jobs:
       - type: n8n-nodes-base.code, typeVersion: 2
       - name: Unwrap High Fit Jobs
       - position: [2896, 64] (between Get High Fit Jobs and Get Profile)
       - Generate unique id
       - jsCode (exact):
         // Unwrap NocoDB Data API v3 response — high-fit untailored jobs
         const response = $input.first().json;
         const records = response.records || [];
         if (records.length === 0) {
           return [{ json: { _empty: true, _message: 'No high-fit jobs needing tailoring' } }];
         }
         return records.map(record => ({
           json: {
             ...record.fields,
             id: record.id
           }
         }));
       - CRITICAL D-05 override (per RESEARCH + Phase 4 proof): expose id: record.id (string UUID for v3 PATCH), NOT Id integer from id_fields. CONTEXT D-05 said loopItem['Id'] — planner adopts record.id per Phase 4 implementation.

    C) Replace "Get Profile" Airtable node with HTTP Request GET:
       - Copy structure from 01a-scanner-greenhouse.json Get Profile (lines 222-257)
       - url: ={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records
       - Preserve executeOnce: true from current tailor node
       - position: [3024, 64]
       - Same auth/retry/credential as Get High Fit Jobs
       - Remove airtableTokenApi credential

    D) Insert new "Unwrap Profile" Code node after Get Profile:
       - name: Unwrap Profile, position: [3136, 64]
       - jsCode: copy from 01a-scanner-greenhouse.json Unwrap Profile (lines 258-270) — identical unwrap with _message: 'No Profile found'
       - Expose id: record.id on each item

    E) Update connections block from:
         Get High Fit Jobs → Get Profile → Build Tailoring Prompt
       To:
         Get High Fit Jobs → Unwrap High Fit Jobs → Get Profile → Unwrap Profile → Build Tailoring Prompt
       - Manual Trigger and Schedule Trigger still connect to Get High Fit Jobs only
       - Follow 02-evaluator.json connections pattern (Get → Unwrap → Get → Unwrap → Code)

    F) Update "Build Tailoring Prompt" Code node jsCode:
       - Replace: const profile = $('Get Profile').first().json;
         With: const profileItems = $('Unwrap Profile').all();
         if (!profileItems || profileItems.length === 0) { throw new Error('Profile table is empty. Add your profile row before running.'); }
         const profile = profileItems[0].json;
       - Replace: const jobs = $('Get High Fit Jobs').all();
         With: const jobs = $('Unwrap High Fit Jobs').all();
       - In the jobs.map callback, replace recordId assignment:
         const recordId = job.json.id;
         if (!recordId) throw new Error('Missing NocoDB record id on job item');
       - Keep _recordId: recordId threading to Loop Over Jobs (Parse Tailored CV reads loopItem._recordId unchanged)
       - Profile field access: profile['CV Markdown'] works on flat Unwrap output — remove profile.fields ? branch if present, or keep as harmless fallback
  </action>
  <verify>
    <automated>node -e "
let fail = false;
const assert = (cond, msg) => { if (!cond) { fail = true; console.error('FAIL: ' + msg); } };
const wf = require('./workflows/03-tailor.json');
const conns = wf.connections || {};
const names = wf.nodes.map(n => n.name);
assert(names.includes('Unwrap High Fit Jobs'), 'Unwrap High Fit Jobs missing');
assert(names.includes('Unwrap Profile'), 'Unwrap Profile missing');
const getHF = wf.nodes.find(n => n.name === 'Get High Fit Jobs');
const getProf = wf.nodes.find(n => n.name === 'Get Profile');
assert(getHF.type === 'n8n-nodes-base.httpRequest', 'Get High Fit Jobs not HTTP');
assert(getProf.type === 'n8n-nodes-base.httpRequest', 'Get Profile not HTTP');
assert(getHF.parameters.url.includes('(is,blank)'), 'Missing D-04 blank filter');
assert(getHF.parameters.url.includes('Fit Tier'), 'Missing Fit Tier filter');
assert(getProf.executeOnce === true, 'Get Profile missing executeOnce');
const unwrapHF = wf.nodes.find(n => n.name === 'Unwrap High Fit Jobs');
const unwrapProf = wf.nodes.find(n => n.name === 'Unwrap Profile');
assert(unwrapHF.parameters.jsCode.includes('id: record.id'), 'Unwrap High Fit Jobs missing id: record.id');
assert(unwrapProf.parameters.jsCode.includes('id: record.id'), 'Unwrap Profile missing id: record.id');
assert(conns['Get High Fit Jobs']?.main[0][0].node === 'Unwrap High Fit Jobs', 'Get High Fit Jobs → Unwrap missing');
assert(conns['Unwrap High Fit Jobs']?.main[0][0].node === 'Get Profile', 'Unwrap HF → Get Profile missing');
assert(conns['Get Profile']?.main[0][0].node === 'Unwrap Profile', 'Get Profile → Unwrap Profile missing');
assert(conns['Unwrap Profile']?.main[0][0].node === 'Build Tailoring Prompt', 'Unwrap Profile → Build Tailoring Prompt missing');
const btp = wf.nodes.find(n => n.name === 'Build Tailoring Prompt');
const code = btp.parameters.jsCode;
assert(code.includes(\"$('Unwrap Profile')\"), 'Build Tailoring Prompt missing Unwrap Profile ref');
assert(code.includes(\"$('Unwrap High Fit Jobs')\"), 'Build Tailoring Prompt missing Unwrap High Fit Jobs ref');
assert(!code.includes(\"$('Get Profile')\"), 'Stale Get Profile ref');
assert(!code.includes(\"$('Get High Fit Jobs')\"), 'Stale Get High Fit Jobs ref');
assert(code.includes('job.json.id'), 'Build Tailoring Prompt missing job.json.id');
assert(getHF.credentials?.httpHeaderAuth?.name === 'NocoDB API', 'Get High Fit Jobs missing NocoDB API credential');
if (fail) process.exit(1);
console.log('All Task 1 assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Get High Fit Jobs is httpRequest GET with D-04 filter containing (Status,eq,Evaluated), ("Fit Tier",eq,High), ("Tailored CV Text",is,blank)
    - Unwrap High Fit Jobs and Unwrap Profile Code nodes exist with id: record.id (string UUID)
    - Get Profile is httpRequest GET with executeOnce: true and NOCODB_TABLE_PROFILE URL
    - Connections: Get High Fit Jobs → Unwrap High Fit Jobs → Get Profile → Unwrap Profile → Build Tailoring Prompt
    - Build Tailoring Prompt references $('Unwrap Profile') and $('Unwrap High Fit Jobs'), sets _recordId from job.json.id
    - No airtableTokenApi credentials on Get High Fit Jobs or Get Profile
    - Both GET nodes use httpHeaderAuth credential "NocoDB API"
  </acceptance_criteria>
  <done>
    NocoDB read path complete: 2 HTTP GET nodes, 2 Unwrap Code nodes, connections wired, Build Tailoring Prompt updated for flat Unwrap items and string UUID record IDs (D-05 override applied).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Convert to File + Upload DOCX + replace Update Pipeline Record with conditional NocoDB PATCH</name>
  <files>workflows/03-tailor.json</files>
  <read_first>
    - workflows/03-tailor.json — after Task 1: "Render DOCX" (lines 184-206), "HTTP Request" upload node (lines 641-675), "Update Pipeline Record" Airtable node (lines 207-625), connections Render DOCX → HTTP Request → Update Pipeline Record
    - workflows/02-evaluator.json lines 193-229 — Update Job Record PATCH reference (auth, retry, jsonBody id+fields format)
    - .planning/phases/05-tailor-workflow-attachments-migration/05-PATTERNS.md lines 246-378 — Convert to File, Upload DOCX, Update Pipeline Record configs
    - .planning/phases/05-tailor-workflow-attachments-migration/05-RESEARCH.md Pattern 3-4 — multipart upload + D-06 conditional PATCH IIFE
    - scripts/nocodb-schema.json — Tailored CV field type Attachment
  </read_first>
  <action>
    Replace the Airtable upload/update pipeline with NocoDB storage upload + v3 PATCH. Mixed API versions per research: v2 storage upload, v3 data PATCH.

    A) Insert "Convert to File" node between Render DOCX and Upload DOCX (D-02 planner correction — base64 JSON cannot go directly to multipart):
       - type: n8n-nodes-base.convertToFile, typeVersion: 1.1
       - name: Convert to File
       - position: [4448, 64] (between Render DOCX ~[4320,64] and Upload DOCX)
       - parameters:
         operation: toBinary
         sourceProperty: docx_base64
         options.fileName: ={{ $('Render DOCX').item.json.filename }}
         options.mimeType: application/vnd.openxmlformats-officedocument.wordprocessingml.document
       - Generate unique id
       - Satisfies D-02 intent: n8n native binary handling, no custom Code node for base64 decode

    B) Replace existing "HTTP Request" node (Airtable content.airtable.com upload) with "Upload DOCX" (D-01, rename per research):
       - Keep node id if possible; rename name from "HTTP Request" to "Upload DOCX"
       - method: POST
       - url: ={{ $env.NOCODB_URL }}/api/v2/storage/upload
       - authentication: genericCredentialType, genericAuthType: httpHeaderAuth
       - sendBody: true, contentType: multipart-form-data
       - bodyParameters.parameters: [{ parameterType: formBinaryData, name: file, inputDataFieldName: data }]
       - continueOnFail: true (D-06 graceful degradation)
       - alwaysOutputData: true
       - retryOnFail: true, maxTries: 2, waitBetweenTries: 5000
       - credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
       - Remove old Airtable content URL, jsonBody with base64, and "Header Auth account" Airtable credential
       - position: [4576, 64]

    C) Replace "Update Pipeline Record" Airtable node with HTTP PATCH (D-03 single combined PATCH):
       - Remove entire Airtable update block (operation update, base, table, columns schema, matchingColumns, airtableTokenApi)
       - type: n8n-nodes-base.httpRequest, typeVersion: 4.2, method: PATCH
       - url: ={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records
       - authentication: genericCredentialType, genericAuthType: httpHeaderAuth
       - sendHeaders: Content-Type application/json
       - sendBody: true, specifyBody: json
       - jsonBody (exact IIFE expression for D-03 + D-06):
         ={{ (() => { const parse = $('Parse Tailored CV').item.json; const upload = $('Upload DOCX').item.json; const fields = { 'Tailored CV Text': parse.tailoredCV, 'CV Tailoring Cost': parse.cost }; if (Array.isArray(upload) && upload.length > 0 && upload[0].url) { fields['Tailored CV'] = upload; } return JSON.stringify({ id: parse.recordId, fields }); })() }}
       - If first test shows upload wrapped (e.g. upload.data array), adjust condition to check upload.data — document in SUMMARY if adjusted
       - retryOnFail: true, maxTries: 3, waitBetweenTries: 5000
       - Do NOT add continueOnFail on PATCH — text+cost write should retry on failure
       - credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
       - Keep position ~[4832, 64]

    D) Update connections block for upload chain:
       - Render DOCX → Convert to File (replace Render DOCX → HTTP Request)
       - Convert to File → Upload DOCX
       - Upload DOCX → Update Pipeline Record (replace HTTP Request → Update Pipeline Record)
       - Update Pipeline Record → Wait 2s (unchanged)
       - Remove any connection referencing node name "HTTP Request"

    E) Verify "Parse Tailored CV" Code node still outputs recordId: loopItem._recordId — no structural change needed if Task 1 set _recordId from job.json.id (string UUID). Confirm jsCode contains recordId: loopItem._recordId.

    F) Final sweep: grep entire workflow JSON for remaining Airtable references:
       - No n8n-nodes-base.airtable nodes
       - No airtableTokenApi credentials
       - No content.airtable.com URLs
       - No $('Get Profile') or $('Get New Jobs') stale refs (tailor uses Get High Fit Jobs)
       - settings.binaryMode must remain "separate" (required for Convert to File chain)
  </action>
  <verify>
    <automated>node -e "
let fail = false;
const assert = (cond, msg) => { if (!cond) { fail = true; console.error('FAIL: ' + msg); } };
const wf = require('./workflows/03-tailor.json');
const conns = wf.connections || {};
const fullJson = JSON.stringify(wf);
// No Airtable nodes
const airtable = wf.nodes.filter(n => n.type === 'n8n-nodes-base.airtable');
assert(airtable.length === 0, 'Airtable nodes remain: ' + airtable.map(n=>n.name).join(', '));
// Convert to File
const ctf = wf.nodes.find(n => n.name === 'Convert to File');
assert(ctf, 'Convert to File missing');
assert(ctf.type === 'n8n-nodes-base.convertToFile', 'Convert to File wrong type');
assert(ctf.parameters.sourceProperty === 'docx_base64', 'Convert to File wrong sourceProperty');
// Upload DOCX
const upload = wf.nodes.find(n => n.name === 'Upload DOCX');
assert(upload, 'Upload DOCX missing');
assert(!wf.nodes.find(n => n.name === 'HTTP Request'), 'Generic HTTP Request node still exists');
assert(upload.parameters.url.includes('/api/v2/storage/upload'), 'Upload wrong URL');
assert(upload.continueOnFail === true, 'Upload DOCX missing continueOnFail (D-06)');
assert(upload.parameters.contentType === 'multipart-form-data', 'Upload not multipart');
const bodyParams = JSON.stringify(upload.parameters.bodyParameters);
assert(bodyParams.includes('formBinaryData'), 'Upload missing formBinaryData');
assert(bodyParams.includes('\"file\"'), 'Upload missing file field name');
// Update Pipeline Record PATCH
const update = wf.nodes.find(n => n.name === 'Update Pipeline Record');
assert(update.type === 'n8n-nodes-base.httpRequest', 'Update Pipeline Record not HTTP');
assert(update.parameters.method === 'PATCH', 'Update Pipeline Record not PATCH');
assert(update.parameters.jsonBody.includes('Tailored CV Text'), 'PATCH missing Tailored CV Text');
assert(update.parameters.jsonBody.includes('CV Tailoring Cost'), 'PATCH missing CV Tailoring Cost');
assert(update.parameters.jsonBody.includes('Tailored CV'), 'PATCH missing Tailored CV attachment logic');
assert(update.parameters.jsonBody.includes('parse.recordId'), 'PATCH missing recordId from Parse Tailored CV');
assert(update.retryOnFail === true, 'PATCH missing retryOnFail');
// Connections
assert(conns['Render DOCX']?.main[0][0].node === 'Convert to File', 'Render DOCX → Convert to File missing');
assert(conns['Convert to File']?.main[0][0].node === 'Upload DOCX', 'Convert to File → Upload DOCX missing');
assert(conns['Upload DOCX']?.main[0][0].node === 'Update Pipeline Record', 'Upload DOCX → Update Pipeline Record missing');
assert(wf.settings?.binaryMode === 'separate', 'binaryMode not separate');
// No stale refs
assert(!fullJson.includes('content.airtable.com'), 'Airtable content URL remains');
assert(!fullJson.includes('airtableTokenApi'), 'airtableTokenApi credential remains');
assert(!fullJson.includes(\"$('Get Profile')\"), 'Stale Get Profile ref');
assert(!fullJson.includes(\"$('Get High Fit Jobs')\"), 'Stale Get High Fit Jobs ref');
// HTTP node count: 2 GET + 1 Render + 1 Upload + 1 PATCH = 5 httpRequest
const httpNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.httpRequest');
assert(httpNodes.length === 5, 'Expected 5 httpRequest nodes, got ' + httpNodes.length);
if (fail) process.exit(1);
console.log('All Task 2 assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Convert to File node exists with operation toBinary, sourceProperty docx_base64, filename from Render DOCX
    - Upload DOCX node POSTs to /api/v2/storage/upload with multipart formBinaryData field file, inputDataFieldName data
    - Upload DOCX has continueOnFail: true and alwaysOutputData: true (D-06)
    - Update Pipeline Record is HTTP PATCH to v3 data API with IIFE jsonBody setting Tailored CV Text, CV Tailoring Cost, and conditional Tailored CV array (D-03)
    - Connections: Render DOCX → Convert to File → Upload DOCX → Update Pipeline Record → Wait 2s
    - Zero Airtable nodes and zero airtableTokenApi credentials in workflow
    - All NocoDB HTTP nodes use httpHeaderAuth "NocoDB API"
    - binaryMode: separate preserved in settings
  </acceptance_criteria>
  <done>
    Upload pipeline migrated: base64→binary via Convert to File, NocoDB v2 multipart upload, v3 PATCH with combined fields and D-06 conditional attachment. No Airtable nodes remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification — TAIL-03 attachment UI + D-06 graceful degradation</name>
  <files>workflows/03-tailor.json</files>
  <read_first>
    - .planning/phases/05-tailor-workflow-attachments-migration/05-VALIDATION.md — Manual Test Cases A through E
    - .planning/phases/05-tailor-workflow-attachments-migration/05-RESEARCH.md lines 574-608 — full manual test protocol
  </read_first>
  <action>
    Re-import or sync workflows/03-tailor.json in n8n UI (git edit alone does not update running n8n instance). Ensure NocoDB API credential exists with xc-token header. Ensure at least one Pipeline record: Status=Evaluated, Fit Tier=High, Tailored CV Text blank.

    Execute manual test cases from 05-VALIDATION.md:

    Test Case A (TAIL-01): Execute workflow; confirm Unwrap Profile shows flat Profile fields; Unwrap High Fit Jobs shows only matching jobs with id (string UUID).

    Test Case B (TAIL-02): Trace Parse Tailored CV → Render DOCX (docx_base64 present) → Convert to File (binary data) → Upload DOCX (HTTP 200, array with url) → Update Pipeline Record (PATCH 200, body includes Tailored CV when upload succeeded).

    Test Case C (TAIL-03): Open tailored Pipeline record in NocoDB UI at NOCODB_URL — confirm Tailored CV Text populated, Tailored CV attachment shows DOCX with preview and download.

    Test Case D (D-06): Temporarily break Upload DOCX URL (e.g. append /broken); re-run one job; confirm Upload fails but PATCH still saves Tailored CV Text + CV Tailoring Cost; Tailored CV attachment empty/unchanged. Restore URL after test.

    Test Case E: When no untailored high-fit jobs exist, Unwrap High Fit Jobs returns _empty sentinel; workflow handles gracefully.
  </action>
  <verify>
    <automated>node -e "
const wf = require('./workflows/03-tailor.json');
const airtable = wf.nodes.filter(n => n.type === 'n8n-nodes-base.airtable');
if (airtable.length > 0) { console.error('FAIL: Airtable nodes still present'); process.exit(1); }
console.log('Pre-manual structural check passed — proceed with n8n UI verification');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - TAIL-03: DOCX attachment visible, previewable, and downloadable in NocoDB Pipeline record UI after successful workflow run
    - TAIL-02: Upload DOCX returns attachment array with url; PATCH persists Tailored CV field
    - D-06: Upload failure does not prevent Tailored CV Text and CV Tailoring Cost from being saved
    - Tailored CV markdown content matches pre-migration quality (spot-check one record)
  </acceptance_criteria>
  <what-built>Migrated Tailor workflow with NocoDB reads, storage upload, and conditional PATCH — all automated structural checks pass</what-built>
  <how-to-verify>
    1. Import workflows/03-tailor.json into n8n (or activate synced copy)
    2. Ensure NocoDB running with test Pipeline record (Status=Evaluated, Fit Tier=High, blank Tailored CV Text)
    3. Execute "JobSignal - Workflow 3 - Tailor" via Manual Trigger
    4. In n8n execution view: verify each node output through Upload DOCX and Update Pipeline Record
    5. Open NocoDB UI → Pipeline table → tailored record → confirm Tailored CV attachment preview + download (TAIL-03)
    6. Optional D-06 test: break upload URL, re-run, confirm text fields still saved
  </how-to-verify>
  <resume-signal>Type "approved" after TAIL-03 UI verification passes, or describe issues found</resume-signal>
  <done>
    Human confirms TAIL-03: CV DOCX attachment visible in NocoDB UI. D-06 graceful degradation verified or documented as acceptable if storage unavailable in test environment.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n nodes → NocoDB HTTP API (v3 data + v2 storage) | Workflow-to-database communication — xc-token auth on every HTTP Request node |
| n8n → cv-renderer sidecar :3456 | Internal Docker network — markdown CV contains PII |
| Upload DOCX → NocoDB storage volume | DOCX files persisted to nocodb_data Docker volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Upload DOCX + all NocoDB HTTP nodes | mitigate | xc-token in n8n "NocoDB API" HTTP Header Auth credential only; remove Airtable PAT and Header Auth account from tailor workflow |
| T-05-02 | Denial of Service | Upload DOCX multipart upload | mitigate | NC_ATTACHMENT_FIELD_SIZE=20971520 (20MB) in docker-compose; CV renderer output well under limit |
| T-05-03 | Tampering | Update Pipeline Record PATCH | accept | PATCH writes to user's own Pipeline table; no external user input. Token scoped to NocoDB base |
| T-05-04 | Information Disclosure | CV DOCX in NocoDB attachment storage | accept | Single-user pipeline; PII in CV is expected data. NocoDB port 8080 should be internal-only in production |
| T-05-SC | Tampering | npm/pip installs | mitigate | No packages installed — workflow JSON edits only. Slopcheck: no eval(), no obfuscated jsCode |

</threat_model>

<verification>
## Overall Phase Checks

Run combined automated validation after Tasks 1-2:
```bash
node -e "
let fail = false;
const assert = (cond, msg) => { if (!cond) { fail = true; console.error('FAIL: ' + msg); } };
const wf = require('./workflows/03-tailor.json');
const fullJson = JSON.stringify(wf);
assert(wf.nodes.filter(n => n.type === 'n8n-nodes-base.airtable').length === 0, 'Airtable nodes remain');
assert(wf.nodes.some(n => n.name === 'Unwrap High Fit Jobs'), 'Missing Unwrap High Fit Jobs');
assert(wf.nodes.some(n => n.name === 'Unwrap Profile'), 'Missing Unwrap Profile');
assert(wf.nodes.some(n => n.name === 'Convert to File'), 'Missing Convert to File');
assert(wf.nodes.some(n => n.name === 'Upload DOCX'), 'Missing Upload DOCX');
const upload = wf.nodes.find(n => n.name === 'Upload DOCX');
assert(upload.continueOnFail === true, 'D-06 continueOnFail missing');
const update = wf.nodes.find(n => n.name === 'Update Pipeline Record');
assert(update.parameters.method === 'PATCH', 'Update not PATCH');
assert(!fullJson.includes('airtable'), 'Airtable string remains in workflow');
if (fail) process.exit(1);
console.log('Phase 5 structural verification passed');
"
```

Manual verification (Task 3 checkpoint) required for TAIL-03 before phase sign-off.
</verification>

<success_criteria>
- All 3 Airtable nodes replaced; Airtable content upload replaced with NocoDB v2 storage multipart upload
- 2 Unwrap Code nodes + 1 Convert to File node inserted; Upload DOCX renamed from generic HTTP Request
- D-04 (is,blank) filter on Get High Fit Jobs; D-05 override uses record.id string UUID for PATCH
- D-06: Upload DOCX continueOnFail true; PATCH saves text+cost even when upload fails
- Combined PATCH writes Tailored CV Text, CV Tailoring Cost, and Tailored CV attachment array (D-03)
- Automated verification scripts exit 0 after Tasks 1-2
- Human verification confirms TAIL-03: DOCX attachment preview and download in NocoDB UI
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Description |
|----------|------|-------------|
| Node: "Unwrap High Fit Jobs" | Code (new) | Flattens NocoDB v3 GET response; exposes id: record.id (string UUID) |
| Node: "Unwrap Profile" | Code (new) | Flattens Profile GET response; executeOnce upstream on Get Profile |
| Node: "Get High Fit Jobs" | HTTP GET (replaced) | NocoDB Pipeline query with D-04 (is,blank) filter |
| Node: "Get Profile" | HTTP GET (replaced) | NocoDB Profile table read |
| Node: "Convert to File" | convertToFile (new) | Moves docx_base64 from Render DOCX to binary data property |
| Node: "Upload DOCX" | HTTP POST multipart (replaced) | NocoDB v2 /api/v2/storage/upload; continueOnFail true |
| Node: "Update Pipeline Record" | HTTP PATCH (replaced) | v3 PATCH with conditional Tailored CV attachment (D-03, D-06) |
| Changed: "Build Tailoring Prompt" | Code edit | $('Unwrap Profile'), $('Unwrap High Fit Jobs'), job.json.id → _recordId |
| Unchanged: "Render DOCX" | HTTP sidecar | Still calls cv-renderer:3456/render |
| Unchanged: "Parse Tailored CV" | Code | recordId from loopItem._recordId (string UUID after Task 1) |
| File: workflows/03-tailor.json | Workflow JSON | Complete migrated Tailor workflow |

<output>
After completion, create `.planning/phases/05-tailor-workflow-attachments-migration/05-01-SUMMARY.md` when done.

Include in SUMMARY:
- Plan: 05-01-PLAN.md executed
- Tasks completed: 3/3 (Task 3 requires human approval)
- Files modified: workflows/03-tailor.json
- D-05 override note: used record.id string UUID, not Id integer
- Upload response shape confirmed (array at root vs wrapped — document actual n8n output)
- Artifacts this phase produces section copied from plan
</output>
