---
phase: 03-scanner-workflows-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.example.yml]
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04]
user_setup:
  - service: n8n
    why: "HTTP Header Auth credential for NocoDB API access"
    dashboard_config:
      - task: "Create HTTP Header Auth credential named 'NocoDB API' with header name 'xc-token' and header value equal to the NocoDB API token"
        location: "n8n UI -> Settings -> Credentials -> Add Credential -> HTTP Header Auth"

must_haves:
  truths:
    - "docker-compose.example.yml includes all NOCODB_* env vars needed by scanner HTTP Request nodes"
    - "Every HTTP Request node can reference {{ $env.NOCODB_URL }}, {{ $env.NOCODB_BASE_ID }}, and {{ $env.NOCODB_TABLE_* }} without undefined env var errors"
    - "User can create the 'NocoDB API' HTTP Header Auth credential in n8n UI with xc-token header"
    - "Existing NOCDB_HOST and NOCDB_API_TOKEN env vars remain unchanged for backward compatibility"
  artifacts:
    - path: "docker-compose.example.yml"
      provides: "NOCODB_* environment variables for n8n service"
      contains: "NOCODB_URL"
      min_lines: 108
    - path: "docker-compose.example.yml"
      provides: "NOCODB_TABLE_PIPELINE env var referenced by POST node"
      contains: "NOCODB_TABLE_PIPELINE"
    - path: "docker-compose.example.yml"
      provides: "NOCODB_TABLE_COMPANIES env var referenced by GET Tracked Companies"
      contains: "NOCODB_TABLE_COMPANIES"
  key_links:
    - from: "docker-compose.example.yml n8n service env"
      to: "HTTP Request nodes in all 4 scanner workflows"
      via: "{{ $env.NOCODB_* }} expression references"
      pattern: "\\$env\\.NOCODB_"
---

<objective>
Add the NocoDB environment variables required by all 4 scanner workflow HTTP Request nodes, and document the n8n credential setup needed for NocoDB API authentication.

**Purpose:** Every scanner HTTP Request node references `{{ $env.NOCODB_URL }}`, `{{ $env.NOCODB_BASE_ID }}`, and `{{ $env.NOCODB_TABLE_* }}` environment variables. Without these, all HTTP nodes fail with unresolved expressions. The credential setup ensures every scanner can authenticate to NocoDB via the `xc-token` header.

**Output:**
- `docker-compose.example.yml` — updated with NOCODB_* env vars in the n8n service section
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-bootstrap/01-CONTEXT.md
@.planning/phases/02-data-migration/02-CONTEXT.md
@.planning/phases/03-scanner-workflows-migration/03-CONTEXT.md
@.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md
@.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md

@docker-compose.example.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NOCODB_* environment variables to docker-compose.example.yml</name>

  <files>docker-compose.example.yml</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/docker-compose.example.yml (lines 40-63 — n8n service environment section)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  Add the following NOCODB_* environment variables to the n8n service `environment:` block in `docker-compose.example.yml`. Insert them after the existing `NOCDB_HOST=http://nocodb:8080` line (line 57), indented with the same spacing (2 leading spaces, dash, space).

  The existing env vars on lines 56-57 are:
    - NOCDB_API_TOKEN=REPLACE_WITH_TOKEN
    - NOCDB_HOST=http://nocodb:8080

  Add these new lines after `NOCDB_HOST`:
    - NOCODB_URL=http://nocodb:8080     # Canonical name per D-03 (alias for NOCDB_HOST)
    - NOCODB_BASE_ID=<p_xxxxx>          # From bootstrap output — run: python scripts/nocodb_bootstrap.py
    - NOCODB_TABLE_PROFILE=<m_xxxxx>    # From bootstrap output
    - NOCODB_TABLE_COMPANIES=<m_xxxxx>  # From bootstrap output
    - NOCODB_TABLE_PIPELINE=<m_xxxxx>   # From bootstrap output
    - NOCODB_TABLE_QUERIES=<m_xxxxx>    # From bootstrap output

  KEY RULES:
  - Indent with 2 spaces before the dash (`  - VAR=value`), matching the existing pattern
  - Use `{{ $env.NOCODB_URL }}` NOT `{{ $env.NOCDB_HOST }}` in all HTTP Request nodes (Plan 02-05)
  - `NOCDB_HOST` and `NOCDB_API_TOKEN` keep existing names for backward compat — do NOT rename or remove them
  - `NOCODB_URL=http://nocodb:8080` is an alias for `NOCDB_HOST=http://nocodb:8080` — same value, different name
  - Placeholder values use `<p_xxxxx>` for base ID and `<m_xxxxx>` for table IDs, matching bootstrap script output format
  - Add a blank line comment above the new block: `      # NocoDB table IDs — replace with values from bootstrap script output`

  IMPORTANT: The existing `NOCDB_HOST` and `NOCDB_API_TOKEN` env vars are referenced by the bootstrap script. Do NOT touch them.
  </action>

  <verify>
  <automated>grep -c 'NOCODB_URL=' docker-compose.example.yml</automated>
  <automated>grep -c 'NOCODB_BASE_ID=' docker-compose.example.yml</automated>
  <automated>grep -c 'NOCODB_TABLE_PROFILE=' docker-compose.example.yml</automated>
  <automated>grep -c 'NOCODB_TABLE_PIPELINE=' docker-compose.example.yml</automated>
  <human-check>Inspect docker-compose.example.yml lines 56-70 to verify all 6 NOCODB_* env vars are present with correct indentation: `  - NOCODB_*=...` (2-space indent)</human-check>
  </verify>

  <acceptance_criteria>
  - `docker-compose.example.yml` contains `NOCODB_URL`, `NOCODB_BASE_ID`, `NOCODB_TABLE_PROFILE`, `NOCODB_TABLE_COMPANIES`, `NOCODB_TABLE_PIPELINE`, and `NOCODB_TABLE_QUERIES` in the n8n service environment block
  - All vars use 2-space dash indent, matching existing pattern
  - Existing `NOCDB_HOST` and `NOCDB_API_TOKEN` are unmodified
  - Placeholder values `NOCODB_BASE_ID=<p_xxxxx>` and table IDs use `<m_xxxxx>` format
  </acceptance_criteria>

  <done>
  docker-compose.example.yml has 6 new NOCODB_* env vars in the n8n service environment section, correctly indented, with existing NOCDB_* vars preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document n8n NocoDB API credential setup and env var usage</name>

  <files>docker-compose.example.yml</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/docker-compose.example.yml (full file — see the existing comment style at lines 46-57)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  Add a comment block to `docker-compose.example.yml` in the n8n service environment section (before the NOCODB_* env vars you added in Task 1) documenting:

  1. That the HTTP Header Auth credential named "NocoDB API" must be created in n8n
  2. The credential config: Header Name = `xc-token`, Header Value = value of `NOCDB_API_TOKEN`
  3. That this credential is referenced by all 4 scanner workflows
  4. How to obtain the `NOCDB_API_TOKEN` value: it was generated by the bootstrap script output

  Use this comment block format (matching the existing `# Replace with your domain` comment style on line 54):
  ```yaml
        # NocoDB API credential setup in n8n UI:
        #   1. Create HTTP Header Auth credential named "NocoDB API"
        #   2. Header Name: xc-token
        #   3. Header Value: the NOCDB_API_TOKEN value from bootstrap script output
        #   4. All 4 scanner HTTP Request nodes reference this credential
  ```

  Place this comment directly above the NOCODB_BASE_ID comment line from Task 1.

  Also add a brief comment after `NOCODB_URL` line explaining the naming:
  ```yaml
      - NOCODB_URL=http://nocodb:8080     # Canonical name per D-03 (alias for NOCDB_HOST)
  ```
  (This comment is already specified in Task 1 — ensure consistency.)
  </action>

  <verify>
  <automated>grep -c 'NocoDB API credential setup' docker-compose.example.yml</automated>
  <automated>grep -c 'xc-token' docker-compose.example.yml</automated>
  <human-check>Verify the credential setup comment appears before the NOCODB_* env vars and correctly documents the 4 setup steps</human-check>
  </verify>

  <acceptance_criteria>
  - docker-compose.example.yml n8n environment section includes a comment block documenting:
    1. HTTP Header Auth credential type
    2. Credential name "NocoDB API"
    3. Header name "xc-token"
    4. Token value from NOCDB_API_TOKEN env var
  - Comment block placed before NOCODB_* env vars
  </acceptance_criteria>

  <done>
  docker-compose.example.yml documents the n8n credential setup process and contains all 6 NOCODB_* env vars.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n → NocoDB (Docker internal) | HTTP API calls with xc-token authentication over internal Docker network, port 8080. No external exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | docker-compose.example.yml (env vars) | mitigate | Placeholder values `<p_xxxxx>` and `<m_xxxxx>` used instead of real IDs. Real values set by user after bootstrap. |
| T-03-02 | Tampering | docker-compose.example.yml (comment text) | accept | Comments are documentation only; no execution impact. |
| T-03-SC | Tampering | docker-compose.example.yml | mitigate | No packages installed. This file only adds env vars and comments — no executable code. slopcheck: N/A. |
</threat_model>

<verification>
1. `grep -c 'NOCODB_URL=' docker-compose.example.yml` returns 1
2. `grep -c 'xc-token' docker-compose.example.yml` returns 1
3. `grep -c 'NOCDB_HOST=' docker-compose.example.yml` returns 1 (existing preserved)
4. Visual inspection: new vars at correct indentation, between existing NOCDB_HOST and blank line before next service
</verification>

<success_criteria>
1. docker-compose.example.yml has all 6 NOCODB_* env vars correctly placed in n8n service environment block
2. Credential setup comment documents "NocoDB API" HTTP Header Auth with xc-token header
3. Existing NOCDB_HOST and NOCDB_API_TOKEN env vars are present and unmodified
4. All env vars use placeholder values that get replaced by user after bootstrap script runs
</success_criteria>

## Phase 3 — Artifacts This Plan Produces

| Artifact | Type | Description |
|----------|------|-------------|
| `docker-compose.example.yml` (modified) | config | Added NOCODB_* env vars and credential setup comments |

## Artifacts This Phase Produces (cumulative across all plans)

| Artifact | Plan | Description |
|----------|------|-------------|
| docker-compose.example.yml (modified) | 03-01 | NOCODB_* env vars + credential docs |
| workflows/01a-scanner-greenhouse.json (modified) | 03-02 | NocoDB HTTP nodes replacing Airtable |
| workflows/01b-scanner-ashby.json (modified) | 03-03 | NocoDB HTTP nodes replacing Airtable |
| workflows/01c-scanner-lever.json (modified) | 03-04 | NocoDB HTTP nodes replacing Airtable |
| workflows/01d-scanner-jobspy.json (modified) | 03-05 | NocoDB HTTP nodes + Get Profile + code edit |
</objective>

<output>
Create `.planning/phases/03-scanner-workflows-migration/03-01-SUMMARY.md` when done
</output>
