---
phase: 03-scanner-workflows-migration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [workflows/01a-scanner-greenhouse.json]
autonomous: true
requirements: [SCAN-01]

must_haves:
  truths:
    - "D-01: Uses n8n HTTP Request nodes (NOT native NocoDB v2 node)"
    - "D-06: Unwrap transform nodes key on response.records (NOT response.list)"
    - "D-07: Column names use Title Case matching Airtable fields"
    - "D-08: Has own 'Get Profile' HTTP Request node"
    - "D-10: Uses field projection (?fields=Job+ID) for dedup queries"
    - "Greenhouse scanner (1a) reads Profile from NocoDB instead of Airtable — unwrap transform produces flat item with Target Geography, Core Skills"
    - "Greenhouse scanner (1a) reads Tracked Companies from NocoDB with where filter (Enabled=true AND Scan Method=Greenhouse API)"
    - "Greenhouse scanner (1a) reads existing Pipeline Job IDs from NocoDB via field projection (?fields=Job+ID) — the 'Deduplicate vs Pipeline' Code node receives flat items and deduplicates correctly"
    - "Greenhouse scanner (1a) creates new Pipeline records in NocoDB via POST with { fields: { ... } } body — all 11 column mappings preserved"
    - "Existing Code nodes (Parse & Filter Jobs, Deduplicate vs Pipeline) receive data in flat format and work unchanged"
  artifacts:
    - path: "workflows/01a-scanner-greenhouse.json"
      provides: "NocoDB-based scanner with HTTP Request nodes replacing Airtable"
      contains: "Get Profile"
      min_lines: 400
    - path: "workflows/01a-scanner-greenhouse.json"
      provides: "HTTP POST creating Pipeline records via NocoDB Data API v3"
      contains: "\"fields\""
    - path: "workflows/01a-scanner-greenhouse.json"
      provides: "Unwrap transform nodes after each NocoDB GET request"
      contains: "code"
    - path: "workflows/01a-scanner-greenhouse.json"
      provides: "Where clause filtering for Tracked Companies by Scan Method"
      contains: "Scan Method"
    - path: "workflows/01a-scanner-greenhouse.json"
      provides: "Field projection for dedup Job ID query"
      contains: "fields=Job+ID"
  key_links:
    - from: "Get Profile HTTP Request node"
      to: "Unwrap Profile Code node"
      via: "output connection"
    - from: "Unwrap Profile Code node"
      to: "Get Tracked Companies HTTP Request node"
      via: "input connection"
    - from: "Get Tracked Companies HTTP Request node"
      to: "Unwrap Tracked Companies Code node"
      via: "output connection"
    - from: "Unwrap Tracked Companies Code node"
      to: "Loop Companies"
      via: "input connection"
    - from: "Get Existing Job IDs HTTP Request node"
      to: "Unwrap Existing Job IDs Code node"
      via: "output connection"
    - from: "Unwrap Existing Job IDs Code node"
      to: "Deduplicate vs Pipeline"
      via: "input connection"
    - from: "HTTP Request POST /records"
      to: "NocoDB Pipeline table"
      via: "{ fields: { ... } } body"
      pattern: "fields"
---

<objective>
Replace all 4 Airtable nodes in the Greenhouse scanner workflow (01a) with n8n HTTP Request nodes targeting NocoDB Data API v3, plus Code node unwrap transforms after each GET request. This establishes the reference pattern for all other scanner workflows.

**Purpose:** Greenhouse scanner (1a) is the most complex scanner with all 4 Airtable node types. Migrating it first establishes the reference pattern — the `where` filter, unwrap transform, field projection, and POST body format that Ashby (1b), Lever (1c), and JobSpy (1d) will replicate.

**Output:**
- `workflows/01a-scanner-greenhouse.json` — 4 Airtable nodes replaced with 4 HTTP Request nodes + 3 Unwrap Code nodes
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scanner-workflows-migration/03-CONTEXT.md
@.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md
@.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md

@workflows/01a-scanner-greenhouse.json
@docker-compose.example.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Get Profile and Get Tracked Companies Airtable nodes with HTTP Request GET + Unwrap Code nodes</name>

  <files>workflows/01a-scanner-greenhouse.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Sections: Pattern 1A, Pattern 1B — full node JSON structures)
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md (Sections: Response Shape Correction, Pattern 1, Pattern 2, Pitfalls 1-4)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01a-scanner-greenhouse.json (full file — focus on the nodes list and connections object)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  In `workflows/01a-scanner-greenhouse.json`, replace the "Get Profile" Airtable node and "Get Tracked Companies" Airtable node with HTTP Request nodes + unwrap Code nodes. Follow the node structures from PATTERNS.md Patterns 1A and 1B exactly.

  **Step 1: Replace "Get Profile" Airtable node** (currently position [352, -400])

  Delete the existing Airtable node for "Get Profile" (currently a `n8n-nodes-base.airtable` node with operation "search" on the Profile table). Replace it with TWO nodes:

  A) **HTTP Request GET** node named "Get Profile":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [352, -400] (same position as original)
  - `parameters.method`: "GET"
  - `parameters.url`: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records"
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.options`: {} (empty object)
  - `retryOnFail`: true, `maxTries`: 3, `waitBetweenTries`: 5000, `alwaysOutputData`: true
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  B) **Code node** named "Unwrap Profile":
  - `type`: "n8n-nodes-base.code"
  - `typeVersion`: 2
  - `position`: [472, -400] (immediately to the right of Get Profile)
  - `parameters.jsCode`: The unwrap transform code from PATTERNS.md Pattern 1A — transforms `{ records: [...] }` into flat items, returns `_empty` sentinel when zero records
  - The JSCode field value (copy verbatim from PATTERNS.md):
    ```
    // Unwrap NocoDB Data API v3 response — transforms { records: [...], next, prev } into flat items
    const response = $input.first().json;
    const records = response.records || [];

    if (records.length === 0) {
      return [{ json: { _empty: true, _message: 'No Profile found' } }];
    }

    // Flatten: each record becomes { json: { ...fields, id: record.id } }
    // Downstream nodes access record.json['Target Geography'] directly (no .fields nesting)
    return records.map(record => ({
      json: {
        ...record.fields,
        id: record.id
      }
    }));
    ```

  **Step 2: Replace "Get Tracked Companies" Airtable node** (currently position [576, -400])

  Delete the existing Airtable node for "Get Tracked Companies" (currently a `n8n-nodes-base.airtable` node with `filterByFormula: "=AND({Enabled}=TRUE(),{Scan Method}='Greenhouse API')"`). Replace it with TWO nodes:

  A) **HTTP Request GET** node named "Get Tracked Companies":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [576, -400] (same position as original)
  - `parameters.method`: "GET"
  - `parameters.url`: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_COMPANIES }}/records?where=(Enabled,is,true)~and(\"Scan Method\",eq,\"Greenhouse API\")"
  - Note: Use `\"` to escape double quotes in the JSON string for the `where` parameter value
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.options`: {} (empty object)
  - `retryOnFail`: true, `maxTries`: 3, `waitBetweenTries`: 5000, `alwaysOutputData`: true
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  B) **Code node** named "Unwrap Tracked Companies":
  - `type`: "n8n-nodes-base.code"
  - `typeVersion`: 2
  - `position`: [688, -400] (immediately to the right of Get Tracked Companies)
  - `parameters.jsCode`:
    ```
    // Unwrap NocoDB Data API v3 response — transforms { records: [...], next, prev } into flat items
    const response = $input.first().json;
    const records = response.records || [];

    if (records.length === 0) {
      return [{ json: { _empty: true, _message: 'No enabled Tracked Companies found (Greenhouse)' } }];
    }

    return records.map(record => ({
      json: { ...record.fields, id: record.id }
    }));
    ```

  **Step 3: Update connections**

  The connections object must be updated to reflect the new node topology. The original connections were:
  - Manual Trigger → Get Profile → Get Tracked Companies → Loop Companies
  - Schedule Trigger → Get Profile → Get Tracked Companies → Loop Companies

  New connections:
  - Manual Trigger → Get Profile → Unwrap Profile → Get Tracked Companies → Unwrap Tracked Companies → Loop Companies
  - Schedule Trigger → Get Profile → Unwrap Profile → Get Tracked Companies → Unwrap Tracked Companies → Loop Companies

  Update the `connections` object in the workflow JSON:
  - Point "Manual Trigger" and "Schedule Trigger" outputs to "Get Profile" (unchanged)
  - Point "Get Profile" output to "Unwrap Profile" (was pointing directly to "Get Tracked Companies")
  - Point "Unwrap Profile" output to "Get Tracked Companies" (new connection)
  - Point "Get Tracked Companies" output to "Unwrap Tracked Companies" (was pointing directly to "Loop Companies")
  - Point "Unwrap Tracked Companies" output to "Loop Companies" (new connection)
  - "Loop Companies" connections unchanged (still points to "Prepare Company Data" output 0 and "Aggregate All Jobs" output 0)

  **CRITICAL — Correct response field:** Use `response.records` NOT `response.list`. The NocoDB Data API v3 returns `{ records: [...], next, prev }`. Using `response.list` would produce empty arrays and break downstream dedup.

  **CRITICAL — Correct API path:** Use `/api/v3/data/{baseId}/{tableId}/records` NOT `/api/v3/{base}/{table}/records`.

  **CRITICAL — Auth:** Reference the credential by name "NocoDB API" with id "TO_BE_CREATED". The user creates the credential before running.

  **CRITICAL — No retry on Get Company:** Add retry config (retryOnFail:true, maxTries:3, waitBetweenTries:5000) to Get Profile and Get Tracked Companies. Do NOT add continueOnFail:true — if Profile/Companies fail, the workflow can't proceed.
  </action>

  <verify>
  <automated>python3 -c "import json; wf=json.load(open('workflows/01a-scanner-greenhouse.json')); nodes={n['name']:n for n in wf['nodes']}; assert 'Unwrap Profile' in nodes, 'Unwrap Profile node missing'; assert 'Unwrap Tracked Companies' in nodes, 'Unwrap Tracked Companies node missing'; assert nodes['Get Profile']['type']=='n8n-nodes-base.httpRequest', 'Get Profile still Airtable'; assert nodes['Get Tracked Companies']['type']=='n8n-nodes-base.httpRequest', 'Get Tracked Companies still Airtable'; print('OK')"</automated>
  <automated>python3 -c "import json; wf=json.load(open('workflows/01a-scanner-greenhouse.json')); nodes={n['name']:n for n in wf['nodes']}; assert 'records' in nodes['Unwrap Profile']['parameters']['jsCode'], 'Unwrap uses wrong key (not records)'; print('Unwrap uses records key: OK')"</automated>
  <human-check>Inspect the connections in the workflow JSON to verify: ManualTrigger→Get Profile→Unwrap Profile→Get Tracked Companies→Unwrap Tracked Companies→Loop Companies. Also verify ScheduleTrigger follows same path.</human-check>
  </verify>

  <acceptance_criteria>
  - "Get Profile" node is now `n8n-nodes-base.httpRequest` with URL pointing to NOCODB_TABLE_PROFILE
  - "Unwrap Profile" Code node exists immediately after Get Profile, uses `response.records`
  - "Get Tracked Companies" node is now `n8n-nodes-base.httpRequest` with where filter `(Enabled,is,true)~and("Scan Method",eq,"Greenhouse API")`
  - "Unwrap Tracked Companies" Code node exists immediately after Get Tracked Companies
  - Connection chain: Trigger→Get Profile→Unwrap Profile→Get Tracked Companies→Unwrap Tracked Companies→Loop Companies
  - Both old Airtable nodes are removed from the nodes array
  - retryOnFail:true, maxTries:3, waitBetweenTries:5000 on both HTTP Request nodes
  </acceptance_criteria>

  <done>
  "Get Profile" (HTTP GET + Unwrap Code) and "Get Tracked Companies" (HTTP GET + Unwrap Code) replace the 2 Airtable nodes. Connections correct. NocoDB Data API v3 response field `records` used in unwrap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Get Existing Job IDs and Create Pipeline Records Airtable nodes with HTTP Request + Unwrap Code node</name>

  <files>workflows/01a-scanner-greenhouse.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Sections: Pattern 1C, Pattern 1D — full node JSON structures)
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md (Sections: Pattern 3, Field Mapping, Pitfall 4)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01a-scanner-greenhouse.json (focus on nodes list — find the "Get Existing Job IDs" and "Create Pipeline Records" entries plus connections)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  In the same `workflows/01a-scanner-greenhouse.json` file, replace the "Get Existing Job IDs" and "Create Pipeline Records" Airtable nodes.

  **Step 1: Replace "Get Existing Job IDs" Airtable node** (currently position [2496, -400])

  Delete the existing Airtable node. Replace it with TWO nodes:

  A) **HTTP Request GET** node named "Get Existing Job IDs":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [2496, -400] (same position)
  - `parameters.method`: "GET"
  - `parameters.url`: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?fields=Job+ID"
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.options`: {} (empty object)
  - `alwaysOutputData`: true
  - `executeOnce`: true (matches existing Airtable node pattern — this node runs once, not once per item)
  - Do NOT set retryOnFail/maxTries/waitBetweenTries — existing Airtable version has no retry config
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  B) **Code node** named "Unwrap Existing Job IDs":
  - `type`: "n8n-nodes-base.code"
  - `typeVersion`: 2
  - `position`: [2592, -400] (immediately to the right of Get Existing Job IDs)
  - `parameters.jsCode`:
    ```
    // Unwrap NocoDB Data API v3 — field-projected response (only 'Job ID' field)
    // The ?fields=Job+ID parameter returns records with only the 'Job ID' field populated
    const response = $input.first().json;
    const records = response.records || [];

    if (records.length === 0) {
      return [{ json: { _empty: true, _message: 'No existing Job IDs found' } }];
    }

    // Flatten fields so downstream dedup code accesses record.json['Job ID'] directly
    return records.map(record => ({
      json: {
        'Job ID': record.fields['Job ID'],
        id: record.id
      }
    }));
    ```

  **Step 2: Replace "Create Pipeline Records" Airtable node** (currently position [3248, -400])

  Delete the existing Airtable node. Replace with ONE node:

  A) **HTTP Request POST** node named "Create Pipeline Records":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [3248, -400] (same position as original)
  - `parameters.method`: "POST"
  - `parameters.url`: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records"
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.sendBody`: true
  - `parameters.specifyBody`: "json"
  - `parameters.jsonBody`: The JSON string body with column mappings in `{ "fields": { ... } }` format
  - `retryOnFail`: true, `maxTries`: 3, `waitBetweenTries`: 5000
  - `alwaysOutputData`: true
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  The `jsonBody` field value (note: the expression engine `={{ }}` wraps the entire body):
  ```
  ={
    "fields": {
      "Job ID": "{{ $json.jobId }}",
      "Job Title": "{{ $json.jobTitle }}",
      "Company": "{{ $json.company }}",
      "Location": "{{ $json.location }}",
      "Apply Link": "{{ $json.applyLink }}",
      "Job Description": "{{ $json.jobDescription }}",
      "Source": "{{ $json.source }}",
      "Source Query": "{{ $json.sourceQuery }}",
      "Source Tag": "{{ $json.sourceTag }}",
      "Discovery Date": "{{ $json.discoveryDate }}",
      "Status": "New"
    }
  }
  ```

  **Step 3: Update connections**

  Original connections involving these nodes:
  - Aggregate All Jobs → Get Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records

  New connections:
  - Aggregate All Jobs → Get Existing Job IDs → Unwrap Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records
  - "If" node still connects to "Create Pipeline Records" (output 0 — true branch)

  Update the `connections` object:
  - "Aggregate All Jobs" → "Get Existing Job IDs" (unchanged)
  - "Get Existing Job IDs" → "Unwrap Existing Job IDs" (was directly to "Deduplicate vs Pipeline")
  - "Unwrap Existing Job IDs" → "Deduplicate vs Pipeline" (new connection)
  - "Deduplicate vs Pipeline" → "If" (unchanged)
  - "If" → "Create Pipeline Records" (unchanged)

  **CRITICAL — POST body format:** The body MUST be `{ "fields": { "Job ID": "...", ... } }` NOT flat key-value pairs. NocoDB Data API v3 requires the `fields` wrapper. Without it, POST returns 400 Bad Request.

  **CRITICAL — No unwrap after POST:** The POST returns the created record directly. Downstream nodes read from the "If" node's output, not from the POST response. No unwrap node needed.

  **CRITICAL — continueOnFail:** Set `continueOnFail: true` on the POST node only. This matches existing behavior where one failed insert doesn't block subsequent records. Do NOT add `continueOnFail: true` to the GET "Get Existing Job IDs" node.

  **CRITICAL — Use `fields=Job+ID` (URL-encoded space):** The `?fields=Job+ID` parameter uses `+` for space (standard URL encoding). Do NOT use `%20` — n8n may double-encode it. The `+` character is safe in URL query strings.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01a-scanner-greenhouse.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}

# Check types changed
assert nodes['Get Existing Job IDs']['type']=='n8n-nodes-base.httpRequest', 'Existing Job IDs still Airtable'
assert nodes['Create Pipeline Records']['type']=='n8n-nodes-base.httpRequest', 'Create Records still Airtable'
assert 'Unwrap Existing Job IDs' in nodes, 'Unwrap node missing'

# Check POST body has fields wrapper
from urllib.parse import unquote
body = nodes['Create Pipeline Records']['parameters']['jsonBody']
assert '\"fields\"' in body, 'POST body missing fields wrapper: ' + body[:100]

# Check URL pattern
url = nodes['Get Existing Job IDs']['parameters']['url']
assert '/api/v3/data/' in url, 'Wrong API path'
assert 'fields=Job+ID' in url, 'Missing field projection'

# Check unwrap uses records key
assert 'response.records' in nodes['Unwrap Existing Job IDs']['parameters']['jsCode'], 'Unwrap uses wrong key'

# Verify no Airtable nodes remain for the ones we replaced
airtable_nodes = [n['name'] for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable']
print(f'Airtable nodes remaining: {airtable_nodes}')
assert 'Get Profile' not in airtable_nodes, 'Get Profile still Airtable'
assert 'Get Tracked Companies' not in airtable_nodes, 'Tracked Companies still Airtable'
assert 'Get Existing Job IDs' not in airtable_nodes, 'Existing Job IDs still Airtable'
assert 'Create Pipeline Records' not in airtable_nodes, 'Create Records still Airtable'
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Existing Job IDs" HTTP Request with fields projection
  - "Create Pipeline Records" HTTP POST with { "fields": { ... } } body
  - "Unwrap Existing Job IDs" Code node exists
  - Connections preserved: Aggregate -> Get Existing Job IDs -> Unwrap -> Deduplicate -> If -> Create Records
  - continueOnFail on POST only, not on GET
  </acceptance_criteria>

  <done>
  "Get Existing Job IDs" (HTTP GET + Unwrap) and "Create Pipeline Records" (HTTP POST) replace 2 Airtable nodes. All 4 Airtable nodes eliminated from Greenhouse scanner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> NocoDB (Docker internal) | HTTP API calls with xc-token auth over internal Docker network. Port 8080 not externally exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | workflows/01a-scanner-greenhouse.json | mitigate | Credential referenced by name only. Token stored in n8n credential store encrypted at rest. |
| T-03-02 | Tampering | HTTP Request URL injection | accept | All URLs use env var expressions. No user input in URL construction. |
| T-03-03 | Spoofing | NocoDB API auth bypass | mitigate | xc-token required for all NocoDB API calls. Token managed by n8n credential store. |
| T-03-SC | Supply Chain | No packages installed | accept | All changes to workflow JSON. No new dependencies. |
</threat_model>

<verification>
1. python3 -c "import json; wf=json.load(open('workflows/01a-scanner-greenhouse.json')); print(sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable'))" returns 0
2. Visual: n8n UI workflow has 3 Unwrap Code nodes in correct positions, no Airtable nodes
3. Manual: Execute workflow, inspect node outputs — unwrap produces flat items, POST creates NocoDB records
</verification>

<success_criteria>
1. All 4 Airtable nodes replaced with HTTP Request nodes + 3 Unwrap Code nodes
2. Get Tracked Companies uses where filter with "Greenhouse API" scan method
3. Get Existing Job IDs uses ?fields=Job+ID field projection
4. Create Pipeline Records uses POST with { "fields": { ... } } body
5. Workflow connections preserved end-to-end
6. Existing Code nodes receive flat data and work unchanged
</success_criteria>

## Plan 02 Artifacts

| Artifact | Type | Description |
|----------|------|-------------|
| workflows/01a-scanner-greenhouse.json (modified) | workflow | 4 Airtable nodes replaced with 4 HTTP Request + 3 Unwrap Code nodes |

## Phase 3 Artifacts (cumulative)

| Artifact | Plan | Description |
|----------|------|-------------|
| docker-compose.example.yml (modified) | 03-01 | NOCODB_* env vars + credential docs |
| workflows/01a-scanner-greenhouse.json (modified) | 03-02 | NocoDB HTTP nodes |
| workflows/01b-scanner-ashby.json (modified) | 03-03 | NocoDB HTTP nodes |
| workflows/01c-scanner-lever.json (modified) | 03-04 | NocoDB HTTP nodes |
| workflows/01d-scanner-jobspy.json (modified) | 03-05 | NocoDB HTTP nodes + Get Profile + code edit |

<output>
Create `.planning/phases/03-scanner-workflows-migration/03-02-SUMMARY.md` when done
</output>