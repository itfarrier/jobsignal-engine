---
phase: 03-scanner-workflows-migration
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified: [workflows/01d-scanner-jobspy.json]
autonomous: true
requirements: [SCAN-04]

must_haves:
  truths:
    - "JobSpy scanner (1d) reads Profile from NocoDB via a NEW Get Profile HTTP Request node + Unwrap — previously missing"
    - "JobSpy scanner (1d) reads Search Queries from NocoDB with where filter (Enabled=true AND Source Type=JobSpy)"
    - "JobSpy scanner (1d) reads existing Pipeline Job IDs from NocoDB via field projection"
    - "JobSpy scanner (1d) creates new Pipeline records in NocoDB via POST with { fields: { ... } } body including Salary Info field"
    - "JobSpy's Parse & Filter Jobs Code node reads userGeographies dynamically from Profile.Target Geography — NOT hardcoded"
    - "Existing dedup Code node receives flat format data and works unchanged"
  artifacts:
    - path: "workflows/01d-scanner-jobspy.json"
      provides: "New Get Profile HTTP Request + Unwrap node at the start of the workflow"
      contains: "Get Profile"
      min_lines: 400
    - path: "workflows/01d-scanner-jobspy.json"
      provides: "NocoDB-based Search Queries reader with where filter"
      contains: "\"Source Type\",eq,\"JobSpy\""
    - path: "workflows/01d-scanner-jobspy.json"
      provides: "Dynamic geography from Profile in Parse & Filter Jobs"
      contains: "profileData['Target Geography']"
    - path: "workflows/01d-scanner-jobspy.json"
      provides: "POST creating Pipeline records with Salary Info field"
      contains: "Salary Info"
    - path: "workflows/01d-scanner-jobspy.json"
      provides: "HTTP POST creating Pipeline records via NocoDB"
      contains: "\"fields\""
  key_links:
    - from: "Schedule/Manual Trigger"
      to: "Get Profile HTTP Request"
      via: "output connection"
    - from: "Get Profile HTTP Request"
      to: "Unwrap Profile Code node"
      via: "output connection"
    - from: "Unwrap Profile"
      to: "Get Search Queries HTTP Request"
      via: "output connection"
    - from: "Get Search Queries"
      to: "Unwrap Search Queries Code node"
      via: "output connection"
    - from: "Unwrap Search Queries"
      to: "Build Batch Payload"
      via: "output connection"
    - from: "Get Existing Job IDs HTTP Request"
      to: "Unwrap Existing Job IDs Code node"
      via: "output connection"
    - from: "Unwrap Existing Job IDs"
      to: "Deduplicate vs Pipeline"
      via: "output connection"
    - from: "Parse & Filter Jobs Code node"
      to: "Profile data via $('Get Profile').first().json"
      via: "reference"
      pattern: "Get Profile"
---

<objective>
Replace all Airtable nodes in the JobSpy scanner workflow (01d) with NocoDB HTTP Request nodes + unwrap transforms, add a new Get Profile node (currently missing), and fix the hardcoded geography TODO in the Parse & Filter Jobs Code node.

**Purpose:** JobSpy scanner (1d) is the most complex of the 4 scanners. Unlike the Greenhouse/Ashby/Lever scanners that use Tracked Companies, JobSpy uses Search Queries. It currently lacks a Get Profile node entirely — the geography filtering is hardcoded (D-09, D-11 fix). This plan adds the missing Profile reading and replaces all 3 Airtable nodes.

**Output:**
- `workflows/01d-scanner-jobspy.json` — ADD Get Profile + Unwrap node pair, replace 3 Airtable nodes with HTTP Request nodes + unwrap transforms, update Parse & Filter Jobs Code node for dynamic geography
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-scanner-workflows-migration/03-CONTEXT.md (D-09, D-11 — Profile + geography fix)
@.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md (Per-Scanner-Workflow Analysis — Workflow 1d)
@.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1J-1 through 1J-5)

@workflows/01d-scanner-jobspy.json
@docker-compose.example.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new Get Profile HTTP Request + Unwrap node; replace Get Search Queries Airtable node</name>

  <files>workflows/01d-scanner-jobspy.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1J-1, 1J-2 — full node JSON structures)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01d-scanner-jobspy.json (full file — nodes array and connections object)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  This task has 3 parts: (A) ADD a new Get Profile node pair, (B) Replace Get Search Queries Airtable, (C) Update connections.

  **Part A: ADD new "Get Profile" HTTP Request + "Unwrap Profile" Code node (NEW — not a replacement)**

  The JobSpy workflow currently starts with:
  ```
  Manual Trigger ──┐
                   ├──▶ Get Search Queries (Airtable)
  Schedule Trigger ┘
  ```

  Add TWO new nodes between the triggers and Get Search Queries:

  A1) **HTTP Request GET** node named "Get Profile":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [-1072, -240] (to the left of Get Search Queries at [-848, -240], aligning vertically)
  - `parameters`: method "GET", URL `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records`
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.options`: {} (empty object)
  - `retryOnFail`: true, `maxTries`: 3, `waitBetweenTries`: 5000, `alwaysOutputData`: true
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  A2) **Code node** named "Unwrap Profile":
  - `type`: "n8n-nodes-base.code"
  - `typeVersion`: 2
  - `position`: [-960, -240] (between Get Profile at [-1072,-240] and Get Search Queries at [-848,-240])
  - `parameters.jsCode`:
    ```
    // Unwrap NocoDB Data API v3 response — transforms { records: [...], next, prev } into flat items
    const response = $input.first().json;
    const records = response.records || [];

    if (records.length === 0) {
      return [{ json: { _empty: true, _message: 'No Profile found' } }];
    }

    // Flatten: each record becomes { json: { ...fields, id: record.id } }
    return records.map(record => ({
      json: { ...record.fields, id: record.id }
    }));
    ```

  **Part B: Replace "Get Search Queries" Airtable node with HTTP Request + Unwrap**

  Delete the existing Airtable node at position [-848, -240]. Replace with TWO nodes:

  B1) **HTTP Request GET** node named "Get Search Queries":
  - `type`: "n8n-nodes-base.httpRequest"
  - `typeVersion`: 4.2
  - `position`: [-848, -240] (original position of the Airtable node)
  - `parameters.method`: "GET"
  - `parameters.url`: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_QUERIES }}/records?where=(Enabled,is,true)~and("Source Type",eq,"JobSpy")`
  - Note: Use `\"` to escape double quotes in the JSON string value
  - `parameters.authentication`: "genericCredentialType"
  - `parameters.genericAuthType`: "httpHeaderAuth"
  - `parameters.sendHeaders`: true
  - `parameters.headerParameters.parameters`: [{"name": "Content-Type", "value": "application/json"}]
  - `parameters.options`: {} (empty object)
  - `retryOnFail`: true, `maxTries`: 3, `waitBetweenTries`: 5000, `alwaysOutputData`: true
  - `credentials`: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}

  B2) **Code node** named "Unwrap Search Queries":
  - `type`: "n8n-nodes-base.code"
  - `typeVersion`: 2
  - `position`: [-736, -240] (between Get Search Queries and Build Batch Payload at [-544,-240])
  - `parameters.jsCode`:
    ```
    // Unwrap NocoDB Data API v3 response — transforms { records: [...], next, prev } into flat items
    const response = $input.first().json;
    const records = response.records || [];

    if (records.length === 0) {
      return [{ json: { _empty: true, _message: 'No enabled Search Queries found (JobSpy)' } }];
    }

    return records.map(record => ({
      json: { ...record.fields, id: record.id }
    }));
    ```

  **Part C: Update connections**

  Original connections (from triggers to Search Queries):
  - Manual Trigger → Get Search Queries
  - Schedule Trigger → Get Search Queries
  - Get Search Queries → Build Batch Payload

  New connections:
  - Manual Trigger → Get Profile
  - Schedule Trigger → Get Profile
  - Get Profile → Unwrap Profile
  - Unwrap Profile → Get Search Queries
  - Get Search Queries → Unwrap Search Queries
  - Unwrap Search Queries → Build Batch Payload

  Update the `connections` object:
  - "Manual Trigger": output → "Get Profile" (was → "Get Search Queries")
  - "Schedule Trigger": output → "Get Profile" (was → "Get Search Queries")
  - "Get Profile": output → "Unwrap Profile" (new node)
  - "Unwrap Profile": output → "Get Search Queries" (new connection)
  - "Get Search Queries": output → "Unwrap Search Queries" (was → "Build Batch Payload")
  - "Unwrap Search Queries": output → "Build Batch Payload" (new connection)

  **CRITICAL — Correct response field:** Use `response.records` NOT `response.list` in all unwrap transforms.
  **CRITICAL — Correct API path:** `/api/v3/data/{baseId}/{tableId}/records` with `/data/` segment.
  **CRITICAL — Table env var:** Get Search Queries uses `NOCODB_TABLE_QUERIES` (NOT `NOCODB_TABLE_COMPANIES`).
  **CRITICAL — where clause:** `("Source Type",eq,"JobSpy")` NOT `("Scan Method",...)` — Search Queries uses Source Type field.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01d-scanner-jobspy.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Get Profile' in nodes, 'Get Profile node missing'
assert 'Unwrap Profile' in nodes, 'Unwrap Profile node missing'
assert 'Unwrap Search Queries' in nodes, 'Unwrap Search Queries missing'
assert nodes['Get Profile']['type']=='n8n-nodes-base.httpRequest', 'Get Profile wrong type'
assert nodes['Get Search Queries']['type']=='n8n-nodes-base.httpRequest', 'Get Search Queries still Airtable'
url = nodes['Get Search Queries']['parameters']['url']
assert 'NOCODB_TABLE_QUERIES' in url, f'Wrong table: {url}'
assert 'Source Type' in url or 'Source+Type' in url, f'Wrong where filter: {url}'
assert 'response.records' in nodes['Unwrap Profile']['parameters']['jsCode'], 'Unwrap uses wrong key'
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Profile" HTTP Request node exists at [-1072, -240] with URL to NOCODB_TABLE_PROFILE
  - "Unwrap Profile" Code node exists at [-960, -240] using `response.records`
  - "Get Search Queries" is HTTP Request (not Airtable) at [-848, -240] with URL to NOCODB_TABLE_QUERIES and where filter `("Source Type",eq,"JobSpy")`
  - "Unwrap Search Queries" Code node at [-736, -240]
  - Connections: Trigger→Get Profile→Unwrap Profile→Get Search Queries→Unwrap Search Queries→Build Batch Payload
  - Old Airtable Get Search Queries removed from nodes array
  </acceptance_criteria>

  <done>
  Get Profile node + unwrap added. Get Search Queries replaced with HTTP Request + unwrap. Connections updated. JobSpy now reads Profile from NocoDB.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Get Existing Job IDs and Create Pipeline Records Airtable nodes with HTTP Request + Unwrap</name>

  <files>workflows/01d-scanner-jobspy.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1J-3, 1J-4 — note Salary Info field addition)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01d-scanner-jobspy.json (Get Existing Job IDs and Create Pipeline Records entries)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  Replace the remaining 2 Airtable nodes in the JobSpy workflow. The Get Existing Job IDs node at [528, -240] and Create Pipeline Records at [1392, -240].

  **Step 1: Replace "Get Existing Job IDs" Airtable node** (position [528, -240])

  Delete existing Airtable node. Add TWO nodes:

  A) HTTP Request GET "Get Existing Job IDs" at [528, -240]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?fields=Job+ID`
  - Method: GET
  - Auth: genericCredentialType / httpHeaderAuth, credential name "NocoDB API"
  - sendHeaders: true, headerParameters: [{"name":"Content-Type","value":"application/json"}]
  - options: {} (empty object)
  - alwaysOutputData: true, executeOnce: true
  - NO retryOnFail (matches existing Airtable pattern)

  B) Code node "Unwrap Existing Job IDs" at [688, -240] (immediately right):
  - Same JSCode as Pattern 1C: `response.records`, flattens `record.fields['Job ID']`, `_empty` sentinel for zero records

  **Step 2: Replace "Create Pipeline Records" Airtable node** (position [1392, -240])

  Delete existing Airtable node. Add ONE node:

  HTTP Request POST "Create Pipeline Records" at [1392, -240]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records`
  - Method: POST
  - Auth: genericCredentialType / httpHeaderAuth, credential name "NocoDB API"
  - sendHeaders: true, headerParameters: [{"name":"Content-Type","value":"application/json"}]
  - sendBody: true, specifyBody: "json"
  - jsonBody: The JSON body INCLUDES Salary Info field (JobSpy-specific difference from 1a/1b/1c):
    ```
    ={
      "fields": {
        "Job ID": "{{ $json.jobId }}",
        "Job Title": "{{ $json.jobTitle }}",
        "Company": "{{ $json.company }}",
        "Location": "{{ $json.location }}",
        "Apply Link": "{{ $json.applyLink }}",
        "Job Description": "{{ $json.jobDescription }}",
        "Source": "{{ $json.source }}",
        "Source Query": "{{ $json.sourceQuery }}",
        "Source Tag": "{{ $json.sourceTag }}",
        "Discovery Date": "{{ $json.discoveryDate }}",
        "Status": "New",
        "Salary Info": "{{ $json.salaryInfo }}"
      }
    }
    ```
  - retryOnFail: true, maxTries: 3, waitBetweenTries: 5000, continueOnFail: true, alwaysOutputData: true
  - options: {} (empty object)

  **Step 3: Update connections**

  Original: Parse & Filter Jobs → Get Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records

  New: Parse & Filter Jobs → Get Existing Job IDs → Unwrap Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records

  Update connections:
  - "Parse & Filter Jobs" → "Get Existing Job IDs" (unchanged)
  - "Get Existing Job IDs" → "Unwrap Existing Job IDs" (was directly to Deduplicate)
  - "Unwrap Existing Job IDs" → "Deduplicate vs Pipeline" (new connection)
  - "Deduplicate vs Pipeline" → "If" (unchanged)
  - "If" → "Create Pipeline Records" (unchanged)

  **CRITICAL — Salary Info field:** This is the ONLY scanner that includes "Salary Info" in the POST body. The existing JobSpy Code node already produces `$json.salaryInfo` in the Parse & Filter Jobs output.
  **CRITICAL — POST body:** Must wrap in `{ "fields": { ... } }`. Flat key-value pairs return 400.
  **CRITICAL — Field projection:** `?fields=Job+ID` uses `+` for space, NOT `%20`.
  **CRITICAL — No unwrap after POST:** POST response goes nowhere useful — the "If" node feeds Create Pipeline Records.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01d-scanner-jobspy.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Unwrap Existing Job IDs' in nodes, 'Unwrap Existing Job IDs missing'
assert nodes['Get Existing Job IDs']['type']=='n8n-nodes-base.httpRequest', 'Existing Job IDs still Airtable'
assert nodes['Create Pipeline Records']['type']=='n8n-nodes-base.httpRequest', 'Create Records still Airtable'
url = nodes['Get Existing Job IDs']['parameters']['url']
assert 'fields=Job+ID' in url, f'Missing field projection: {url}'
body = nodes['Create Pipeline Records']['parameters']['jsonBody']
assert '\"Salary Info\"' in body, f'Missing Salary Info in POST: {body[:200]}'
assert '\"fields\"' in body, 'POST body missing fields wrapper'
assert 'response.records' in nodes['Unwrap Existing Job IDs']['parameters']['jsCode']
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Existing Job IDs" HTTP Request with field projection at [528, -240]
  - "Unwrap Existing Job IDs" Code node at [688, -240] uses response.records
  - "Create Pipeline Records" HTTP POST includes Salary Info field
  - Connections: Parse & Filter Jobs -> Get Existing Job IDs -> Unwrap Existing Job IDs -> Deduplicate vs Pipeline -> If -> Create Pipeline Records
  </acceptance_criteria>

  <done>
  Get Existing Job IDs and Create Pipeline Records replaced with HTTP Request nodes. POST body includes Salary Info field. Connections correct.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Parse & Filter Jobs Code node with dynamic geography from Profile</name>

  <files>workflows/01d-scanner-jobspy.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Pattern 1J-5)
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md (Workflow 1d Step 5)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01d-scanner-jobspy.json (Parse & Filter Jobs Code node's jsCode field)
  </read_first>

  <action>
  Edit the "Parse & Filter Jobs" Code node in `workflows/01d-scanner-jobspy.json`. Find the existing hardcoded geography lines and replace with dynamic Profile-based code.

  **LOCATE these lines in the jsCode:**
  ```
  // Geography filter — same pattern as Workflows 1a/1b/1c
  // TODO: Read from Profile.Target Geography in v1.1
  const userGeographies = ['Canada', 'Remote Global', 'Remote North America'];
  ```

  **REPLACE with:**
  ```
  // Dynamic geography from Profile table (same pattern as Workflows 1a/1b/1c)
  const profileData = $('Get Profile').first().json;
  const geoField = profileData['Target Geography'];
  const userGeographies = Array.isArray(geoField) ? geoField : ['Remote Global'];
  ```

  **DO NOT change anything else in the Code node.** The rest of the code (FNV-1a hashing, GEO_CONFIG map, matchGeography, title filtering, Salary Info extraction, -spy suffix) stays identical.

  **Key design decisions:**
  - Uses `profileData['Target Geography']` directly without `.fields` fallback. Unlike Greenhouse's Parse & Filter Jobs where data may arrive in nested or flat format, in JobSpy the Profile data ALWAYS goes through the Unwrap Profile Code node first, producing flat items.
  - Fallback is `['Remote Global']` if Target Geography is not an array. This prevents crashes on empty/misconfigured Profile.
  - The `$('Get Profile')` node reference works because Task 1 added the "Get Profile" HTTP Request + Unwrap node pair at the start of the workflow.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01d-scanner-jobspy.json') as f:
    wf = json.load(f)
for n in wf['nodes']:
    if n['name'] == 'Parse & Filter Jobs':
        code = n['parameters']['jsCode']
        assert code.find('Get Profile') > -1, 'Missing Get Profile reference'
        assert code.find('Target Geography') > -1, 'Missing Target Geography'
        assert code.find('Array.isArray') > -1, 'Missing array check'
        assert code.find('TODO') == -1, 'TODO still present'
        assert code.find('Remote Global') > -1, 'Missing fallback'
        print('OK - Geography dynamic from Profile')
        break
"
  </verify>

  <acceptance_criteria>
  - Parse & Filter Jobs no longer has `const userGeographies = ['Canada', 'Remote Global', 'Remote North America'];` hardcoded
  - Parse & Filter Jobs reads `profileData['Target Geography']` from `$('Get Profile').first().json`
  - Array fallback `['Remote Global']` present for empty/missing Target Geography
  - All other code unchanged (title keywords, Salary Info, FNV-1a hashing, GEO_CONFIG)
  </acceptance_criteria>

  <done>
  JobSpy's Parse & Filter Jobs now reads geography dynamically from the NocoDB Profile record. Per D-09 and D-11.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> NocoDB (Docker internal) | HTTP API calls with xc-token auth over internal Docker network |
| Code node -> Profile data | In-memory data flow: Get Profile node reads from NocoDB; Parse & Filter Jobs accesses via $('Get Profile').first().json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | workflows/01d-scanner-jobspy.json | mitigate | Credential referenced by name only. No token value in workflow JSON. |
| T-03-02 | Tampering | Code node JS injection | accept | Code nodes execute in n8n sandbox; user controls all Code node content. No user input in these values. |
| T-03-03 | Spoofing | Parse & Filter Jobs Get Profile ref | mitigate | Profile data comes from authenticated NocoDB API call (xc-token). No unauthenticated data path. |
| T-03-SC | Supply Chain | No packages installed | accept | All changes are to workflow JSON. No new dependencies. |
</threat_model>

<verification>
1. python3 -c "import json; wf=json.load(open('workflows/01d-scanner-jobspy.json')); print(sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable'))" returns 0
2. Parse & Filter Jobs Code node: verify `TODO` is gone, `$('Get Profile').first().json` is present
3. Visual: n8n UI workflow has Get Profile -> Unwrap Profile -> Get Search Queries -> Unwrap Search Queries at the start
4. Manual: Trigger JobSpy workflow, inspect Parse & Filter Jobs output to verify userGeographies matches Profile.Target Geography
</verification>

<success_criteria>
1. 3 Airtable nodes replaced with HTTP Request nodes + unwrap transforms (Get Search Queries, Get Existing Job IDs, Create Pipeline Records)
2. NEW Get Profile HTTP Request + Unwrap Code node pair added (previously missing)
3. Parse & Filter Jobs reads geography from Profile, not hardcoded
4. Create Pipeline Records POST includes Salary Info field
5. Get Search Queries reads from NOCODB_TABLE_QUERIES with (Enabled=true AND Source Type=JobSpy) filter
6. No Airtable nodes remain in the workflow
</success_criteria>

## Plan 05 Artifacts

| Artifact | Type | Description |
|----------|------|-------------|
| workflows/01d-scanner-jobspy.json (modified) | workflow | 3 Airtable replaced + Get Profile added + Code node edited |

## Phase 3 Artifacts (cumulative)

| Artifact | Plan | Description |
|----------|------|-------------|
| docker-compose.example.yml (modified) | 03-01 | NOCODB_* env vars + credential docs |
| workflows/01a-scanner-greenhouse.json (modified) | 03-02 | NocoDB HTTP nodes |
| workflows/01b-scanner-ashby.json (modified) | 03-03 | NocoDB HTTP nodes |
| workflows/01c-scanner-lever.json (modified) | 03-04 | NocoDB HTTP nodes |
| workflows/01d-scanner-jobspy.json (modified) | 03-05 | NocoDB HTTP nodes + Get Profile + code edit |

<output>
Create `.planning/phases/03-scanner-workflows-migration/03-05-SUMMARY.md` when done
</output>