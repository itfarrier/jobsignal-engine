---
phase: 03-scanner-workflows-migration
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [workflows/01b-scanner-ashby.json]
autonomous: true
requirements: [SCAN-02]

must_haves:
  truths:
    - "Ashby scanner (1b) reads Profile from NocoDB — unwrap produces flat item with Target Geography"
    - "Ashby scanner (1b) reads Tracked Companies from NocoDB with where filter (Enabled=true AND Scan Method=Ashby API)"
    - "Ashby scanner (1b) reads existing Pipeline Job IDs from NocoDB via field projection"
    - "Ashby scanner (1b) creates new Pipeline records in NocoDB via POST with { fields: { ... } } body"
    - "Existing Code nodes receive flat format data and work unchanged"
  artifacts:
    - path: "workflows/01b-scanner-ashby.json"
      provides: "NocoDB-based Ashby scanner with HTTP Request nodes replacing Airtable"
      contains: "\"Scan Method\",eq,\"Ashby API\""
      min_lines: 380
    - path: "workflows/01b-scanner-ashby.json"
      provides: "Unwrap transform nodes after each NocoDB GET request"
      contains: "Unwrap Profile"
    - path: "workflows/01b-scanner-ashby.json"
      provides: "HTTP POST creating Pipeline records with { fields: { ... } } body"
      contains: "\"fields\""
    - path: "workflows/01b-scanner-ashby.json"
      provides: "Where clause filtering by Ashby API scan method"
      contains: "Ashby API"
  key_links:
    - from: "Get Profile HTTP Request"
      to: "Unwrap Profile Code node"
      via: "output connection"
    - from: "Unwrap Profile"
      to: "Get Tracked Companies HTTP Request"
      via: "output connection"
    - from: "Get Tracked Companies HTTP Request"
      to: "Unwrap Tracked Companies Code node"
      via: "output connection"
    - from: "Unwrap Tracked Companies"
      to: "Loop Companies"
      via: "output connection"
    - from: "Get Existing Job IDs HTTP Request"
      to: "Unwrap Existing Job IDs Code node"
      via: "output connection"
    - from: "Unwrap Existing Job IDs"
      to: "Deduplicate vs Pipeline"
      via: "output connection"
---

<objective>
Replace all 4 Airtable nodes in the Ashby scanner workflow (01b) with n8n HTTP Request nodes targeting NocoDB Data API v3, following the same pattern as the Greenhouse scanner (Plan 02) but with Ashby API filter.

**Purpose:** Ashby scanner (1b) is structurally identical to Greenhouse (1a) except for the `where` clause filter targeting `"Ashby API"`. The 4 node replacements and 3 unwrap transforms follow the proven pattern from 03-02-PLAN.md.

**Output:**
- `workflows/01b-scanner-ashby.json` — 4 Airtable nodes replaced with 4 HTTP Request nodes + 3 Unwrap Code nodes
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-scanner-workflows-migration/03-CONTEXT.md
@.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md
@.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1A, 1B, 1C, 1D — full node JSON structures)

@workflows/01b-scanner-ashby.json
@docker-compose.example.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Get Profile and Get Tracked Companies Airtable nodes in Ashby scanner</name>

  <files>workflows/01b-scanner-ashby.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1A, 1B — Ashby variant with where clause on line 582)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01b-scanner-ashby.json (full file — nodes array and connections object)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  In `workflows/01b-scanner-ashby.json`, replace "Get Profile" and "Get Tracked Companies" Airtable nodes with HTTP Request nodes + unwrap Code nodes. Follow the EXACT same pattern as Greenhouse scanner (Plan 02 Task 1) but with these changes:

  **Key differences from Greenhouse:**
  1. The `where` clause filter for Tracked Companies uses `"Ashby API"` instead of `"Greenhouse API"`:
     `where=(Enabled,is,true)~and("Scan Method",eq,"Ashby API")`
  2. Node positions differ from Greenhouse. Use the CURRENT positions from the 01b JSON file:
     - Get Profile: position [448, -384] (not [352, -400] like Greenhouse)
     - Unwrap Profile: position [568, -384] (immediately right of Get Profile)
     - Get Tracked Companies: position [656, -384] (not [576, -400] like Greenhouse) — KEEP original position
     - Unwrap Tracked Companies: position [768, -384] (immediately right of Get Tracked Companies)
     - Loop Companies remains at [880, -384]

  **Step 1: Replace "Get Profile" Airtable node**

  Delete existing Airtable "Get Profile". Add HTTP Request GET + Code node:

  A) HTTP Request GET named "Get Profile":
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records`
  - Position: [448, -384] (from original)
  - Authentication: genericCredentialType / httpHeaderAuth
  - Credentials: {"httpHeaderAuth": {"id": "TO_BE_CREATED", "name": "NocoDB API"}}
  - retryOnFail:true, maxTries:3, waitBetweenTries:5000, alwaysOutputData:true
  - sendHeaders:true, headerParameters: [{"name":"Content-Type","value":"application/json"}]
  - options: {} (empty object)

  B) Code node named "Unwrap Profile":
  - Position: [568, -384]
  - Same JSCode as Pattern 1A in PATTERNS.md — uses `response.records`, produces flat items with `_empty` sentinel for zero records

  **Step 2: Replace "Get Tracked Companies" Airtable node** (position [656, -384])

  Delete existing Airtable node. Add HTTP Request GET + Code node:

  A) HTTP Request GET named "Get Tracked Companies":
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_COMPANIES }}/records?where=(Enabled,is,true)~and("Scan Method",eq,"Ashby API")`
  - Note: `\"` around the "Scan Method" field name in the JSON string
  - Position: [656, -384] (original position)
  - Same auth/retry/headers config as Get Profile
  - retryOnFail:true, maxTries:3, waitBetweenTries:5000, alwaysOutputData:true

  B) Code node named "Unwrap Tracked Companies":
  - Position: [768, -384]
  - Same JSCode as Pattern 1B — `response.records`, flat items, `_empty` sentinel for "No enabled Tracked Companies found (Ashby)"

  **Step 3: Update connections**

  Original: Trigger → Get Profile → Get Tracked Companies → Loop Companies

  New: Trigger → Get Profile → Unwrap Profile → Get Tracked Companies → Unwrap Tracked Companies → Loop Companies

  Update connections object:
  - "Manual Trigger": output → "Get Profile"
  - "Schedule Trigger": output → "Get Profile"
  - "Get Profile": output → "Unwrap Profile"
  - "Unwrap Profile": output → "Get Tracked Companies"
  - "Get Tracked Companies": output → "Unwrap Tracked Companies"
  - "Unwrap Tracked Companies": output → "Loop Companies"

  **CRITICAL:** Same corrections as Plan 02:
  - Use `response.records` NOT `response.list`
  - Use `/api/v3/data/{baseId}/{tableId}/records` path format
  - No continueOnFail on GET nodes
  - The existing "Parse & Filter Jobs" Code node already reads Profile via `$('Get Profile').first().json` — that reference still works because the node name "Get Profile" stays the same.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01b-scanner-ashby.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Unwrap Profile' in nodes, 'Unwrap Profile missing'
assert 'Unwrap Tracked Companies' in nodes, 'Unwrap Tracked Companies missing'
assert nodes['Get Profile']['type']=='n8n-nodes-base.httpRequest', 'Get Profile still Airtable'
assert nodes['Get Tracked Companies']['type']=='n8n-nodes-base.httpRequest', 'Get Tracked Companies still Airtable'
url = nodes['Get Tracked Companies']['parameters']['url']
assert 'Ashby API' in url, f'Wrong filter: {url}'
assert 'response.records' in nodes['Unwrap Profile']['parameters']['jsCode'], 'Unwrap uses wrong key'
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Profile" HTTP Request at [448, -384] with URL to NOCODB_TABLE_PROFILE
  - "Unwrap Profile" Code node at [568, -384] uses response.records
  - "Get Tracked Companies" HTTP Request with where filter (Enabled,is,true)~and("Scan Method",eq,"Ashby API")
  - "Unwrap Tracked Companies" Code node at [768, -384]
  - Connections: Trigger -> Get Profile -> Unwrap Profile -> Get Tracked Companies -> Unwrap Tracked Companies -> Loop Companies
  </acceptance_criteria>

  <done>
  "Get Profile" (HTTP GET + Unwrap) and "Get Tracked Companies" (HTTP GET + Unwrap) replace 2 Airtable nodes in Ashby scanner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Get Existing Job IDs and Create Pipeline Records Airtable nodes in Ashby scanner</name>

  <files>workflows/01b-scanner-ashby.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1C, 1D)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01b-scanner-ashby.json (existing "Get Existing Job IDs" and "Create Pipeline Records" entries)
  </read_first>

  <action>
  Replace the remaining 2 Airtable nodes in the Ashby workflow (01b).

  **Step 1: Replace "Get Existing Job IDs" Airtable node** (position [2192, -368])

  Delete existing Airtable node. Add TWO nodes:

  A) HTTP Request GET "Get Existing Job IDs" at [2192, -368]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?fields=Job+ID`
  - genericCredentialType / httpHeaderAuth auth, credential "NocoDB API"
  - alwaysOutputData: true, executeOnce: true
  - NO retryOnFail
  - sendHeaders: true, headerParameters: [{"name":"Content-Type","value":"application/json"}]

  B) Code node "Unwrap Existing Job IDs" at [2304, -368] (immediately right):
  - Same JSCode as Pattern 1C: response.records, flatten fields['Job ID'], _empty sentinel

  **Step 2: Replace "Create Pipeline Records" Airtable node** (position [2832, -368])

  Delete existing Airtable node. Add ONE node:

  HTTP Request POST "Create Pipeline Records" at [2832, -368]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records`
  - Method: POST, sendBody: true, specifyBody: "json"
  - jsonBody: ={ "fields": { "Job ID": "{{ $json.jobId }}", ... 11 fields ... "Status": "New" } }
  - retryOnFail: true, maxTries: 3, waitBetweenTries: 5000, continueOnFail: true, alwaysOutputData: true
  - genericCredentialType / httpHeaderAuth auth, credential "NocoDB API"

  **Step 3: Update connections**

  Original: Aggregate All Jobs -> Get Existing Job IDs -> Deduplicate vs Pipeline -> If -> Create Pipeline Records
  New: Aggregate All Jobs -> Get Existing Job IDs -> Unwrap Existing Job IDs -> Deduplicate vs Pipeline -> If -> Create Pipeline Records

  Same column mapping as Greenhouse: Job ID, Job Title, Company, Location, Apply Link, Job Description, Source, Source Query, Source Tag, Discovery Date, Status (hardcoded "New").
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01b-scanner-ashby.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Unwrap Existing Job IDs' in nodes
assert nodes['Get Existing Job IDs']['type']=='n8n-nodes-base.httpRequest'
assert nodes['Create Pipeline Records']['type']=='n8n-nodes-base.httpRequest'
body = nodes['Create Pipeline Records']['parameters']['jsonBody']
assert '\"fields\"' in body, 'POST body missing fields wrapper'
airtable_count = sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable')
assert airtable_count == 0, f'Still have {airtable_count} Airtable nodes'
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Existing Job IDs" HTTP Request with field projection at [2192, -368]
  - "Unwrap Existing Job IDs" Code node at [2304, -368]
  - "Create Pipeline Records" HTTP POST with fields wrapper at [2832, -368]
  - No Airtable nodes remain in the workflow
  </acceptance_criteria>

  <done>
  All 4 Airtable nodes replaced with HTTP Request + unwrap transforms. Ashby scanner reads/writes NocoDB.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> NocoDB (Docker internal) | HTTP API calls with xc-token auth over internal Docker network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | workflows/01b-scanner-ashby.json | mitigate | Credential referenced by name only. Token in n8n credential store. |
| T-03-02 | Tampering | POST body injection | accept | All field values come from Code node outputs (pre-transformed). No user input. |
| T-03-03 | Spoofing | NocoDB API auth | mitigate | xc-token authentication validates callers. NocoDB not externally exposed. |
| T-03-SC | Supply Chain | No packages installed | accept | All changes to workflow JSON. No new dependencies. |
</threat_model>

<verification>
1. python3 -c "import json; wf=json.load(open('workflows/01b-scanner-ashby.json')); print(sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable'))" returns 0
2. Visual: n8n UI workflow has 3 Unwrap Code nodes, 4 HTTP Request nodes, no Airtable nodes
3. Manual: Execute workflow, verify Tracked Companies filtered by Ashby API, POST creates NocoDB records
</verification>

<success_criteria>
1. All 4 Airtable nodes replaced with HTTP Request nodes + 3 Unwrap Code nodes
2. Get Tracked Companies uses where filter with "Ashby API" scan method
3. Get Existing Job IDs uses ?fields=Job+ID field projection
4. Create Pipeline Records uses POST with { "fields": { ... } } body
5. Workflow connections preserved end-to-end
</success_criteria>

## Plan 03 Artifacts

| Artifact | Type | Description |
|----------|------|-------------|
| workflows/01b-scanner-ashby.json (modified) | workflow | 4 Airtable replaced with 4 HTTP Request + 3 Unwrap Code nodes |

## Phase 3 Artifacts (cumulative)

| Artifact | Plan | Description |
|----------|------|-------------|
| docker-compose.example.yml (modified) | 03-01 | NOCODB_* env vars + credential docs |
| workflows/01a-scanner-greenhouse.json (modified) | 03-02 | NocoDB HTTP nodes |
| workflows/01b-scanner-ashby.json (modified) | 03-03 | NocoDB HTTP nodes |
| workflows/01c-scanner-lever.json (modified) | 03-04 | NocoDB HTTP nodes |
| workflows/01d-scanner-jobspy.json (modified) | 03-05 | NocoDB HTTP nodes + Get Profile + code edit |

<output>
Create `.planning/phases/03-scanner-workflows-migration/03-03-SUMMARY.md` when done
</output>