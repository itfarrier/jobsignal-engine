---
phase: 03-scanner-workflows-migration
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified: [workflows/01c-scanner-lever.json]
autonomous: true
requirements: [SCAN-03]

must_haves:
  truths:
    - "D-01: Uses n8n HTTP Request nodes (NOT native NocoDB v2 node)"
    - "D-06: Unwrap transform nodes key on response.records (NOT response.list)"
    - "D-07: Column names use Title Case matching Airtable fields"
    - "D-08: Has own 'Get Profile' HTTP Request node"
    - "D-10: Uses field projection (?fields=Job+ID) for dedup queries"
    - "Lever scanner (1c) reads Profile from NocoDB — unwrap produces flat item with Target Geography"
    - "Lever scanner (1c) reads Tracked Companies from NocoDB with where filter (Enabled=true AND Scan Method=Lever API)"
    - "Lever scanner (1c) reads existing Pipeline Job IDs from NocoDB via field projection"
    - "Lever scanner (1c) creates new Pipeline records in NocoDB via POST with { fields: { ... } } body"
    - "Existing Code nodes receive flat format data and work unchanged"
  artifacts:
    - path: "workflows/01c-scanner-lever.json"
      provides: "NocoDB-based Lever scanner with HTTP Request nodes replacing Airtable"
      contains: "\"Scan Method\",eq,\"Lever API\""
      min_lines: 380
    - path: "workflows/01c-scanner-lever.json"
      provides: "Unwrap transform nodes after each NocoDB GET request"
      contains: "Unwrap Profile"
    - path: "workflows/01c-scanner-lever.json"
      provides: "HTTP POST creating Pipeline records with { fields: { ... } } body"
      contains: "\"fields\""
    - path: "workflows/01c-scanner-lever.json"
      provides: "Where clause filtering by Lever API scan method"
      contains: "Lever API"
  key_links:
    - from: "Get Profile HTTP Request"
      to: "Unwrap Profile Code node"
      via: "output connection"
    - from: "Unwrap Profile"
      to: "Get Tracked Companies HTTP Request"
      via: "output connection"
    - from: "Get Tracked Companies HTTP Request"
      to: "Unwrap Tracked Companies Code node"
      via: "output connection"
    - from: "Unwrap Tracked Companies"
      to: "Loop Companies"
      via: "output connection"
    - from: "Get Existing Job IDs HTTP Request"
      to: "Unwrap Existing Job IDs Code node"
      via: "output connection"
    - from: "Unwrap Existing Job IDs"
      to: "Deduplicate vs Pipeline"
      via: "output connection"
---

<objective>
Replace all 4 Airtable nodes in the Lever scanner workflow (01c) with n8n HTTP Request nodes targeting NocoDB Data API v3, following the same pattern as the Greenhouse scanner with Lever API filter.

**Purpose:** Lever scanner (1c) is structurally identical to Greenhouse and Ashby except for the `where` clause filter targeting `"Lever API"`. This is the third and final "standard pattern" scanner.

**Output:**
- `workflows/01c-scanner-lever.json` — 4 Airtable nodes replaced with 4 HTTP Request nodes + 3 Unwrap Code nodes
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-scanner-workflows-migration/03-CONTEXT.md
@.planning/phases/03-scanner-workflows-migration/03-RESEARCH.md
@.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1A, 1B, 1C, 1D)

@workflows/01c-scanner-lever.json
@docker-compose.example.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Get Profile and Get Tracked Companies Airtable nodes in Lever scanner</name>

  <files>workflows/01c-scanner-lever.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1A, 1B — Lever variant where clause on line 583)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01c-scanner-lever.json (full file — nodes array and connections object)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  In `workflows/01c-scanner-lever.json`, replace "Get Profile" and "Get Tracked Companies" Airtable nodes with HTTP Request nodes + unwrap Code nodes.

  **Key difference from Greenhouse:** The `where` clause filter uses `"Lever API"` instead of `"Greenhouse API"`:
  `where=(Enabled,is,true)~and("Scan Method",eq,"Lever API")`

  Node positions from the 01c JSON:
  - Manual Trigger: [224, -384]
  - Schedule Trigger: [208, -176]
  - Get Profile: position [416, -384]
  - Unwrap Profile: position [528, -384] (immediately right)
  - Get Tracked Companies: position [640, -384] — KEEP original
  - Unwrap Tracked Companies: position [752, -384] (immediately right)
  - Loop Companies: [832, -384]

  **Step 1: Replace "Get Profile" Airtable node**

  Delete existing Airtable node at position [416, -384]. Add:
  - HTTP Request GET "Get Profile" at [416, -384] — same URL pattern as Plan 02/03 targeting NOCODB_TABLE_PROFILE
  - Code node "Unwrap Profile" at [528, -384] — same JSCode as Pattern 1A: `response.records`, `_empty` sentinel, flat items

  **Step 2: Replace "Get Tracked Companies" Airtable node**

  Delete existing Airtable node at position [640, -384]. Add:
  - HTTP Request GET "Get Tracked Companies" at [640, -384] — URL with where filter:
    `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_COMPANIES }}/records?where=(Enabled,is,true)~and("Scan Method",eq,"Lever API")`
  - Code node "Unwrap Tracked Companies" at [752, -384] — same JSCode as Pattern 1B

  **Step 3: Update connections**

  Original: Trigger → Get Profile → Get Tracked Companies → Loop Companies
  New: Trigger → Get Profile → Unwrap Profile → Get Tracked Companies → Unwrap Tracked Companies → Loop Companies

  Connection updates:
  - Manual Trigger: output → Get Profile
  - Schedule Trigger: output → Get Profile
  - Get Profile: output → Unwrap Profile
  - Unwrap Profile: output → Get Tracked Companies
  - Get Tracked Companies: output → Unwrap Tracked Companies
  - Unwrap Tracked Companies: output → Loop Companies

  **Node config details (must match exactly):**

  Get Profile HTTP Request:
  ```json
  {
    "parameters": {
      "method": "GET",
      "url": "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records",
      "authentication": "genericCredentialType",
      "genericAuthType": "httpHeaderAuth",
      "sendHeaders": true,
      "headerParameters": { "parameters": [{"name":"Content-Type","value":"application/json"}] },
      "options": {}
    },
    "name": "Get Profile",
    "type": "n8n-nodes-base.httpRequest",
    "typeVersion": 4.2,
    "position": [416, -384],
    "retryOnFail": true,
    "maxTries": 3,
    "waitBetweenTries": 5000,
    "alwaysOutputData": true,
    "credentials": { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
  }
  ```

  Get Tracked Companies — same structure but position [640, -384] and URL with Lever filter. Use `\"` around "Scan Method" in the JSON where string.
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01c-scanner-lever.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Unwrap Profile' in nodes
assert 'Unwrap Tracked Companies' in nodes
assert nodes['Get Profile']['type']=='n8n-nodes-base.httpRequest'
assert nodes['Get Tracked Companies']['type']=='n8n-nodes-base.httpRequest'
url = nodes['Get Tracked Companies']['parameters']['url']
assert 'Lever API' in url, f'Wrong filter: {url}'
assert 'response.records' in nodes['Unwrap Profile']['parameters']['jsCode']
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Profile" node is HTTP Request with URL to NOCODB_TABLE_PROFILE, position [416, -384]
  - "Unwrap Profile" Code node at [528, -384] uses `response.records`
  - "Get Tracked Companies" HTTP Request with where filter (Enabled,is,true)~and("Scan Method",eq,"Lever API")
  - "Unwrap Tracked Companies" Code node at [752, -384]
  - Connections: Trigger→Get Profile→Unwrap Profile→Get Tracked Companies→Unwrap Tracked Companies→Loop Companies
  - Old Airtable nodes removed from nodes array
  </acceptance_criteria>

  <done>
  "Get Profile" (HTTP GET + Unwrap) and "Get Tracked Companies" (HTTP GET + Unwrap) replace 2 Airtable nodes. Connections correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Get Existing Job IDs and Create Pipeline Records Airtable nodes in Lever scanner</name>

  <files>workflows/01c-scanner-lever.json</files>

  <read_first>
  @/Users/itfarrier/pet/jobsignal-engine/.planning/phases/03-scanner-workflows-migration/03-PATTERNS.md (Patterns 1C, 1D)
  @/Users/itfarrier/pet/jobsignal-engine/workflows/01c-scanner-lever.json (existing "Get Existing Job IDs" and "Create Pipeline Records" entries)
  @/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
  </read_first>

  <action>
  Replace the remaining 2 Airtable nodes in the same Lever workflow file.

  **Step 1: Replace "Get Existing Job IDs" Airtable node** (position [2192, -368])

  Delete existing Airtable node. Add TWO nodes:

  A) HTTP Request GET "Get Existing Job IDs" at [2192, -368]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?fields=Job+ID`
  - auth: genericCredentialType / httpHeaderAuth, credential: "NocoDB API"
  - alwaysOutputData: true, executeOnce: true
  - NO retryOnFail (matches existing pattern)
  - sendHeaders: true, headerParameters: [{"name":"Content-Type","value":"application/json"}]

  B) Code node "Unwrap Existing Job IDs" at [2296, -368] (immediately right):
  - Same JSCode as Pattern 1C: `response.records`, flattens `record.fields['Job ID']`, `_empty` sentinel

  **Step 2: Replace "Create Pipeline Records" Airtable node** (position [2848, -384])

  Delete existing Airtable node. Add ONE node:

  HTTP Request POST "Create Pipeline Records" at [2848, -384]:
  - URL: `={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records`
  - Method: POST
  - sendBody: true, specifyBody: "json"
  - jsonBody: `={\n  \"fields\": {\n    \"Job ID\": \"{{ $json.jobId }}\",\n    \"Job Title\": \"{{ $json.jobTitle }}\",\n    \"Company\": \"{{ $json.company }}\",\n    \"Location\": \"{{ $json.location }}\",\n    \"Apply Link\": \"{{ $json.applyLink }}\",\n    \"Job Description\": \"{{ $json.jobDescription }}\",\n    \"Source\": \"{{ $json.source }}\",\n    \"Source Query\": \"{{ $json.sourceQuery }}\",\n    \"Source Tag\": \"{{ $json.sourceTag }}\",\n    \"Discovery Date\": \"{{ $json.discoveryDate }}\",\n    \"Status\": \"New\"\n  }\n}`
  - retryOnFail: true, maxTries: 3, waitBetweenTries: 5000, continueOnFail: true, alwaysOutputData: true
  - auth: genericCredentialType / httpHeaderAuth, credential: "NocoDB API"

  **Step 3: Update connections**

  Original: Aggregate All Jobs → Get Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records
  New: Aggregate All Jobs → Get Existing Job IDs → Unwrap Existing Job IDs → Deduplicate vs Pipeline → If → Create Pipeline Records
  (If → Create Pipeline Records unchanged)

  Update connections:
  - "Aggregate All Jobs" → "Get Existing Job IDs" (unchanged)
  - "Get Existing Job IDs" → "Unwrap Existing Job IDs" (was directly to Deduplicate)
  - "Unwrap Existing Job IDs" → "Deduplicate vs Pipeline" (new)
  - "Deduplicate vs Pipeline" → "If" (unchanged)
  - "If" → "Create Pipeline Records" (unchanged)

  **CRITICAL:** Same corrections as all plans:
  - POST body must be `{ "fields": { ... } }` NOT flat
  - `fields=Job+ID` NOT `?fields=Job%20ID` (n8x may double-encode %20)
  - No unwrap after POST
  - continueOnFail ONLY on POST, not on GET
  </action>

  <verify>
  <automated>python3 -c "
import json
with open('workflows/01c-scanner-lever.json') as f:
    wf = json.load(f)
nodes = {n['name']:n for n in wf['nodes']}
assert 'Unwrap Existing Job IDs' in nodes, 'Unwrap node missing'
assert nodes['Get Existing Job IDs']['type']=='n8n-nodes-base.httpRequest', 'Existing Job IDs still Airtable'
assert nodes['Create Pipeline Records']['type']=='n8n-nodes-base.httpRequest', 'Create Records still Airtable'
url = nodes['Get Existing Job IDs']['parameters']['url']
assert 'fields=Job+ID' in url, f'Missing field projection: {url}'
body = nodes['Create Pipeline Records']['parameters']['jsonBody']
assert '\"fields\"' in body, 'POST body missing fields wrapper'
assert 'response.records' in nodes['Unwrap Existing Job IDs']['parameters']['jsCode']
airtable_count = sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable')
print(f'Remaining Airtable nodes: {airtable_count} (expected 0 for 01c)')
assert airtable_count == 0, f'Still have {airtable_count} Airtable nodes'
print('All checks passed')
"
  </verify>

  <acceptance_criteria>
  - "Get Existing Job IDs" HTTP Request with `?fields=Job+ID` field projection, position [2192, -368]
  - "Unwrap Existing Job IDs" Code node at [2296, -368] uses `response.records`
  - "Create Pipeline Records" HTTP POST has `{ "fields": { ... } }` body with all 11 columns
  - Connections: Aggregate All Jobs→Get Existing Job IDs→Unwrap Existing Job IDs→Deduplicate vs Pipeline→If→Create Pipeline Records
  - No Airtable nodes remain in the workflow (all 4 replaced)
  </acceptance_criteria>

  <done>
  All 4 Airtable nodes replaced with HTTP Request + unwrap transforms. Lever scanner reads/writes NocoDB.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n → NocoDB (Docker internal) | HTTP API calls with xc-token auth over internal Docker network. Port 8080 not mapped externally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | workflows/01c-scanner-lever.json (credential name) | mitigate | Credential referenced by name `"NocoDB API"` only — no token value in workflow JSON. Token stored in n8n credential store encrypted at rest. |
| T-03-02 | Tampering | HTTP Request POST to NocoDB | mitigate | continueOnFail:true on write operations ensures one failed insert doesn't block subsequent records. Same pattern as existing Airtable behavior. |
| T-03-03 | Spoofing | NocoDB API endpoint | mitigate | xc-token authentication validates that only authorized callers (n8n) can write to Pipeline table. NocoDB not exposed to external network. |
| T-03-SC | Supply Chain | No packages installed | accept | All changes are to workflow JSON and existing docker-compose only. No new dependencies. |
</threat_model>

<verification>
1. `python3 -c "import json; wf=json.load(open('workflows/01c-scanner-lever.json')); print(sum(1 for n in wf['nodes'] if n['type']=='n8n-nodes-base.airtable'))"` returns 0
2. Visual inspection: Unwrap Profile, Unwrap Tracked Companies, Unwrap Existing Job IDs Code nodes present
3. Manual: Open in n8n UI, verify no red nodes (broken connections), execute Manually to test
</verification>

<success_criteria>
1. All 4 Airtable nodes in 01c-scanner-lever.json replaced with HTTP Request nodes + 3 Unwrap Code nodes
2. Get Tracked Companies uses `where` filter with `"Lever API"` scan method
3. Get Existing Job IDs uses `?fields=Job+ID` field projection
4. Create Pipeline Records uses POST with `{ "fields": { ... } }` body
5. Workflow connections preserved end-to-end
</success_criteria>

## Phase 3 — Artifacts This Plan Produces

| Artifact | Type | Description |
|----------|------|-------------|
| `workflows/01c-scanner-lever.json` (modified) | workflow | All 4 Airtable nodes replaced with 4 HTTP Request + 3 Unwrap Code nodes |

## Artifacts This Phase Produces (cumulative)

| Artifact | Plan | Description |
|----------|------|-------------|
| docker-compose.example.yml (modified) | 03-01 | NOCODB_* env vars + credential docs |
| workflows/01a-scanner-greenhouse.json (modified) | 03-02 | NocoDB HTTP nodes replacing Airtable |
| workflows/01b-scanner-ashby.json (modified) | 03-03 | NocoDB HTTP nodes replacing Airtable |
| workflows/01c-scanner-lever.json (modified) | 03-04 | NocoDB HTTP nodes replacing Airtable |
| workflows/01d-scanner-jobspy.json (modified) | 03-05 | NocoDB HTTP nodes + Get Profile + code edit |

<output>
Create `.planning/phases/03-scanner-workflows-migration/03-04-SUMMARY.md` when done
</output>