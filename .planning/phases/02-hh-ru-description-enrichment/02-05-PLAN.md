---
phase: 02-hh-ru-description-enrichment
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SETUP.md
  - airtable/AIRTABLE-SCHEMA.md
autonomous: true
gap_closure: true
requirements:
  - HH-10
user_setup:
  - service: airtable
    why: "Pipeline.Source single-select must include hh.ru before Create node writes correct Source"
    dashboard_config:
      - task: "Add hh.ru to Pipeline → Source single-select options"
        location: "Airtable base → Pipeline table → Source field → Edit field → Add option hh.ru"
      - task: "Add HH RSS to Search Queries → Source Type (if using manual HH queries)"
        location: "Airtable base → Search Queries table → Source Type field"
      - task: "Refresh Create Pipeline Records field list in n8n after Airtable changes"
        location: "n8n → 01e-scanner-hhru → Create Pipeline Records node → re-select base/table or click refresh schema"
must_haves:
  truths:
    - "SETUP.md lists explicit 01e pre-flight checks before first hh.ru scanner run"
    - "Operator can diagnose Source=LinkedIn and empty Pipeline rows from documented symptoms"
    - "AIRTABLE-SCHEMA.md explains n8n typecast cannot create hh.ru Source option automatically"
  artifacts:
    - path: docs/SETUP.md
      provides: "01e pre-flight checklist with Airtable + n8n schema refresh steps"
      contains: "hh.ru"
    - path: airtable/AIRTABLE-SCHEMA.md
      provides: "Troubleshooting for partial Pipeline writes and wrong Source value"
      contains: "hh.ru"
  key_links:
    - from: docs/SETUP.md
      to: airtable/AIRTABLE-SCHEMA.md
      via: "pre-flight links to Manual Airtable Setup section"
      pattern: "Manual Airtable Setup"
    - from: workflows/01e-scanner-hhru.json
      to: Airtable Pipeline.Source
      via: "Create Pipeline Records maps Source={{$json.source}} — requires hh.ru option"
      pattern: "Source.*hh.ru"
---

<objective>
Close UAT test 3 Airtable half: document mandatory pre-flight setup so Pipeline rows write Source=hh.ru with full job fields instead of partial records with Source=LinkedIn.

Purpose: Dual root cause — user's base likely missing hh.ru Source option and n8n cached stale field schema; n8n typecast does not auto-create single-select options.
Output: SETUP.md pre-flight checklist and AIRTABLE-SCHEMA.md troubleshooting for 01e Create failures.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/02-hh-ru-description-enrichment/02-UAT.md
@.planning/debug/phase02-uat-test3-enrich.md
</execution_context>

<context>
@.planning/phases/02-hh-ru-description-enrichment/02-02-SUMMARY.md
@airtable/AIRTABLE-SCHEMA.md
@docs/SETUP.md
@workflows/01e-scanner-hhru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 01e pre-flight checklist to SETUP.md</name>
  <files>docs/SETUP.md</files>
  <action>
    Close UAT gap test 3 Airtable root cause per 02-UAT.md missing items.

    Add a new subsection **"01e hh.ru Pre-flight (required before first run)"** in docs/SETUP.md — place it after Step 1.6 (Base ID / Table IDs) and before Self-Hosted Setup, OR integrate into "Verifying Your Setup" as a dedicated numbered block before the hh.ru RSS smoke bullet. Choose whichever location keeps the checklist visible before operators import 01e.

    Checklist must include these numbered steps (operator-facing, imperative):

    1. **Pipeline → Source:** manually add single-select option `hh.ru` in Airtable UI (n8n cannot create new select options via typecast). Link to airtable/AIRTABLE-SCHEMA.md Manual Airtable Setup section.

    2. **Search Queries → Source Type:** add `HH RSS` if using manual HH RSS query rows.

    3. **Profile → Target Geography:** add Russia / Moscow / Saint Petersburg / Remote Russia if using auto-feeds.

    4. **n8n schema refresh:** after any Airtable field-option change, open 01e → Create Pipeline Records → refresh/re-select base and Pipeline table so field mappings match live schema.

    5. **Re-import workflow:** pull latest workflows/01e-scanner-hhru.json and import in n8n before manual test execute.

    6. **Success criteria:** new Pipeline rows show Source=`hh.ru`, Job ID ends with `-hhr`, Job Description substantially longer than RSS summary (~500+ chars when enrichment succeeds).

    Update existing "hh.ru RSS smoke" bullet in Verifying Your Setup to reference this pre-flight section ("complete pre-flight first").

    Do not duplicate entire AIRTABLE-SCHEMA.md — link to it.
  </action>
  <verify>
    <automated>grep -q '01e hh.ru Pre-flight' docs/SETUP.md && grep -q 'schema refresh' docs/SETUP.md && grep -q 'hh.ru' docs/SETUP.md</automated>
  </verify>
  <done>SETUP.md contains explicit pre-flight steps for hh.ru Source option and n8n schema refresh</done>
</task>

<task type="auto">
  <name>Task 2: Add Airtable troubleshooting for partial 01e writes</name>
  <files>airtable/AIRTABLE-SCHEMA.md</files>
  <action>
    Extend airtable/AIRTABLE-SCHEMA.md after the existing "Manual Airtable Setup (required before first 01e run)" section with a **"Troubleshooting 01e Pipeline writes"** subsection.

    Document these symptom → cause → fix rows (from UAT test 3 evidence):

    | Symptom | Likely cause | Fix |
    | Source shows LinkedIn instead of hh.ru | Pipeline.Source missing `hh.ru` option; n8n coerced/defaulted invalid select | Add `hh.ru` option manually; refresh Create Pipeline Records schema in n8n |
    | Only Status / Fit Tier populated; job fields empty | Stale n8n field schema or column name mismatch | Refresh schema; verify Create node maps Job Title, Company, Job Description, etc. |
    | Job Description thin (~200 chars), _descriptionSource=rss in n8n | Vacancy page fetch returned empty HTML (see workflow diagnostics) | Complete SETUP pre-flight; re-import 01e after 02-04 fetch hardening; inspect Fetch Vacancy Page node output |

    Reinforce that n8n typecast does **not** create new single-select options — manual Airtable UI step is mandatory.

    Cross-link to docs/SETUP.md pre-flight section.
  </action>
  <verify>
    <automated>grep -q 'Troubleshooting 01e Pipeline writes' airtable/AIRTABLE-SCHEMA.md && grep -q 'Source shows LinkedIn' airtable/AIRTABLE-SCHEMA.md</automated>
  </verify>
  <done>AIRTABLE-SCHEMA.md documents Source=LinkedIn and empty-row symptoms with fixes</done>
</task>

<task type="auto">
  <name>Task 3: Verify documentation cross-links and pre-flight coverage</name>
  <files>docs/SETUP.md, airtable/AIRTABLE-SCHEMA.md</files>
  <action>
    Ensure bidirectional references: SETUP pre-flight links to AIRTABLE-SCHEMA Manual Setup + Troubleshooting; AIRTABLE-SCHEMA troubleshooting links back to SETUP pre-flight.

  Confirm all UAT gap test 3 missing items from 02-UAT.md are addressed in docs:
    - hh.ru in Pipeline.Source (manual step documented)
    - n8n schema refresh after Airtable changes (documented)
    - pre-flight before first 01e run (documented)
  </action>
  <verify>
    <automated>grep -q 'AIRTABLE-SCHEMA' docs/SETUP.md && grep -q 'SETUP.md' airtable/AIRTABLE-SCHEMA.md && grep -q 'typecast' airtable/AIRTABLE-SCHEMA.md</automated>
  </verify>
  <done>Documentation cross-linked; all Airtable-side UAT gap missing items covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Airtable UI | Manual field-option changes require authenticated human |
| n8n → Airtable API | Create node writes depend on schema matching live base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Pipeline.Source field | mitigate | Document manual hh.ru option; invalid select causes visible wrong Source — troubleshooting guides operator fix |
| T-02-05-02 | Information Disclosure | SETUP.md | accept | No secrets in docs; only field names and setup steps |
| T-02-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Operator completes SETUP pre-flight (add hh.ru Source, refresh n8n schema, re-import 01e), then re-runs UAT test 3: Pipeline rows have Source=hh.ru and populated job fields.
</verification>

<success_criteria>
- SETUP.md has 01e hh.ru pre-flight checklist with schema refresh step
- AIRTABLE-SCHEMA.md has troubleshooting for Source=LinkedIn and partial writes
- Docs cross-link; user_setup frontmatter matches documented manual steps
</success_criteria>

<output>
Create `.planning/phases/02-hh-ru-description-enrichment/02-05-SUMMARY.md` when done
</output>
