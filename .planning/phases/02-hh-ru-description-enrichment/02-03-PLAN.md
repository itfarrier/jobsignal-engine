---
phase: 02-hh-ru-description-enrichment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test_hh_rss_parse.mjs
autonomous: true
gap_closure: true
requirements:
  - HH-10
user_setup: []
must_haves:
  truths:
    - "node scripts/test_hh_rss_parse.mjs exits 0 with only the single OK line on stdout"
    - "Phase 1 RSS parse regression still passes after enrichment changes"
  artifacts:
    - path: scripts/test_hh_rss_parse.mjs
      provides: "RSS parse fixture test with vacancy-parse-matching stdout contract"
      min_lines: 100
  key_links:
    - from: scripts/test_hh_rss_parse.mjs
      to: scripts/test_hh_vacancy_parse.mjs
      via: "success path logs only OK line, no JSON dump"
      pattern: "OK: hh.ru RSS parse fixture passed"
---

<objective>
Close UAT test 2 (minor): remove debug JSON output from RSS parse test success path so stdout matches test_hh_vacancy_parse.mjs contract.

Purpose: UAT operator expects a clean pass line; extra JSON was mistaken for a parser regression.
Output: scripts/test_hh_rss_parse.mjs with single-line success stdout.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/02-hh-ru-description-enrichment/02-UAT.md
@.planning/debug/hh-rss-extra-json-output.md
</execution_context>

<context>
@.planning/phases/02-hh-ru-description-enrichment/02-01-SUMMARY.md
@scripts/test_hh_rss_parse.mjs
@scripts/test_hh_vacancy_parse.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove success-path JSON dump from RSS parse test</name>
  <files>scripts/test_hh_rss_parse.mjs</files>
  <action>
    Close UAT gap test 2 per root cause in 02-UAT.md and .planning/debug/hh-rss-extra-json-output.md.

    In scripts/test_hh_rss_parse.mjs main(), delete line 103 (console.log(JSON.stringify(parsed, null, 2))) so the success path logs only console.log('OK: hh.ru RSS parse fixture passed') — matching scripts/test_hh_vacancy_parse.mjs line 161 behavior.

    Keep failure-path diagnostics unchanged (console.error for errors, stripped, parsed on exit 1). Do not add new stdout on success.
  </action>
  <verify>
    <automated>node scripts/test_hh_rss_parse.mjs 2>&1 | tee /tmp/hh-rss-out.txt; test "$(wc -l &lt; /tmp/hh-rss-out.txt | tr -d ' ')" -eq 1; grep -qx 'OK: hh.ru RSS parse fixture passed' /tmp/hh-rss-out.txt</automated>
  </verify>
  <done>Success stdout is exactly one OK line; no JSON object follows</done>
</task>

<task type="auto">
  <name>Task 2: Confirm RSS and vacancy parse regressions still pass</name>
  <files>scripts/test_hh_rss_parse.mjs</files>
  <action>
    Run both offline parse scripts from repo root to confirm the stdout fix did not break assertions. Both must exit 0.
  </action>
  <verify>
    <automated>node scripts/test_hh_rss_parse.mjs && node scripts/test_hh_vacancy_parse.mjs</automated>
  </verify>
  <done>Both parse tests exit 0; UAT test 2 truth is achievable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test script stdout | Operator reads pass/fail from CLI output only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | scripts/test_hh_rss_parse.mjs | accept | Test fixture is local-only; no runtime trust boundary |
| T-02-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
UAT test 2 gap closed when `node scripts/test_hh_rss_parse.mjs` prints only `OK: hh.ru RSS parse fixture passed` with exit 0.
</verification>

<success_criteria>
- No JSON object on RSS test success stdout
- test_hh_rss_parse.mjs and test_hh_vacancy_parse.mjs both exit 0
</success_criteria>

<output>
Create `.planning/phases/02-hh-ru-description-enrichment/02-03-SUMMARY.md` when done
</output>
