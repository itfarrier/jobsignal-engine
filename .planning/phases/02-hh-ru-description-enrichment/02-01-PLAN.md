---
phase: 02-hh-ru-description-enrichment
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fixtures/hh-vacancy-page-sample.html
  - scripts/test_hh_vacancy_parse.mjs
autonomous: true
requirements:
  - HH-10
user_setup: []
must_haves:
  truths:
    - "extractVacancyDescriptionHtml returns full description HTML from JSON-LD JobPosting on fixture"
    - "stripHtml removes tags and decodes entities from extracted vacancy HTML"
    - "mergeVacancyDescription falls back to RSS text when page HTML is empty or unparseable"
    - "Descriptions longer than 50,000 characters are truncated with marker suffix"
  artifacts:
    - path: fixtures/hh-vacancy-page-sample.html
      provides: "Anonymized hh.ru vacancy page excerpt with JSON-LD and data-qa markers"
      contains: "application/ld+json"
    - path: scripts/test_hh_vacancy_parse.mjs
      provides: "Offline unit tests for vacancy page extraction"
      exports: ["extractVacancyDescriptionHtml", "stripHtml", "mergeVacancyDescription"]
  key_links:
    - from: scripts/test_hh_vacancy_parse.mjs
      to: fixtures/hh-vacancy-page-sample.html
      via: "readFileSync fixture path"
      pattern: "hh-vacancy-page-sample\\.html"
    - from: scripts/test_hh_vacancy_parse.mjs
      to: workflows/01e-scanner-hhru.json
      via: "stripHtml mirrors 01e Parse node implementation"
      pattern: "stripHtml"
---

<objective>
Implement and test vacancy page description extraction logic (JSON-LD → data-qa → RSS fallback) before wiring into n8n workflow.

Purpose: HH-10 enrichment depends on correct HTML extraction and stripHtml sanitization; TDD isolates parse logic from n8n graph edits (per locked Testing decision).
Output: `fixtures/hh-vacancy-page-sample.html`, `scripts/test_hh_vacancy_parse.mjs` with exported functions and passing assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hh-ru-description-enrichment/02-CONTEXT.md
@.planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md
@scripts/test_hh_rss_parse.mjs
@workflows/01e-scanner-hhru.json
@workflows/01a-scanner-greenhouse.json
</context>

<feature>
  <name>Vacancy page description extraction</name>
  <files>fixtures/hh-vacancy-page-sample.html, scripts/test_hh_vacancy_parse.mjs</files>
  <behavior>
    - extractVacancyDescriptionHtml(fixture) → non-empty HTML string containing JobPosting description (JSON-LD primary)
    - extractVacancyDescriptionHtml(htmlWithoutJsonLd) → extracts from data-qa="vacancy-description" fallback block
    - extractVacancyDescriptionHtml('') → ''
    - stripHtml('&lt;p&gt;Hello&lt;/p&gt;') → 'Hello' (no angle brackets in output)
    - mergeVacancyDescription(fixtureHtml, 'rss fallback') → stripped text length >> 200 chars (full page, not RSS thin summary)
    - mergeVacancyDescription('', 'rss fallback text') → 'rss fallback text'
    - mergeVacancyDescription(html, rssFallback) with 50,001+ char result → ends with '\n\n[Description truncated]' and length ≤ 50,000 + suffix
  </behavior>
  <implementation>
    Mirror stripHtml from scripts/test_hh_rss_parse.mjs (entity decode + char-walk tag strip + whitespace collapse). Implement extractVacancyDescriptionHtml per 02-RESEARCH.md Pattern 2: iterate application/ld+json script blocks, parse JSON, return JobPosting.description; fallback regex on data-qa="vacancy-description". Export mergeVacancyDescription(rawHtml, rssFallback) applying extract → stripHtml → 50k cap. No npm packages.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Fixture and failing vacancy parse tests</name>
  <read_first>
    - .planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md (Pattern 2, Validation Architecture)
    - scripts/test_hh_rss_parse.mjs (stripHtml reference, test script structure)
    - fixtures/hh-rss-sample.xml (fixture style precedent)
  </read_first>
  <files>fixtures/hh-vacancy-page-sample.html, scripts/test_hh_vacancy_parse.mjs</files>
  <behavior>
    - Test 1: extractVacancyDescriptionHtml reads fixture → returns non-empty HTML with substantive content
    - Test 2: stripHtml on extracted HTML → no remaining &lt; or &gt; tag markers; collapsed whitespace
    - Test 3: mergeVacancyDescription(fixture, 'thin rss') → result longer than 500 chars and does not equal 'thin rss'
    - Test 4: mergeVacancyDescription('', 'rss only') → 'rss only'
    - Test 5: 50k+ input → truncated with '[Description truncated]' suffix
    - Test 6: fixture without JSON-LD but with data-qa block still extracts (add minimal secondary snippet in fixture or inline test HTML)
  </behavior>
  <action>
    Create fixtures/hh-vacancy-page-sample.html: anonymized excerpt (~100–200 lines) of a real hh.ru vacancy page structure containing (a) a script type="application/ld+json" block with @type JobPosting and a multi-paragraph description field (entity-encoded HTML), and (b) a div with data-qa="vacancy-description" containing equivalent content. Replace company names/titles with generic placeholders; keep HTML structure faithful to live page (per locked Testing decision).

    Create scripts/test_hh_vacancy_parse.mjs following test_hh_rss_parse.mjs pattern: ESM imports from node:fs/path/url, export stub functions extractVacancyDescriptionHtml, stripHtml, mergeVacancyDescription that throw or return empty so tests fail. Implement main() with assertions for all behavior cases above; exit 1 on failure. Do NOT install test runners.
  </action>
  <acceptance_criteria>
    - fixtures/hh-vacancy-page-sample.html exists and contains both JSON-LD JobPosting and data-qa="vacancy-description"
    - scripts/test_hh_vacancy_parse.mjs exists with exported function names
    - node scripts/test_hh_vacancy_parse.mjs exits non-zero (RED)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test_hh_vacancy_parse.mjs; test - $? -ne 0</automated>
  </verify>
  <done>Fixture and test script exist; test run fails as expected before implementation (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement extraction and merge functions</name>
  <read_first>
    - scripts/test_hh_vacancy_parse.mjs (failing tests from Task 1)
    - scripts/test_hh_rss_parse.mjs (stripHtml implementation to copy verbatim)
    - .planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md (Pattern 2 extractVacancyDescriptionHtml)
  </read_first>
  <files>scripts/test_hh_vacancy_parse.mjs</files>
  <behavior>
    - All Task 1 assertions pass
    - stripHtml matches test_hh_rss_parse.mjs behavior (entity decode order: &amp; last)
    - mergeVacancyDescription uses RSS fallback only when extraction yields empty string after stripHtml
  </behavior>
  <action>
    Implement extractVacancyDescriptionHtml per 02-RESEARCH.md Pattern 2 (JSON-LD loop with try/catch per block; data-qa regex fallback). Copy stripHtml from scripts/test_hh_rss_parse.mjs unchanged (per locked HTML extraction / stripHtml decision). Implement mergeVacancyDescription(rawHtml, rssFallback): const html = extractVacancyDescriptionHtml(rawHtml); let text = html ? stripHtml(html) : rssFallback; if text.length > 50000 apply substring(0, 50000) + '\n\n[Description truncated]'; return text (per locked 50,000 character cap). Keep functions exported for 02-02 workflow port.
  </action>
  <acceptance_criteria>
    - node scripts/test_hh_vacancy_parse.mjs prints OK and exits 0
    - grep exports in scripts/test_hh_vacancy_parse.mjs shows extractVacancyDescriptionHtml, stripHtml, mergeVacancyDescription
    - Merged fixture description length exceeds 500 characters (full page, not RSS-thin)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test_hh_vacancy_parse.mjs</automated>
  </verify>
  <done>All vacancy parse unit tests pass (GREEN); functions ready to port into n8n Merge Descriptions node.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture → test script | Local file read only; no network |
| Parse logic → Airtable (downstream) | stripHtml must sanitize before persistence (extends T-01-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Vacancy page HTML | mitigate | stripHtml in mergeVacancyDescription; 50k cap (extends T-01-04) |
| T-02-SC | Tampering | npm installs | accept | Node builtins only; no package-manager install tasks |
</threat_model>

<verification>
- node scripts/test_hh_vacancy_parse.mjs exits 0
- node scripts/test_hh_rss_parse.mjs still exits 0 (no regression)
</verification>

<success_criteria>
- Exported parse functions pass all fixture-based assertions
- stripHtml behavior matches Phase 1 RSS test script
- Ready for port into workflows/01e-scanner-hhru.json Merge Descriptions node
</success_criteria>

<artifacts_this_phase_produces>
- fixtures/hh-vacancy-page-sample.html — anonymized vacancy page HTML for offline tests
- scripts/test_hh_vacancy_parse.mjs — extractVacancyDescriptionHtml, stripHtml, mergeVacancyDescription + assertions
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-hh-ru-description-enrichment/02-01-SUMMARY.md` when done
</output>
