---
phase: 02-hh-ru-description-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - workflows/01e-scanner-hhru.json
  - docs/SETUP.md
autonomous: true
requirements:
  - HH-10
user_setup: []
must_haves:
  truths:
    - "01e fetches each parsed job's applyLink HTML page before aggregation"
    - "Enriched jobDescription replaces RSS summary when page parse succeeds"
    - "Failed fetch or empty parse keeps RSS summary; job still flows to Pipeline"
    - "1 second wait occurs between each vacancy page fetch inside feed loop"
    - "8:20 schedule, 100 net-new safety brake, and dedup logic unchanged"
  artifacts:
    - path: workflows/01e-scanner-hhru.json
      provides: "Loop Over Jobs, Fetch Vacancy Page, Merge Descriptions, Wait 1s Vacancy nodes"
      contains: "Loop Over Jobs"
    - path: docs/SETUP.md
      provides: "Operator verification steps for full description enrichment"
  key_links:
    - from: Parse & Filter Jobs
      to: Loop Over Jobs
      via: "main output connection (replaces direct Parse → Wait 1s)"
      pattern: "Parse & Filter Jobs"
    - from: Fetch Vacancy Page
      to: Merge Descriptions
      via: "HTTP text response in $json.data"
      pattern: "applyLink"
    - from: Merge Descriptions
      to: Create Pipeline Records
      via: "jobDescription field unchanged mapping"
      pattern: "Job Description"
---

<objective>
Wire vacancy page fetch + merge into 01e-scanner-hhru.json so Pipeline Job Description contains full stripped page text with RSS fallback, per HH-10 and all locked CONTEXT decisions.

Purpose: Evaluator at 9:00 needs rich descriptions; workflow extension completes the v2.0 milestone deliverable.
Output: Updated 01e workflow JSON and SETUP.md verification notes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/02-hh-ru-description-enrichment/02-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hh-ru-description-enrichment/02-CONTEXT.md
@.planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md
@scripts/test_hh_vacancy_parse.mjs
@workflows/01e-scanner-hhru.json
@workflows/01a-scanner-greenhouse.json
@docs/SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Loop Over Jobs, Fetch Vacancy Page, Wait 1s Vacancy nodes</name>
  <read_first>
    - workflows/01e-scanner-hhru.json (current Parse & Filter Jobs → Wait 1s → Loop Over Feeds connections)
    - workflows/01a-scanner-greenhouse.json (Fetch Job Detail: alwaysOutputData, onError continueRegularOutput, 10s timeout)
    - .planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md (Pattern 1, Pattern 3, Exact Insertion Point)
    - .planning/phases/02-hh-ru-description-enrichment/02-CONTEXT.md (locked Fetch mechanism, Workflow placement, Rate limiting, HTTP headers)
  </read_first>
  <files>workflows/01e-scanner-hhru.json</files>
  <action>
    Insert four new nodes into workflows/01e-scanner-hhru.json (per locked Workflow placement):

    1. **Loop Over Jobs** — type splitInBatches v3, batchSize 1, name "Loop Over Jobs". Receives output from Parse & Filter Jobs.

    2. **Fetch Vacancy Page** — type httpRequest v4.2+, url ={{ $json.applyLink }}, sendHeaders true, User-Agent header "Mozilla/5.0 (compatible; JobSignalEngine/2.0; +https://github.com/jobsignal-engine)" (per locked HTTP headers), options.timeout 10000, responseFormat text with outputPropertyName data (per RESEARCH Pattern 1). Set alwaysOutputData true and onError continueRegularOutput (per locked Fetch mechanism / 01a precedent). Do NOT use hh.ru API endpoints.

    3. **Wait 1s Vacancy** — type wait v1.1, amount 1 second (per locked Rate limiting).

    4. **Merge Descriptions** — placeholder Code node v2 (empty jsCode stub); implemented in Task 2.

    Rewire connections per RESEARCH target graph:
    - Parse & Filter Jobs → Loop Over Jobs (disconnect Parse → Wait 1s direct link)
    - Loop Over Jobs output 1 (batch) → Fetch Vacancy Page → Merge Descriptions → Wait 1s Vacancy → Loop Over Jobs
    - Loop Over Jobs output 0 (done) → Wait 1s (existing feed wait) → Loop Over Feeds
    - Leave Aggregate All Jobs and all downstream nodes unchanged

    Position new nodes between Parse & Filter Jobs and Wait 1s. Use unique ids consistent with existing a1e00001-* pattern. Do not modify schedule trigger, safety brake, or dedup nodes.
  </action>
  <acceptance_criteria>
    - python3 -m json.tool workflows/01e-scanner-hhru.json succeeds
    - grep -c '"Loop Over Jobs"' workflows/01e-scanner-hhru.json equals 1
    - grep -c '"Fetch Vacancy Page"' workflows/01e-scanner-hhru.json equals 1
    - grep -c '"Wait 1s Vacancy"' workflows/01e-scanner-hhru.json equals 1
    - connections show Parse & Filter Jobs → Loop Over Jobs (not directly to Wait 1s)
    - Fetch Vacancy Page has alwaysOutputData true and onError continueRegularOutput
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool workflows/01e-scanner-hhru.json > /dev/null && grep -c '"Loop Over Jobs"' workflows/01e-scanner-hhru.json | grep -q '^1$'</automated>
  </verify>
  <done>01e graph has per-job loop, HTTP fetch, and vacancy wait nodes with correct connections; feed-level Wait 1s preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Merge Descriptions and port parse logic</name>
  <read_first>
    - scripts/test_hh_vacancy_parse.mjs (canonical extractVacancyDescriptionHtml, stripHtml, mergeVacancyDescription)
    - workflows/01a-scanner-greenhouse.json (Merge Descriptions single-item pattern)
    - .planning/phases/02-hh-ru-description-enrichment/02-CONTEXT.md (locked Failure handling, HTML extraction)
    - .planning/phases/02-hh-ru-description-enrichment/02-RESEARCH.md (Pattern 2 Merge Descriptions jsCode, Pitfall 4 SSRF guard)
  </read_first>
  <files>workflows/01e-scanner-hhru.json</files>
  <action>
    Replace Merge Descriptions jsCode with logic ported from scripts/test_hh_vacancy_parse.mjs (inline functions, not import — n8n sandbox). Per-item processing inside Loop Over Jobs batch:

    - Read job from $('Loop Over Jobs').item.json; skip/pass through _empty items unchanged
    - Read fetchResult from $input.first().json; rawHtml = fetchResult.data || fetchResult.body || ''
    - rssFallback = job.jobDescription || '' (per locked Failure handling)
    - SSRF guard: if applyLink does not match ^https://hh\.ru/vacancy/\d+, return job with jobDescription=rssFallback and _descriptionSource='rss' (T-02-02); do not fetch non-hh.ru URLs
    - Apply extractVacancyDescriptionHtml → stripHtml → 50k cap identical to test script
    - Set _descriptionSource to 'page' or 'rss' (omit from Airtable Create mapping — internal only per RESEARCH open question)
    - Return [{ json: { ...job, jobDescription, _descriptionSource } }]

    Verify Create Pipeline Records still maps "Job Description": "={{ $json.jobDescription }}" — no Airtable schema change. Confirm Aggregate All Jobs receives enriched items from Loop Over Jobs done output.
  </action>
  <acceptance_criteria>
    - Merge Descriptions jsCode contains extractVacancyDescriptionHtml and stripHtml function bodies
    - Merge Descriptions jsCode contains hh\.ru/vacancy SSRF whitelist check
    - Merge Descriptions jsCode contains 50000 truncation logic
    - node scripts/test_hh_vacancy_parse.mjs exits 0
    - node scripts/test_hh_rss_parse.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test_hh_vacancy_parse.mjs && node scripts/test_hh_rss_parse.mjs</automated>
  </verify>
  <done>Merge node enriches descriptions with page text or RSS fallback; both parse test scripts pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update SETUP.md verification for description enrichment</name>
  <read_first>
    - docs/SETUP.md (existing 01e import and hh.ru RSS smoke sections ~lines 196, 262, 286, 353)
    - .planning/ROADMAP.md (Phase 2 success criteria)
    - .planning/phases/01-hh-ru-rss-scanner/01-02-SUMMARY.md (SETUP update style)
  </read_first>
  <files>docs/SETUP.md</files>
  <action>
    Update docs/SETUP.md in the hh.ru / 01e sections:

    - Note that 01e now fetches full vacancy page HTML per job (1s pacing between fetches) after RSS parse
    - Add verification bullet: after manual n8n execute, inspect a new hh.ru Pipeline row — Job Description should be substantially longer than RSS summary (~500+ chars for typical vacancies), with readable plain text (no HTML tags)
    - Add command: node scripts/test_hh_vacancy_parse.mjs alongside existing test_hh_rss_parse.mjs in verification checklist
  </action>
  <acceptance_criteria>
    - docs/SETUP.md mentions vacancy page description enrichment for 01e
    - docs/SETUP.md references scripts/test_hh_vacancy_parse.mjs
    - python3 -m json.tool workflows/01e-scanner-hhru.json succeeds
    - node scripts/test_hh_vacancy_parse.mjs && node scripts/test_hh_rss_parse.mjs both exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' docs/SETUP.md | grep -c 'test_hh_vacancy_parse' | grep -qv '^0$' && node scripts/test_hh_vacancy_parse.mjs && node scripts/test_hh_rss_parse.mjs</automated>
  </verify>
  <done>Operator docs describe enriched descriptions and automated parse test command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hh.ru vacancy HTML → n8n Fetch | Untrusted external HTML crosses into workflow |
| RSS applyLink → HTTP Request URL | User-influenced URL via hh.ru RSS link field |
| Merge Descriptions → Airtable Create | Sanitized plain text written to Pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Vacancy page HTML | mitigate | stripHtml + 50k cap in Merge Descriptions before Airtable write |
| T-02-02 | Spoofing | SSRF via applyLink | mitigate | Whitelist ^https://hh\.ru/vacancy/\d+ in Merge before using fetch result |
| T-02-03 | Denial of service | hh.ru rate limiting | mitigate | Loop Over Jobs batch=1 + Wait 1s Vacancy between fetches; 10s HTTP timeout; existing feed Wait 1s |
| T-02-04 | Information disclosure | n8n execution logs | accept | Job titles/descriptions in logs — AR-01-02 precedent from Phase 1 |
| T-02-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- python3 -m json.tool workflows/01e-scanner-hhru.json
- node scripts/test_hh_vacancy_parse.mjs && node scripts/test_hh_rss_parse.mjs
- Manual: import updated 01e in n8n, execute, confirm Pipeline Job Description length >> RSS summary for new hh.ru row
</verification>

<success_criteria>
- HH-10 satisfied: net-new hh.ru jobs written to Pipeline with full page description when fetch succeeds
- RSS fallback preserved on fetch/parse failure
- No changes to Evaluator, schedule, safety brake, or dedup behavior
</success_criteria>

<artifacts_this_phase_produces>
- workflows/01e-scanner-hhru.json — extended with Loop Over Jobs, Fetch Vacancy Page, Merge Descriptions, Wait 1s Vacancy
- docs/SETUP.md — enrichment verification steps
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-hh-ru-description-enrichment/02-02-SUMMARY.md` when done
</output>
