---
phase: 03-baseline-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - airtable/AIRTABLE-SCHEMA.md
  - airtable/templates/Baseline Metrics-Grid view.csv
  - docs/SETUP-03-BASELINE.md
autonomous: true
requirements:
  - HH-12
user_setup:
  - service: Airtable
    why: Create "Baseline Metrics" table and import CSV template
    env_vars: []
    dashboard_config:
      - task: "Import Baseline Metrics CSV template"
        location: "Airtable Base → '+' → 'Import data' → 'CSV file'"
      - task: "Update field types from plain text to correct types"
        location: "Airtable Base → Baseline Metrics table → customize field types"
  - service: n8n
    why: Link Error Trigger from 01e settings to measurement workflow
    env_vars: []
    dashboard_config:
      - task: "Set 01e Error Workflow to measurement workflow"
        location: "n8n UI → 01e scanner workflow → Settings → Error Workflow dropdown"
      - task: "Generate n8n API key for n8n node authentication"
        location: "n8n UI → Settings → API Keys → Add API Key"
  - service: n8n credential
    why: Create Header Auth credential for n8n API key
    env_vars: []
    dashboard_config:
      - task: "Create Header Auth credential with X-N8N-API-KEY header"
        location: "n8n UI → Credentials → Add → Header Auth → Name: 'n8n API Key' → Header Name: X-N8N-API-KEY"
must_haves:
  truths:
    - "Baseline Metrics table exists in Airtable with all required fields"
    - "CSV template can be imported to create the table without manual field creation"
    - "Setup instructions document all manual steps: import, credential config, Error Trigger linking, two-run protocol"
    - "No existing workflow files (.json) were modified"
  artifacts:
    - path: "airtable/AIRTABLE-SCHEMA.md"
      provides: "Updated schema doc including Baseline Metrics table definition"
      contains: "## Baseline Metrics Table"
    - path: "airtable/templates/Baseline Metrics-Grid view.csv"
      provides: "Importable CSV template for the new table"
      min_lines: 2
    - path: "docs/SETUP-03-BASELINE.md"
      provides: "Setup and configuration instructions for the baseline measurement workflow"
      min_lines: 30
  key_links:
    - from: "docs/SETUP-03-BASELINE.md"
      to: "workflows/03-baseline-diagnosis.json"
      via: "references the measurement workflow file"
      pattern: "03-baseline-diagnosis"
    - from: "airtable/AIRTABLE-SCHEMA.md"
      to: "airtable/templates/Baseline Metrics-Grid view.csv"
      via: "references CSV template"
      pattern: "Baseline Metrics"

---

<objective>
Create the Airtable "Baseline Metrics" table schema, importable CSV template, and setup documentation for the Phase 3 measurement workflow.

Purpose: Establish the data persistence layer and all manual configuration instructions so the Phase 3 measurement workflow (Plan 03) has a defined target table. Per D-06, baseline metrics are stored as Airtable records. Per D-07, a Profile snapshot is included with each record.

Output:
1. `airtable/AIRTABLE-SCHEMA.md` — Updated with Baseline Metrics table definition (D-06)
2. `airtable/templates/Baseline Metrics-Grid view.csv` — Importable CSV template
3. `docs/SETUP-03-BASELINE.md` — Setup instructions covering: workflow import, credential configuration, Error Trigger linking (per RESEARCH Pitfall 4), two-run protocol (D-08), and debugging guidance (D-09)
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/project/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@airtable/AIRTABLE-SCHEMA.md
@airtable/templates/
@.planning/phases/03-baseline-diagnosis/03-CONTEXT.md
@.planning/phases/03-baseline-diagnosis/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Baseline Metrics table schema to AIRTABLE-SCHEMA.md</name>
  <files>airtable/AIRTABLE-SCHEMA.md</files>
  <read_first>airtable/AIRTABLE-SCHEMA.md</read_first>
  <action>
    Append a new section "## Baseline Metrics Table" to airtable/AIRTABLE-SCHEMA.md after the "Search Queries Table" section (before "Manual Airtable Setup").

    Define these fields (per D-04, D-05, D-07):

    | Field | Type | Auto-populated | Notes |
    |-------|------|:--------------:|-------|
    | Execution ID | Text | ✅ | n8n execution ID of the measured 01e run |
    | Run Timestamp | Date (DateTime) | ✅ | When the 01e execution started |
    | Execution Status | Single select | ✅ | Options: Success, Error |
    | Feeds Generated | Number (Integer) | ✅ | Count of feed URLs from `Build Feed List` |
    | Total RSS Items | Number (Integer) | ✅ | Raw RSS items across all feeds |
    | After Geography Filter | Number (Integer) | ✅ | Items that passed `Parse & Filter Jobs` |
    | After Dedup | Number (Integer) | ✅ | Items after `Aggregate All Jobs` (within-run dedup) |
    | Net New Pipeline Records | Number (Integer) | ✅ | Items after `Deduplicate vs Pipeline` (non-_empty) |
    | Pipeline Records Created | Number (Integer) | ✅ | Records created by `Create Pipeline Records` |
    | Run Duration Seconds | Number (Decimal) | ✅ | 01e execution wall clock time |
    | Error Details | Long text | ✅ | JSON string with error message/node info (null if success) |
    | Per Feed Items | Long text | ✅ | JSON array: items per feed index |
    | Per Feed After Geo | Long text | ✅ | JSON array: items after geo filter per feed index |
    | Profile Snapshot | Long text | ✅ | JSON string of the Profile row at time of measurement (D-07) |
    | Notes | Long text | | Human-readable notes about this baseline run |

    Also add "Baseline Metrics" to the "## Tables Overview" table.
  </action>
  <acceptance_criteria>
    - AIRTABLE-SCHEMA.md contains a "## Baseline Metrics Table" section
    - The section defines all 15 fields listed above with correct types
    - "## Tables Overview" includes Baseline Metrics row
    - No existing sections were removed or modified (only appended)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Baseline Metrics Table" airtable/AIRTABLE-SCHEMA.md && grep -c "Execution ID" airtable/AIRTABLE-SCHEMA.md && grep -c "Profile Snapshot" airtable/AIRTABLE-SCHEMA.md</automated>
  </verify>
  <done>
    All 15 fields documented in AIRTABLE-SCHEMA.md with correct types and descriptions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Baseline Metrics CSV template</name>
  <files>airtable/templates/Baseline Metrics-Grid view.csv</files>
  <read_first>airtable/templates/Pipeline-Grid view.csv</read_first>
  <action>
    Create `airtable/templates/Baseline Metrics-Grid view.csv` following the existing CSV template pattern (tablename with spaces, `-Grid view.csv` suffix).

    The CSV header row must match the Airtable field names defined in Task 1:
    `Execution ID,Run Timestamp,Execution Status,Feeds Generated,Total RSS Items,After Geography Filter,After Dedup,Net New Pipeline Records,Pipeline Records Created,Run Duration Seconds,Error Details,Per Feed Items,Per Feed After Geo,Profile Snapshot,Notes`

    Include one empty data row (placeholders) so the Airtable import creates all columns.
  </action>
  <acceptance_criteria>
    - File exists at `airtable/templates/Baseline Metrics-Grid view.csv`
    - CSV headers contain all 15 field names matching the AIRTABLE-SCHEMA.md field names exactly
    - File follows the existing naming convention: `{Table Name}-Grid view.csv`
  </acceptance_criteria>
  <verify>
    <automated>head -1 airtable/templates/Baseline\ Metrics-Grid\ view.csv | grep -c "Execution ID" && head -1 airtable/templates/Baseline\ Metrics-Grid\ view.csv | grep -c "Profile Snapshot"</automated>
  </verify>
  <done>
    CSV template exists with all 15 fields in header, matching AIRTABLE-SCHEMA.md definitions
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SETUP-03-BASELINE.md documentation</name>
  <files>docs/SETUP-03-BASELINE.md</files>
  <read_first>docs/SETUP.md</read_first>
  <action>
    Create `docs/SETUP-03-BASELINE.md` following the tone and format of `docs/SETUP.md`. Cover these sections:

    1. **Overview** — What this workflow does (measurement/diagnosis of 01e, zero code changes to existing workflows). Reference D-01 through D-09 as numbered footnotes.

    2. **Import Workflow** — Import `workflows/03-baseline-diagnosis.json` into n8n via Settings → Import from File.

    3. **Airtable Setup** — 
       - Import `airtable/templates/Baseline Metrics-Grid view.csv` to create the Baseline Metrics table
       - Update field types (Single select for Execution Status, Number for counts, Long text for JSON fields, Date for Run Timestamp)
       - Verify the `airtableTokenApi` credential has write access to the new table

    4. **n8n Credential Setup** —
       - Create Header Auth credential in n8n named "n8n API Key" with header `X-N8N-API-KEY`
       - Generate API key from n8n Settings → API Keys
       - Assign the credential to the n8n node in the measurement workflow

    5. **Configure Error Trigger** (per RESEARCH Pitfall 4) —
       - Open 01e scanner workflow in n8n
       - Go to Workflow Settings → Error Workflow
       - Select "JobSignal - Workflow 03 - Baseline Diagnosis"
       - Save the workflow

    6. **Verify 01e Workflow ID** (per RESEARCH A7) —
       - Open 01e scanner workflow in n8n
       - Note the numeric workflow ID from the URL
       - Open the measurement workflow's "Get Latest Execution" n8n node
       - Set the "Workflow" filter to the correct 01e workflow

    7. **Two-Run Protocol** (D-08) —
       - Day 1: Trigger 01e manually (or wait for 8:20 schedule)
       - Verify measurement workflow ran at 8:25 and recorded metrics in Baseline Metrics table
       - Trigger 01e manually again immediately after (back-to-back, same day)
       - Verify second measurement record appears
       - Compare metrics between the two runs

    8. **Debugging** (D-09) —
       - If measurement shows zero results: check 01e executed successfully first
       - If no Airtable record appears: check n8n execution history for errors in measurement workflow
       - If n8n node returns no data: verify execution ID, workflow ID filter, and credentials
       - If Error Trigger doesn't fire: verify Error Workflow setting was saved in 01e
       - Common errors reference table (RESEARCH Common Pitfalls mapping)

    9. **Scraping After Baseline** — After baseline is recorded, the workflow continues running daily. Phase 5 (Verification) will query the Baseline Metrics table for comparison.
  </action>
  <acceptance_criteria>
    - File exists at `docs/SETUP-03-BASELINE.md` with all 9 sections
    - Documents manual Error Trigger linking (Pitfall 4 mitigation)
    - Documents Schedule Trigger timing (Pitfall 5: start at 8:25, verify 01e completion)
    - Documents two-run protocol from D-08
    - Documents D-09 debug-first protocol
    - Documents workflow ID verification (A7)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Error Trigger" docs/SETUP-03-BASELINE.md && grep -c "Two-Run" docs/SETUP-03-BASELINE.md && grep -c "debug" docs/SETUP-03-BASELINE.md</automated>
  </verify>
  <done>
    Complete setup documentation covering import, credential config, Error Trigger linking, two-run protocol, and debugging
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measurement workflow → Airtable API | Outbound write to Baseline Metrics table via PAT credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Airtable PAT in workflow JSON | mitigate | Use existing `airtableTokenApi` credential reference (not inline PAT token). Same pattern as 01e and all other workflows. |
| T-03-02 | Information Disclosure | n8n API key in workflow JSON | mitigate | Store n8n API key as n8n Header Auth credential. Never embed key as plain text in workflow parameters. Documented in SETUP-03-BASELINE.md step 4. |
| T-03-SC | Tampering | Package installs | mitigate | No packages installed — all work is n8n workflow JSON + Airtable CSV + markdown docs. No slopcheck required. |
</threat_model>

<verification>
## Plan Verification

1. `grep -c "## Baseline Metrics Table" airtable/AIRTABLE-SCHEMA.md` ≥ 1
2. `head -1 airtable/templates/Baseline\ Metrics-Grid\ view.csv` contains all 15 field names
3. `docs/SETUP-03-BASELINE.md` exists with all 9 required sections
4. No running processes or daemons were modified
</verification>

<success_criteria>
1. Baseline Metrics table documented in AIRTABLE-SCHEMA.md with all 15 fields
2. Importable CSV template created with matching field headers
3. Setup documentation covers: import, credentials, Error Trigger linking, two-run protocol, debugging
4. Zero existing files modified — only additions to `airtable/` and `docs/`
</success_criteria>

<output>
Create `.planning/phases/03-baseline-diagnosis/03-01-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

| Artifact | Path | Type |
|----------|------|------|
| Baseline Metrics table schema | `airtable/AIRTABLE-SCHEMA.md` (appended) | Documentation |
| Baseline Metrics CSV template | `airtable/templates/Baseline Metrics-Grid view.csv` | Importable CSV |
| Setup & config guide | `docs/SETUP-03-BASELINE.md` | Documentation |
</plan>
