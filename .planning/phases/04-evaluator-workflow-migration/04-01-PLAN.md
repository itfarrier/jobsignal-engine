---
phase: 04-evaluator-workflow-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/02-evaluator.json
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
user_setup: []

must_haves:
  truths:
    - "Evaluator workflow reads Profile record from NocoDB via HTTP GET + Unwrap"
    - "Evaluator workflow reads Pipeline records with Status='New' from NocoDB via HTTP GET + Unwrap with ?where=(Status,eq,New)&limit=5"
    - "AI scoring results (Fit Score, Fit Tier, Match Reasoning, etc.) written to NocoDB Pipeline via HTTP PATCH"
    - "High Fit email alerts trigger when Fit Tier is 'High', using correct $('Parse AI Response') reference"
    - "Interview prep (Interview Questions, STAR Responses) written to NocoDB Pipeline via HTTP PATCH"
    - "Code nodes reference $('Unwrap Profile') and $('Unwrap New Jobs') instead of $('Get Profile') and $('Get New Jobs')"
    - "If node condition uses $('Parse AI Response').item.json.fields['Fit Tier'] instead of $json.fields['Fit Tier']"
  artifacts:
    - path: "workflows/02-evaluator.json"
      provides: "Modified evaluator workflow with NocoDB nodes replacing Airtable nodes"
      contains: "httpRequest"
      min_lines: 1300
  key_links:
    - from: "Unwrap Profile (Code node)"
      to: "Get Profile (HTTP GET)"
      via: "$input.first().json"
      pattern: "records.map.*record.fields.*id: record.id"
    - from: "Unwrap New Jobs (Code node)"
      to: "Get New Jobs (HTTP GET with where clause)"
      via: "$input.first().json"
      pattern: "records.map.*record.fields.*id: record.id"
    - from: "Parse AI Response (Code node)"
      to: "Loop Over Jobs"
      via: "loopItem.id"
      pattern: "recordId = loopItem.id"
    - from: "If node condition"
      to: "Parse AI Response"
      via: "$('Parse AI Response').item.json.fields['Fit Tier']"
      pattern: "Parse AI Response.*item.json.fields"
    - from: "Update Job Record (HTTP PATCH)"
      to: "Parse AI Response"
      via: "$json.recordId"
      pattern: '"id":'
    - from: "Update Interview Prep (HTTP PATCH)"
      to: "Parse & Save Interview Prep"
      via: "$json.recordId"
      pattern: '"id":'

---

<objective>
Replace all 4 Airtable nodes in the Evaluator workflow with NocoDB HTTP Request nodes, insert Unwrap Code nodes, and update all Code node references and parameter references.

Purpose: Complete the database backend migration for the Evaluator workflow (Workflow 2), enabling it to read Profile and Pipeline records from NocoDB and write scoring/interview prep results back via the Data API v3. This eliminates the last Airtable dependency for job evaluation — AI scoring logic, interview prep generation, and email alerting remain unchanged.

Output: `workflows/02-evaluator.json` — modified in-place with 2 HTTP GET + 2 Unwrap Code nodes replacing Airtable reads (per D-02 naming convention: HTTP Request nodes named descriptively, Unwrap nodes immediately downstream), and 2 HTTP PATCH nodes replacing Airtable updates, plus corrected node references throughout.
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluator-workflow-migration/04-CONTEXT.md
@.planning/phases/04-evaluator-workflow-migration/04-RESEARCH.md
@.planning/phases/04-evaluator-workflow-migration/04-PATTERNS.md
@workflows/02-evaluator.json
@workflows/01a-scanner-greenhouse.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Airtable GET reads with NocoDB HTTP GET + insert Unwrap nodes + update connections</name>
  <files>workflows/02-evaluator.json</files>
  <read_first>
    - workflows/02-evaluator.json — current state of the file, specifically nodes array index 0 (Manual Trigger), index 1 (Get Profile Airtable node lines 16-50), index 2 (Get New Jobs Airtable node lines 52-87), and the connections block (lines 1177-1361)
    - workflows/01a-scanner-greenhouse.json lines 222-270 and 307-319 — reference pattern for HTTP GET Profile + Unwrap Profile node (credential structure, URL expression, jsCode)
    - workflows/01a-scanner-greenhouse.json lines 560-592 — reference connections block wiring for Get Profile → Unwrap Profile → Get Tracked Companies pattern
  </read_first>
  <action>
    Make four structural changes to the nodes array in workflows/02-evaluator.json and update the connections block:

    A) Replace "Get Profile" Airtable node (index 1, type: n8n-nodes-base.airtable, lines 16-50) with an HTTP Request node:
       - Change type to "n8n-nodes-base.httpRequest"
       - Set typeVersion to 4.2
       - Set method: GET
       - Set url: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PROFILE }}/records"
       - Set authentication: "genericCredentialType"
       - Set genericAuthType: "httpHeaderAuth"
       - Send header "Content-Type": "application/json"
       - Add retryOnFail: true, maxTries: 3, waitBetweenTries: 5000, alwaysOutputData: true
       - Set credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
       - Keep same id and position [240, 0]
       - Keep same name "Get Profile"
       - Remove the old airtableTokenApi credential reference

    B) Insert new "Unwrap Profile" Code node after Get Profile (between Get Profile and Get New Jobs in the nodes array):
       - type: "n8n-nodes-base.code"
       - typeVersion: 2
       - name: "Unwrap Profile"
       - position: [352, 0]
       - Generate a unique id
       - jsCode (exact unwrap transform):
         ```
         // Unwrap NocoDB Data API v3 response — transforms { records: [...], next, prev } into flat items
         const response = $input.first().json;
         const records = response.records || [];
         
         if (records.length === 0) {
           return [{ json: { _empty: true, _message: 'No Profile found' } }];
         }
         
         return records.map(record => ({
           json: {
             ...record.fields,
             id: record.id
           }
         }));
         ```
       
       CRITICAL — id field correction (per D-01 intent vs API reality):
       D-01 specifies using `Id` (auto-increment integer from `record.id_fields.Id`), but the NocoDB Data API v3 PATCH endpoint requires the string UUID (`record.id`). The unwrap exposes `id: record.id` (string UUID) to match the API contract. The auto-increment integer `Id` is not exposed because it cannot be used for PATCH identification. This applies to both Unwrap Profile and Unwrap New Jobs.

    C) Replace "Get New Jobs" Airtable node (index 2 → becomes index 3 after Unwrap Profile insertion, lines 52-87) with an HTTP Request node:
       - Same HTTP Request node structure as Get Profile
       - Name: "Get New Jobs"
       - URL: "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records?where=(Status,eq,New)&limit=5"
       - Keep position [448, 0]
       - Per D-03: use NocoDB URL query filter syntax (Status has no spaces, so unquoted syntax is correct)

    D) Insert new "Unwrap New Jobs" Code node after Get New Jobs (between Get New Jobs and Build Scoring Prompt):
       - Same structure as Unwrap Profile
       - name: "Unwrap New Jobs"
       - position: [672, 0]
       - jsCode same unwrap transform but with `_message: 'No new jobs found'`
       - Generate a unique id

    E) Update connections block (lines 1177-1361) from the current:
       - "Manual Trigger" → "Get Profile" (unchanged)
       - "Get Profile" → "Get New Jobs"          → "Get Profile" → "Unwrap Profile"
       - "Get New Jobs" → "Build Scoring Prompt" → "Get New Jobs" → "Unwrap New Jobs"
       - "Schedule Trigger" → "Get Profile" → "Get New Jobs"  → "Schedule Trigger" → "Get Profile" → "Unwrap Profile"
       - Add "Unwrap Profile" → "Get New Jobs"
       - Add "Unwrap New Jobs" → "Build Scoring Prompt"
       
       Follow the Phase 3 connection pattern (01a-scanner-greenhouse.json lines 560-592): 
       - "Get Profile" outputs to "Unwrap Profile" only
       - "Unwrap Profile" outputs to "Get New Jobs" only
       - "Get New Jobs" outputs to "Unwrap New Jobs" only
       - "Unwrap New Jobs" outputs to "Build Scoring Prompt" only
  </action>
  <verify>
    <automated>node -e "
const wf = require('./workflows/02-evaluator.json');
const names = wf.nodes.map(n => n.name);
const types = wf.nodes.map(n => n.type);
const conns = wf.connections || {};
// Check: nodes exist
console.assert(names.includes('Unwrap Profile'), 'Unwrap Profile missing');
console.assert(names.includes('Unwrap New Jobs'), 'Unwrap New Jobs missing');
console.assert(names.includes('Get Profile'), 'Get Profile missing');
console.assert(names.includes('Get New Jobs'), 'Get New Jobs missing');
// Check: proper types
const getProfile = wf.nodes.find(n => n.name === 'Get Profile');
const getNewJobs = wf.nodes.find(n => n.name === 'Get New Jobs');
console.assert(getProfile.type === 'n8n-nodes-base.httpRequest', 'Get Profile not HTTP');
console.assert(getNewJobs.type === 'n8n-nodes-base.httpRequest', 'Get New Jobs not HTTP');
console.assert(getNewJobs.type === 'n8n-nodes-base.httpRequest' && getNewJobs.parameters.url.includes('?where='), 'Get New Jobs missing where filter');
const unwrapProfile = wf.nodes.find(n => n.name === 'Unwrap Profile');
console.assert(unwrapProfile.type === 'n8n-nodes-base.code', 'Unwrap Profile not Code');
const unwrapNewJobs = wf.nodes.find(n => n.name === 'Unwrap New Jobs');
console.assert(unwrapNewJobs.type === 'n8n-nodes-base.code', 'Unwrap New Jobs not Code');
// Check: connections wired correctly
console.assert(conns['Get Profile']?.main[0][0].node === 'Unwrap Profile', 'Get Profile → Unwrap Profile missing');
console.assert(conns['Unwrap Profile']?.main[0][0].node === 'Get New Jobs', 'Unwrap Profile → Get New Jobs missing');
console.assert(conns['Get New Jobs']?.main[0][0].node === 'Unwrap New Jobs', 'Get New Jobs → Unwrap New Jobs missing');
console.assert(conns['Unwrap New Jobs']?.main[0][0].node === 'Build Scoring Prompt', 'Unwrap New Jobs → Build Scoring Prompt missing');
// Check: unwrap code contains id: record.id
console.assert(unwrapProfile.parameters.jsCode.includes('id: record.id'), 'Unwrap Profile missing id: record.id');
console.assert(unwrapNewJobs.parameters.jsCode.includes('id: record.id'), 'Unwrap New Jobs missing id: record.id');
console.log('All Task 1 assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - "Get Profile" node is type httpRequest (not airtable) with NOCODB_URL/NOCODB_BASE_ID/NOCODB_TABLE_PROFILE URL expression
    - "Unwrap Profile" Code node exists in nodes array with position [352, 0], jsCode contains `id: record.id`
    - "Get New Jobs" node is type httpRequest with URL containing `?where=(Status,eq,New)&limit=5`
    - "Unwrap New Jobs" Code node exists in nodes array with position [672, 0]
    - Both Unwrap Code nodes have `_empty` sentinel for zero-record responses
    - Connections block: Get Profile → Unwrap Profile → Get New Jobs → Unwrap New Jobs → Build Scoring Prompt
    - Schedule Trigger → Get Profile → Unwrap Profile
    - No Airtable credential references remain on Get Profile or Get New Jobs
    - All 4 HTTP Request nodes use httpHeaderAuth credential "NocoDB API"
    - Unwrap nodes expose `id: record.id` (NocoDB string UUID), NOT `Id` from `id_fields`
  </acceptance_criteria>
  <done>
    Get Profile and Get New Jobs are HTTP GET nodes. Unwrap Profile and Unwrap New Jobs Code nodes exist. Connections block correctly wires all new nodes in the flow. Node jsCode exposes `id: record.id` (string UUID).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Airtable PATCH nodes with NocoDB HTTP PATCH nodes</name>
  <files>workflows/02-evaluator.json</files>
  <read_first>
    - workflows/02-evaluator.json — the current state of the file after Task 1, specifically the "Update Job Record" node (lines 168-562, large Airtable schema block) and "Update Interview Prep" node (lines 688-1141, large Airtable schema block)
    - .planning/phases/04-evaluator-workflow-migration/04-PATTERNS.md lines 185-293 — exact PATCH node JSON structures
    - .planning/phases/04-evaluator-workflow-migration/04-RESEARCH.md lines 158-186 — NocoDB PATCH correction (uses `id` string UUID, not `Id` integer) and lines 214-245 — PATCH body format
  </read_first>
  <action>
    Replace two Airtable update nodes with HTTP PATCH nodes. Each PATCH requires `{ "id": "string_UUID", "fields": { ... } }` per NocoDB Data API v3 spec. The `id` is the string UUID from `record.id`, NOT the integer `Id` from `record.id_fields.Id`.

    A) Replace "Update Job Record" Airtable node (currently index 6 after Task 1 rearranged nodes):
       - Remove the entire Airtable node block (parameters with operation: "update", base, table, columns with full 30+ field schema, matchingColumns, airtableTokenApi credential)
       - Replace with HTTP Request node:
         ```json
         {
           "parameters": {
             "method": "PATCH",
             "url": "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records",
             "authentication": "genericCredentialType",
             "genericAuthType": "httpHeaderAuth",
             "sendHeaders": true,
             "headerParameters": {
               "parameters": [
                 { "name": "Content-Type", "value": "application/json" }
               ]
             },
             "sendBody": true,
             "specifyBody": "json",
             "jsonBody": "={\n  \"id\": \"{{ $json.recordId }}\",\n  \"fields\": {\n    \"Status\": \"Evaluated\",\n    \"Fit Score\": \"{{ $json.fields['Fit Score'] }}\",\n    \"Fit Tier\": \"{{ $json.fields['Fit Tier'] }}\",\n    \"Match Reasoning\": \"{{ $json.fields['Match Reasoning'] }}\",\n    \"Matched Skills\": \"{{ $json.fields['Matched Skills'] }}\",\n    \"Missing Skills\": \"{{ $json.fields['Missing Skills'] }}\",\n    \"CV Tailoring Notes\": \"{{ $json.fields['CV Tailoring Notes'] }}\",\n    \"Salary Info\": \"{{ $json.fields['Salary Info'] }}\",\n    \"AI Evaluation Cost\": \"{{ $json.fields['AI Evaluation Cost'] }}\"\n  }\n}",
             "options": {}
           },
           "name": "Update Job Record",
           "type": "n8n-nodes-base.httpRequest",
           "typeVersion": 4.2,
           "position": [1520, 64]
         }
         ```
       - Keep the same node id
       - Keep retryOnFail: true, maxTries: 3, waitBetweenTries: 5000
       - Set credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }
       - Do NOT add continueOnFail (per Research: scoring write failure should halt the workflow — inconsistent scoring is worse than no scoring)

    B) Replace "Update Interview Prep" Airtable node (currently at position [2912, -192] after Task 1):
       - Remove the entire Airtable node block (operation: "update", base, table, columns with schema, matchingColumns, airtableTokenApi credential)
       - Replace with HTTP Request node:
         ```json
         {
           "parameters": {
             "method": "PATCH",
             "url": "={{ $env.NOCODB_URL }}/api/v3/data/{{ $env.NOCODB_BASE_ID }}/{{ $env.NOCODB_TABLE_PIPELINE }}/records",
             "authentication": "genericCredentialType",
             "genericAuthType": "httpHeaderAuth",
             "sendHeaders": true,
             "headerParameters": {
               "parameters": [
                 { "name": "Content-Type", "value": "application/json" }
               ]
             },
             "sendBody": true,
             "specifyBody": "json",
             "jsonBody": "={\n  \"id\": \"{{ $json.recordId }}\",\n  \"fields\": {\n    \"Interview Questions\": \"{{ $json.interviewQuestions }}\",\n    \"STAR Responses\": \"{{ $json.starResponses }}\",\n    \"Interview Prep Cost\": \"{{ $json.interviewPrepCost }}\"\n  }\n}",
             "options": {}
           },
           "name": "Update Interview Prep",
           "type": "n8n-nodes-base.httpRequest",
           "typeVersion": 4.2,
           "position": [2912, -192]
         }
         ```
       - Keep the same node id
       - Do NOT add retryOnFail (matches original Airtable node per D-04 and graceful degradation pattern)
       - Set credentials: { "httpHeaderAuth": { "id": "TO_BE_CREATED", "name": "NocoDB API" } }

    Both PATCH nodes must use `"id"` (string) in the request body for the record identifier, NOT the Airtable-style `"matchingColumns"` pattern. The `$json.recordId` value comes from Parse AI Response which reads `loopItem.id` (the NocoDB string UUID exposed by the Unwrap node).
  </action>
  <verify>
    <automated>node -e "
const wf = require('./workflows/02-evaluator.json');
const names = wf.nodes.map(n => n.name);
// Check: no Airtable update nodes remain
const airtableUpdateNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.airtable' && (n.name === 'Update Job Record' || n.name === 'Update Interview Prep'));
console.assert(airtableUpdateNodes.length === 0, 'Airtable update nodes still present: ' + airtableUpdateNodes.length);
// Check: PATCH nodes exist with correct structure
const patchNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.httpRequest' && n.parameters.method === 'PATCH');
console.assert(patchNodes.length === 2, 'Expected 2 PATCH nodes, got ' + patchNodes.length);
// Check: Update Job Record has PATCH method and scoring fields
const updateJob = wf.nodes.find(n => n.name === 'Update Job Record');
console.assert(updateJob.parameters.method === 'PATCH', 'Update Job Record not PATCH');
console.assert(updateJob.parameters.jsonBody.includes('Fit Score'), 'Missing Fit Score field');
console.assert(updateJob.parameters.jsonBody.includes('Fit Tier'), 'Missing Fit Tier field');
console.assert(updateJob.parameters.jsonBody.includes('Status'), 'Missing Status field');
console.assert(updateJob.retryOnFail === true, 'Missing retryOnFail');
// Check: Update Interview Prep has PATCH method and interview fields
const updatePrep = wf.nodes.find(n => n.name === 'Update Interview Prep');
console.assert(updatePrep.parameters.method === 'PATCH', 'Update Interview Prep not PATCH');
console.assert(updatePrep.parameters.jsonBody.includes('Interview Questions'), 'Missing Interview Questions');
console.assert(updatePrep.parameters.jsonBody.includes('STAR Responses'), 'Missing STAR Responses');
// Check: both use httpHeaderAuth
console.assert(updateJob.credentials?.httpHeaderAuth?.name === 'NocoDB API', 'Update Job missing NocoDB API credential');
console.assert(updatePrep.credentials?.httpHeaderAuth?.name === 'NocoDB API', 'Update Interview Prep missing NocoDB API credential');
// Check: PATCH body uses id (string), not matchingColumns pattern
console.assert(updateJob.parameters.jsonBody.includes('\"id\"'), 'PATCH body missing id field');
console.assert(!updateJob.parameters.columns, 'Airtable columns still present on Update Job Record');
console.assert(!updatePrep.parameters.columns, 'Airtable columns still present on Update Interview Prep');
console.log('All Task 2 assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - "Update Job Record" is type httpRequest with method PATCH, no Airtable column schema
    - "Update Interview Prep" is type httpRequest with method PATCH, no Airtable column schema
    - Both PATCH nodes use credentials httpHeaderAuth "NocoDB API"
    - Update Job Record retains retryOnFail: true, maxTries: 3, waitBetweenTries: 5000
    - Update Interview Prep has no retryOnFail (matches original behavior)
    - PATCH body for Update Job Record contains all 9 fields: Status, Fit Score, Fit Tier, Match Reasoning, Matched Skills, Missing Skills, CV Tailoring Notes, Salary Info, AI Evaluation Cost
    - PATCH body for Update Interview Prep contains: Interview Questions, STAR Responses, Interview Prep Cost
    - Both PATCH bodies use `"id": "{{ $json.recordId }}"` (string UUID), not Airtable matchingColumns
    - No continueOnFail on Update Job Record
  </acceptance_criteria>
  <done>
    Update Job Record and Update Interview Prep are HTTP PATCH nodes with correct NocoDB Data API v3 body format. No Airtable update nodes remain in the workflow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Code node references + If condition + Resend email references</name>
  <files>workflows/02-evaluator.json</files>
  <read_first>
    - workflows/02-evaluator.json — current state after Task 2, specifically:
      - "Build Scoring Prompt" Code node (jsCode line: `const profileItems = $('Get Profile').all();`)
      - "Parse AI Response" Code node (jsCode line: `const recordId = loopItem._airtableRecordId || loopItem.id;`)
      - "If" node condition (line: `"leftValue": "={{ $json.fields['Fit Tier'] }}"`)
      - "Send a new email" (Resend) node parameters (to, subject, html fields containing `$('Get Profile')` and `$('Get New Jobs')` references)
      - "Build Interview Prep Prompt" Code node (jsCode line: `const profile = $('Get Profile').item.json;`)
    - .planning/phases/04-evaluator-workflow-migration/04-PATTERNS.md lines 299-402 — exact code edit patterns for all 5 changes
    - .planning/phases/04-evaluator-workflow-migration/04-RESEARCH.md lines 343-401 — detailed explanation of why each change is needed
  </read_first>
  <action>
    Apply 5 edits across the workflow JSON. All changes are in existing nodes — no new nodes.

    A) "Build Scoring Prompt" Code node — node reference update:
       Find: `const profileItems = $('Get Profile').all();`
       Replace: `const profileItems = $('Unwrap Profile').all();`
       Why: The original Airtable "Get Profile" returned flat items. The HTTP GET replacement returns `{ records: [...] }`. "Unwrap Profile" flattens this to the same format Build Scoring Prompt expects.

    B) "Parse AI Response" Code node — recordId field change:
       Find: `const recordId = loopItem._airtableRecordId || loopItem.id;`
       Replace: `const recordId = loopItem.id;`
       Why: After migration, loop items are unwrapped NocoDB records with `id: record.id` (NocoDB string UUID). The old `_airtableRecordId` fallback no longer exists. This matches the D-05 intent (recordId from loop item) using the correct field per PATCH API requirement — `id` (string UUID), not `Id` (auto-increment integer).

    C) "Build Interview Prep Prompt" Code node — node reference update:
       Find: `const profile = $('Get Profile').item.json;`
       Replace: `const profile = $('Unwrap Profile').item.json;`
       Why: Same reason as Change A — `$('Get Profile')` now returns raw NocoDB response. Must reference Unwrap Profile.

    D) "If" node — condition change:
       Find the condition object with:
       ```json
       "leftValue": "={{ $json.fields['Fit Tier'] }}"
       ```
       Replace with:
       ```json
       "leftValue": "={{ $('Parse AI Response').item.json.fields['Fit Tier'] }}"
       ```
       Keep rightValue: "High", operator: { type: "string", operation: "equals", name: "filter.operator.equals" } unchanged.
       Why: After HTTP PATCH replaces the Airtable update node, the PATCH response shape is `{ records: [{ id, fields }] }` — NOT `{ recordId, fields }`. `$json.fields` would evaluate to `undefined`. By referencing `$('Parse AI Response').item.json.fields['Fit Tier']`, the condition always has the correct `fields` object. This pattern is already used by the Resend email node and Build Interview Prep Prompt.

    E) "Send a new email" (Resend) — node reference updates:
       Make the following text replacements in the Resend node's parameters:
       1. `$('Get Profile').item.json['Notification Email']` → `$('Unwrap Profile').item.json['Notification Email']`
       2. `$('Get New Jobs').item.json['Job Title']` → `$('Unwrap New Jobs').item.json['Job Title']` (appears in both `subject` and `html`)
       3. `$('Get New Jobs').item.json.Company` → `$('Unwrap New Jobs').item.json.Company` (appears in both `subject` and `html`)
       4. `$('Get New Jobs').item.json.Location` → `$('Unwrap New Jobs').item.json.Location`
       
       Do NOT change `$('Parse AI Response').item.json.fields['Fit Score']` or `$('Parse AI Response').item.json.fields['Matched Skills']` — these already reference Parse AI Response by name and remain correct.

    Summary of all 5 changes:
    | Node | Current | New | Occurrences |
    |------|---------|-----|-------------|
    | Build Scoring Prompt | `$('Get Profile').all()` | `$('Unwrap Profile').all()` | 1 |
    | Parse AI Response | `loopItem._airtableRecordId \|\| loopItem.id` | `loopItem.id` | 1 |
    | Build Interview Prep Prompt | `$('Get Profile').item.json` | `$('Unwrap Profile').item.json` | 1 |
    | If node | `$json.fields['Fit Tier']` | `$('Parse AI Response').item.json.fields['Fit Tier']` | 1 |
    | Send a new email | `$('Get Profile')` → `$('Unwrap Profile')` | 1 ref |
    | Send a new email | `$('Get New Jobs')` → `$('Unwrap New Jobs')` | 5 refs |
  </action>
  <verify>
    <automated>node -e "
const wf = require('./workflows/02-evaluator.json');

// A) Build Scoring Prompt uses Unwrap Profile
const scoringPrompt = wf.nodes.find(n => n.name === 'Build Scoring Prompt');
const scoringCode = scoringPrompt.parameters.jsCode;
console.assert(!scoringCode.includes(\"$('Get Profile').all()\"), 'Build Scoring Prompt still references Get Profile');
console.assert(scoringCode.includes(\"$('Unwrap Profile').all()\"), 'Build Scoring Prompt missing Unwrap Profile reference');

// B) Parse AI Response uses loopItem.id
const parseAI = wf.nodes.find(n => n.name === 'Parse AI Response');
const parseAICode = parseAI.parameters.jsCode;
console.assert(!parseAICode.includes('_airtableRecordId'), 'Parse AI Response still has _airtableRecordId');
console.assert(parseAICode.includes('recordId = loopItem.id'), 'Parse AI Response missing loopItem.id');

// C) Build Interview Prep Prompt uses Unwrap Profile
const prepPrompt = wf.nodes.find(n => n.name === 'Build Interview Prep Prompt');
const prepCode = prepPrompt.parameters.jsCode;
console.assert(!prepCode.includes(\"$('Get Profile').item.json\"), 'Build Interview Prep still references Get Profile');
console.assert(prepCode.includes(\"$('Unwrap Profile').item.json\"), 'Build Interview Prep missing Unwrap Profile');

// D) If node condition references Parse AI Response
const ifNode = wf.nodes.find(n => n.type === 'n8n-nodes-base.if');
const conditionStr = JSON.stringify(ifNode.parameters.conditions);
console.assert(!conditionStr.includes('$json.fields'), 'If node still uses $json.fields');
console.assert(conditionStr.includes(\"$('Parse AI Response').item.json.fields['Fit Tier']\"), 'If node missing Parse AI Response reference');

// E) Resend email references updated
const resend = wf.nodes.find(n => n.name === 'Send a new email');
const paramsStr = JSON.stringify(resend.parameters);
console.assert(!paramsStr.includes(\"$('Get Profile').item.json\"), 'Resend still references Get Profile');
console.assert(!paramsStr.includes(\"$('Get New Jobs').item.json\"), 'Resend still references Get New Jobs');
console.assert(paramsStr.includes(\"$('Unwrap Profile').item.json\"), 'Resend missing Unwrap Profile');
console.assert(paramsStr.includes(\"$('Unwrap New Jobs').item.json\"), 'Resend missing Unwrap New Jobs');
// Parse AI Response references should remain unchanged
console.assert(paramsStr.includes(\"$('Parse AI Response').item.json\"), 'Resend Parse AI Response ref incorrectly changed');

console.log('All Task 3 assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Build Scoring Prompt jsCode contains `$('Unwrap Profile').all()` (NOT `$('Get Profile')`)
    - Parse AI Response jsCode contains `recordId = loopItem.id` (NOT `_airtableRecordId`)
    - Build Interview Prep Prompt jsCode contains `$('Unwrap Profile').item.json` (NOT `$('Get Profile')`)
    - If node condition leftValue is `$('Parse AI Response').item.json.fields['Fit Tier']` (NOT `$json.fields`)
    - Resend email to/subject/html use `$('Unwrap Profile')` and `$('Unwrap New Jobs')` (NOT `$('Get Profile')` or `$('Get New Jobs')`)
    - Resend email still references `$('Parse AI Response').item.json.fields['Fit Score']` and `$('Parse AI Response').item.json.fields['Matched Skills']` unchanged
    - No remaining references to `$('Get Profile')` or `$('Get New Jobs')` in the entire workflow JSON
  </acceptance_criteria>
  <done>
    All 5 code/parameter edits applied. No stale `$('Get Profile')` or `$('Get New Jobs')` references remain. If node condition references Parse AI Response directly. Resend email node uses Unwrap node references. Parse AI Response uses `loopItem.id` (NocoDB string UUID).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n nodes → NocoDB HTTP API | All workflow-to-database communication crosses this boundary — API token auth applied at every HTTP Request node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | All 4 HTTP Request nodes to NocoDB API | mitigate | xc-token stored in n8n HTTP Header Auth credential ("NocoDB API" from Phase 3), never in code or workflow JSON |
| T-04-02 | Tampering | NocoDB PATCH nodes (Update Job Record, Update Interview Prep) | accept | PATCH writes scoring/prep data only — no user-controllable input. Token access already scoped to NocoDB base/table level |
| T-04-03 | Elevation of Privilege | NocoDB Data API v3 access via HTTP Header Auth | accept | Single-user pipeline; n8n credentials managed by the same user who deploys the stack. No privilege escalation path |
| T-04-04 | Tampering | npm installs | mitigate | No packages installed in this phase — all changes are n8n workflow JSON edits against existing infrastructure |
| T-04-SC | Tampering | n8n workflow JSON (workflows/02-evaluator.json) | mitigate | Version-controlled in git; changes are visible in commit history. Slopcheck: verify no obfuscated scripts, eval(), or suspicious string escaping |

</threat_model>

<verification>
## Overall Phase Checks

Run all automated assertions:
```bash
# Full automated validation (runs all task verifications)
node -e "
const wf = require('./workflows/02-evaluator.json');
const names = wf.nodes.map(n => n.name);
const types = wf.nodes.map(n => n.type);

// === Structural checks ===
// 4 HTTP Request nodes (2 GET + 2 PATCH)
const httpNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.httpRequest');
console.assert(httpNodes.length === 4, 'Expected 4 HTTP nodes, got ' + httpNodes.length);

// 2 Code nodes (2 Unwrap)
const codeNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.code');
console.assert(codeNodes.length >= 2, 'Expected at least 2 Code nodes');

// No Airtable nodes remain
const airtableNodes = wf.nodes.filter(n => n.type === 'n8n-nodes-base.airtable');
console.assert(airtableNodes.length === 0, 'Airtable nodes still present: ' + airtableNodes.length);

// === Reference checks ===
const fullJson = JSON.stringify(wf);
console.assert(!fullJson.includes(\"$('Get Profile')\"), 'Stale Get Profile ref exists');
console.assert(!fullJson.includes(\"$('Get New Jobs')\"), 'Stale Get New Jobs ref exists');

// === PATCH structure ===
const patches = httpNodes.filter(n => n.parameters.method === 'PATCH');
console.assert(patches.length === 2, 'Expected 2 PATCH nodes');
patches.forEach(p => {
  console.assert(p.parameters.jsonBody.includes('\"id\"'), p.name + ' missing id field');
  console.assert(p.credentials?.httpHeaderAuth?.name === 'NocoDB API', p.name + ' wrong credential');
});

// === GET structure ===
const gets = httpNodes.filter(n => n.parameters.method === 'GET');
console.assert(gets.length === 2, 'Expected 2 GET nodes');

// === If node ===
const ifNode = wf.nodes.find(n => n.type === 'n8n-nodes-base.if');
const ifCond = JSON.stringify(ifNode.parameters.conditions);
console.assert(ifCond.includes('Parse AI Response'), 'If condition missing Parse AI Response ref');

console.log('All phase verification checks passed');
"
```

## Manual Verification (post-automation)
1. Open `workflows/02-evaluator.json` in n8n UI
2. Verify workflow structure: 4 HTTP nodes (2 GET at top, 2 PATCH downstream), 2 new Unwrap Code nodes, no red error badges
3. Verify "Unwrap Profile" output shows flat items with Profile fields + `id` (string UUID)
4. Verify "Unwrap New Jobs" output shows only Status="New" Pipeline records, max 5, with `id` (string UUID)
5. Verify "Update Job Record" request body has correct `{ "id": "...", "fields": {...} }` format
6. Verify workflow executes without errors (manual trigger) — see Verification Architecture in RESEARCH.md for full test cases
</verification>

<success_criteria>
- All 4 Airtable nodes in workflows/02-evaluator.json replaced with NocoDB HTTP Request nodes (2 GET + 2 PATCH)
- 2 new Unwrap Code nodes inserted post-GET with `id: record.id` (string UUID) exposure
- All Code node references updated from `$('Get Profile')` → `$('Unwrap Profile')` and `$('Get New Jobs')` → `$('Unwrap New Jobs')`
- Parse AI Response reads `loopItem.id` (NocoDB string UUID) instead of `loopItem._airtableRecordId || loopItem.id`
- If node condition references `$('Parse AI Response').item.json.fields['Fit Tier']` instead of `$json.fields['Fit Tier']`
- Resend email node references all updated to Unwrap node names
- Connections block correctly wires: Get Profile → Unwrap Profile → Get New Jobs → Unwrap New Jobs → Build Scoring Prompt
- No Airtable credential references remain anywhere in the workflow
- Automated verification script exits with 0 (all assertions pass)
- Manual trigger of workflow produces identical output to pre-migration (same Fit Scores, same interview prep content, same email alerts)
</success_criteria>

<output>
After completion, create SUMMARY.md:
- Plan: 04-01-PLAN.md executed
- Tasks completed: 3/3
- Files modified: workflows/02-evaluator.json
- Key changes: 4 Airtable nodes replaced, 2 Unwrap nodes inserted, 5 code/parameter edits applied
- Artifacts this phase produces:
  - Node: "Unwrap Profile" (Code node) — new node, transforms `{ records: [...] }` into flat items with `id: record.id`
  - Node: "Unwrap New Jobs" (Code node) — new node, same unwrap transform for Pipeline records
  - Node: "Get Profile" (HTTP GET) — replaced Airtable node, NocoDB Data API v3
  - Node: "Get New Jobs" (HTTP GET with where clause) — replaced Airtable node, `?where=(Status,eq,New)&limit=5`
  - Node: "Update Job Record" (HTTP PATCH) — replaced Airtable node, writes scoring fields + Status
  - Node: "Update Interview Prep" (HTTP PATCH) — replaced Airtable node, writes interview prep fields
  - Changed: Parse AI Response Code node — `recordId = loopItem.id` (NocoDB string UUID)
  - Changed: Build Scoring Prompt Code node — `$('Unwrap Profile').all()` reference
  - Changed: Build Interview Prep Prompt Code node — `$('Unwrap Profile').item.json` reference
  - Changed: If node condition — `$('Parse AI Response').item.json.fields['Fit Tier']`
  - Changed: Send a new email (Resend) parameters — 6 `$('Get X')` → `$('Unwrap X')` reference updates
</output>
