---
phase: 01-infrastructure-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.example.yml
  - scripts/nocodb-schema.json
  - scripts/requirements.txt
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - BOOT-02
user_setup: []

must_haves:
  truths:
    - "docker-compose up -d starts NocoDB and nocodb-postgres containers"
    - "NocoDB UI is accessible at http://localhost:8080"
    - "n8n can reach NocoDB at http://nocodb:8080 via Docker internal network"
    - "D-06: A canonical schema file (scripts/nocodb-schema.json) defines all 4 tables with correct NocoDB v3 field types"
    - "All field type mappings from AIRTABLE-SCHEMA.md are translated to NocoDB equivalents"
    - "`requests` library is available for the bootstrap script"
  artifacts:
    - path: "docker-compose.example.yml"
      provides: "NocoDB infrastructure services, volumes, env vars"
      contains: "nocodb service, nocodb-postgres service, NOCDB_API_TOKEN env var in n8n"
    - path: "scripts/nocodb-schema.json"
      provides: "Canonical NocoDB schema for 4 tables"
      min_lines: 200
    - path: "scripts/requirements.txt"
      provides: "Python dependencies including requests"
      contains: "requests"
  key_links:
    - from: "docker-compose.example.yml"
      to: "nocodb-postgres"
      via: "depends_on with condition: service_healthy"
      pattern: "depends_on.*nocodb-postgres"
    - from: "n8n service env"
      to: "nocodb service"
      via: "NOCDB_HOST=http://nocodb:8080"
      pattern: "NOCDB_HOST"

---

<objective>
Deploy NocoDB with PostgreSQL backend in the existing Docker Compose stack and define the canonical NocoDB schema for all 4 tables.

Purpose: Establish the NocoDB infrastructure layer that all subsequent phases build upon. The schema file provides a single source of truth for table/field definitions consumed by the bootstrap script.

Output:
- Updated `docker-compose.example.yml` with `nocodb` and `nocodb-postgres` services
- New `scripts/nocodb-schema.json` with all 4 tables defined
- Updated `scripts/requirements.txt` with `requests` dependency
</objective>

<execution_context>
@/Users/itfarrier/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/itfarrier/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-bootstrap/01-CONTEXT.md
@.planning/phases/01-infrastructure-bootstrap/01-RESEARCH.md
@.planning/phases/01-infrastructure-bootstrap/01-PATTERNS.md

# Source files
@docker-compose.example.yml
@airtable/AIRTABLE-SCHEMA.md
@scripts/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docker-compose.example.yml with NocoDB infrastructure</name>
  <files>docker-compose.example.yml</files>

  <read_first>
    - docker-compose.example.yml (existing Docker Compose stack — read before editing to see exact patterns)
    - .planning/phases/01-infrastructure-bootstrap/01-RESEARCH.md (lines 108-198 for exact service definitions, env vars, volumes)
    - .planning/phases/01-infrastructure-bootstrap/01-PATTERNS.md (lines 19-103 for existing Compose conventions)
    - .planning/phases/01-infrastructure-bootstrap/01-CONTEXT.md (all locked decisions D-01 through D-13)
  </read_first>

  <action>
    Add three sections to docker-compose.example.yml following the existing conventions (lowercase-with-hyphens service names, `- KEY=value` env format, `snake_case` volumes, comments above each service block):

    1. **nocodb-postgres service** (add after `db:` service, before `n8n:`):
      - image: `postgres:16-alpine`
      - restart: always
      - environment: POSTGRES_USER=nocodb_user, POSTGRES_PASSWORD=REPLACE_WITH_YOUR_PASSWORD, POSTGRES_DB=nocodb_db
      - volumes: nocodb_db_storage:/var/lib/postgresql/data
      - healthcheck with `pg_isready -U nocodb_user -d nocodb_db`, interval 10s, timeout 5s, retries 10
      - Per D-04, D-12: separate Postgres container with its own volume
      - Per T-01-02 security: NO ports mapping (Postgres only accessed by NocoDB via Docker internal network)

    2. **nocodb service** (add after `n8n:` service, before `cv-renderer:`):
      - image: `nocodb/nocodb:latest` (per D-01)
      - restart: always
      - depends_on with `condition: service_healthy` for nocodb-postgres
      - environment vars (per D-11):
        - `NC_DB=pg://nocodb-postgres:5432?u=nocodb_user&p=REPLACE_WITH_YOUR_PASSWORD&d=nocodb_db`
        - `NC_AUTH_JWT_SECRET=REPLACE_WITH_YOUR_JWT_SECRET`
        - `NC_PUBLIC_URL=https://nocodb.yourdomain.com`
        - `NC_ATTACHMENT_FIELD_SIZE=20971520` (20MB per D-13)
        - `NC_DISABLE_TELE=true`
      - ports: `"8080:8080"` (per D-02)
      - volumes: `nocodb_data:/usr/app/data` (attachment storage per D-12)
      - Per D-03: n8n references NocoDB internally via service name, no additional host mapping needed

    3. **Add to n8n service** environment section:
      - `NOCDB_API_TOKEN=REPLACE_WITH_TOKEN` (per D-10)
      - `NOCDB_HOST=http://nocodb:8080` (per D-03)

    4. **Add to volumes** section at bottom:
      - `nocodb_db_storage:` (Postgres data for NocoDB)
      - `nocodb_data:` (NocoDB attachment storage)

    Security per T-01-01 / T-01-03: ALL sensitive values use `REPLACE_WITH_YOUR_*` placeholders. No hardcoded passwords, tokens, or secrets. The example file is safe to commit to git.

    Follow exact indentation pattern from existing file (2-space indent for service keys, 4-space for nested keys, `- KEY=value` list format with 6-space indent).
  </action>

  <verify>
    <automated>grep -c "nocodb:" docker-compose.example.yml > /dev/null && grep -c "nocodb-postgres:" docker-compose.example.yml > /dev/null && grep -c "NC_DB=" docker-compose.example.yml > /dev/null && grep -c "nocodb_db_storage:" docker-compose.example.yml > /dev/null && grep -c "nocodb_data:" docker-compose.example.yml > /dev/null && grep -c "NOCDB_API_TOKEN" docker-compose.example.yml > /dev/null && grep -c "NOCDB_HOST" docker-compose.example.yml > /dev/null && echo "ALL SERVICES VERIFIED"</automated>
  </verify>

  <acceptance_criteria>
    - `docker-compose.example.yml` contains a `nocodb:` service entry with image nocodb/nocodb:latest
    - `docker-compose.example.yml` contains a `nocodb-postgres:` service entry with postgres:16-alpine
    - nocodb-postgres has a healthcheck using `pg_isready -U nocodb_user -d nocodb_db`
    - nocodb has `depends_on` with `nocodb-postgres` and `condition: service_healthy`
    - nocodb-postgres has NO `ports:` mapping (internal only)
    - All sensitive values use `REPLACE_WITH_YOUR_*` placeholders
    - n8n service has `NOCDB_API_TOKEN` and `NOCDB_HOST` env vars
    - `volumes:` section includes `nocodb_db_storage:` and `nocodb_data:`
    - File parses as valid YAML (verified via `python3 -c "import yaml; yaml.safe_load(open('docker-compose.example.yml'))"`)
  </acceptance_criteria>

  <done>docker-compose.example.yml has NocoDB + nocodb-postgres services with all env vars, volumes, healthcheck, and placeholders. n8n service has NocoDB connection env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/nocodb-schema.json with all 4 tables</name>
  <files>scripts/nocodb-schema.json</files>

  <read_first>
    - airtable/AIRTABLE-SCHEMA.md (source schema with all field names, types, select options)
    - .planning/phases/01-infrastructure-bootstrap/01-RESEARCH.md (lines 459-484 for NocoDB v3 field type reference, lines 488-540 for Airtable→NocoDB type mapping, lines 970-1040 for canonical structure)
    - .planning/phases/01-infrastructure-bootstrap/01-PATTERNS.md (lines 274-299 for field type reference)
  </read_first>

  <action>
    Create `scripts/nocodb-schema.json` with the canonical NocoDB schema. Use the structure from RESEARCH.md lines 972-1029. Root object has: `version`, `description`, `base_title`, `tables` array.

    Tables in dependency order (no actual FK dependencies but order matters for readability):

    1. **Profile** — 14 fields. Per AIRTABLE-SCHEMA.md lines 29-49:
      - Full Name (SingleLineText, required)
      - Professional Summary (LongText, required)
      - Core Skills (MultiSelect, required — default choices: Python, JavaScript)
      - Secondary Skills (MultiSelect)
      - Target Roles (MultiSelect, required — default choices: Software Engineer)
      - Target Industries (MultiSelect, required)
      - Negative Filters (MultiSelect, required)
      - Seniority Level (SingleSelect, required — choices: Mid, Senior, Staff, Lead, Head)
      - Location Preference (SingleSelect — choices: Remote Only, Hybrid, On-site, Any)
      - Target Geography (MultiSelect, required — choices: Canada, USA, Remote Global, Remote North America, EMEA, UK, APAC)
      - AI Model (SingleLineText)
      - Scoring Rubric Override (LongText)
      - CV Markdown (LongText, required)
      - Notification Email (Email, required)

    2. **Tracked Companies** — 9 fields. Per AIRTABLE-SCHEMA.md lines 89-104:
      - Company Name (SingleLineText)
      - Careers URL (URL)
      - API Endpoint (URL)
      - Scan Method (SingleSelect — choices: Greenhouse API, Ashby, Lever)
      - Title Keywords (SingleLineText)
      - Enabled (Checkbox)
      - Notes (SingleLineText)
      - Last Scan (Date)
      - Jobs Found Last Scan (Number)

    3. **Pipeline** — 28 fields. Per AIRTABLE-SCHEMA.md lines 52-86:
      - Job ID (SingleLineText)
      - Job Title (SingleLineText)
      - Company (SingleLineText)
      - Location (SingleLineText)
      - Apply Link (URL)
      - Job Description (LongText)
      - Source (SingleSelect — choices: LinkedIn, Indeed, Greenhouse, Ashby, Lever, Glassdoor)
      - Source Query (SingleLineText)
      - Source Tag (SingleLineText)
      - Discovery Date (Date — date_format: YYYY-MM-DD)
      - Status (SingleSelect — choices: New, Evaluated, Applied, Interview, Offer, Rejected, Archived)
      - Fit Score (Decimal — precision: 1)
      - Fit Tier (SingleSelect — choices: High, Medium, Low)
      - Match Reasoning (LongText)
      - Matched Skills (LongText)
      - Missing Skills (LongText)
      - CV Tailoring Notes (LongText)
      - Salary Info (SingleLineText)
      - AI Evaluation Cost (Decimal — precision: 6)
      - Applied Date (Date — date_format: YYYY-MM-DD)
      - Response Date (Date — date_format: YYYY-MM-DD)
      - Outcome Notes (LongText)
      - Tailored CV Text (LongText)
      - CV Tailoring Cost (Decimal — precision: 6)
      - Tailored CV (Attachment)
      - Interview Questions (LongText)
      - STAR Responses (LongText)
      - Interview Prep Cost (Decimal — precision: 6)

    4. **Search Queries** — 13 fields. Per AIRTABLE-SCHEMA.md lines 107-126:
      - Query (SingleLineText)
      - Source Type (SingleSelect — choices: JobSpy)
      - Query String (SingleLineText)
      - Title Keywords (SingleLineText)
      - JobSpy Sites (SingleLineText)
      - Location (SingleLineText)
      - Country Filter (SingleLineText)
      - Hours Old (Number)
      - Results Wanted (Number)
      - Source Tag (SingleLineText)
      - Enabled (Checkbox)
      - Last Run (Date — date_format: YYYY-MM-DD)
      - Results Last Run (Number)

    Field type mapping rules (from RESEARCH.md lines 488-510):
    - Airtable "Text" → SingleLineText
    - Airtable "Long text" → LongText
    - Airtable "URL" → URL
    - Airtable "Number (1 decimal)" → Decimal with precision: 1
    - Airtable "Number (6 decimals)" → Decimal with precision: 6
    - Airtable "Number (integer)" → Number (no precision)
    - Airtable "Single select" → SingleSelect with options.choices
    - Airtable "Multiple select" → MultiSelect with options.choices
    - Airtable "Checkbox" → Checkbox
    - Airtable "Date" → Date with date_format: YYYY-MM-DD
    - Airtable "Attachment" → Attachment
    - Airtable "Email" → Email (Notification Email in Profile)

    Select option color convention: use `"#36BFFF"` as default for all choices (per PATTERNS.md line 299).

    Descriptions: Include a meaningful `description` on each field explaining its purpose (sourced from AIRTABLE-SCHEMA.md Notes column and RESEARCH.md commentary).

    No `Links` type fields needed per RESEARCH.md line 587 (Pipeline relationships through workflow logic, not database-level links).
  </action>

  <verify>
    <automated>python3 -c "
import json
s = json.load(open('scripts/nocodb-schema.json'))
assert s['version'] == '1.0'
assert len(s['tables']) == 4
titles = [t['title'] for t in s['tables']]
assert 'Profile' in titles
assert 'Tracked Companies' in titles
assert 'Pipeline' in titles
assert 'Search Queries' in titles
# Verify every field has title, type
for t in s['tables']:
    for f in t['fields']:
        assert 'title' in f, f'Missing title in {t[\"title\"]}.{f}'
        assert 'type' in f, f'Missing type in {t[\"title\"]}.{f[\"title\"]}'
        if f['type'] in ('SingleSelect', 'MultiSelect'):
            assert 'options' in f and 'choices' in f['options'], f'{t[\"title\"]}.{f[\"title\"]} missing choices'
print(f'{len(s[\"tables\"])} tables, {sum(len(t[\"fields\"]) for t in s[\"tables\"])} fields total')
"</automated>
  </verify>

  <acceptance_criteria>
    - File is valid JSON (parses with `json.load()`)
    - Contains exactly 4 tables: Profile, Tracked Companies, Pipeline, Search Queries
    - Profile table has 14 fields
    - Pipeline table has 28 fields
    - Tracked Companies table has 9 fields
    - Search Queries table has 13 fields
    - Every field has `title` and `type` keys
    - Every SingleSelect/MultiSelect field has `options.choices` array with `title` and `color` per field
    - All select option values match AIRTABLE-SCHEMA.md (Seniority Level: Mid/Senior/Staff/Lead/Head; Status: New/Evaluated/Applied/Interview/Offer/Rejected/Archived; Source: LinkedIn/Indeed/Greenhouse/Ashby/Lever/Glassdoor; Fit Tier: High/Medium/Low; Scan Method: Greenhouse API/Ashby/Lever; Source Type: JobSpy)
    - Decimal fields have correct precision (Fit Score: 1, Cost fields: 6)
    - Date fields have date_format: YYYY-MM-DD
    - No `Links` type fields (relationships handled by workflow logic per A3)
  </acceptance_criteria>

  <done>scripts/nocodb-schema.json created with all 4 tables, 60+ fields, correct types, select options, and descriptions.</done>
</task>

<task type="auto">
  <name>Task 3: Add requests dependency to requirements.txt</name>
  <files>scripts/requirements.txt</files>

  <read_first>
    - scripts/requirements.txt (existing file to edit — currently has python-docx, flask, python-jobspy)
  </read_first>

  <action>
    Append `requests` to `scripts/requirements.txt` on a new line, maintaining alphabetical order (insert between `python-docx` and `flask` → no, alphabetical would be flask, python-docx, python-jobspy, requests). Add `requests` at the end of the file.
  </action>

  <verify>
    <automated>grep -q "^requests$" scripts/requirements.txt && echo "requests found in requirements.txt"</automated>
  </verify>

  <acceptance_criteria>
    - scripts/requirements.txt contains a line with exactly `requests`
    - All existing dependencies are preserved unchanged
  </acceptance_criteria>

  <done>requests added to scripts/requirements.txt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nocodb-postgres → host network | PostgreSQL port should not be exposed to host |
| nocodb → n8n internal network | n8n accesses NocoDB API via Docker internal network |
| user → docker-compose.example.yml | Sensitive values use placeholders; safe to commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | docker-compose.example.yml | mitigate | All sensitive values use `REPLACE_WITH_YOUR_*` placeholders; API token stored only in docker-compose environment section per D-10 |
| T-01-02 | Tampering | nocodb-postgres service | mitigate | No port mapping on nocodb-postgres; only accessible via Docker internal network |
| T-01-03 | Information Disclosure | docker-compose.example.yml | mitigate | No hardcoded credentials; bootstrap credentials passed via env vars (NOCODB_ADMIN_EMAIL, NOCODB_ADMIN_PASSWORD) per T-01-03 |
| T-01-SC | Tampering | pip installs | mitigate | `requests` is an approved package per Package Legitimacy Audit in RESEARCH.md |
</threat_model>

<verification>
Each task has its own inline verification. Overall phase verification:
1. Validate docker-compose YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('docker-compose.example.yml'))"`
2. Validate schema JSON syntax: `python3 -c "import json; json.load(open('scripts/nocodb-schema.json'))"`
3. Verify requirements.txt: `grep -q "^requests$" scripts/requirements.txt`
</verification>

<success_criteria>
- docker-compose.example.yml has both nocodb and nocodb-postgres services with all env vars and volumes
- NocoDB Postgres has healthcheck and no host port mapping
- scripts/nocodb-schema.json has all 4 tables with correct field types and select options
- scripts/requirements.txt includes requests
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-bootstrap/01-01-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

### Docker Services
- `nocodb`: NocoDB application service (image: nocodb/nocodb:latest)
- `nocodb-postgres`: Dedicated PostgreSQL for NocoDB (image: postgres:16-alpine)

### Docker Volumes
- `nocodb_db_storage`: PostgreSQL data persistence
- `nocodb_data`: NocoDB attachment storage

### Environment Variables
- `NC_DB`: PostgreSQL connection string (nocodb service)
- `NC_AUTH_JWT_SECRET`: JWT signing secret (nocodb service)
- `NC_PUBLIC_URL`: Public URL for Caddy routing (nocodb service)
- `NC_ATTACHMENT_FIELD_SIZE`: Attachment size limit = 20971520 (nocodb service)
- `NC_DISABLE_TELE`: Telemetry disabled (nocodb service)
- `NOCDB_API_TOKEN`: Shared API token for bootstrap + n8n (n8n service)
- `NOCDB_HOST`: Internal NocoDB URL http://nocodb:8080 (n8n service)

### Files
- `scripts/nocodb-schema.json`: Canonical NocoDB schema definition (4 tables, 60+ fields)
- `scripts/requirements.txt`: Updated with `requests` dependency

### Schema Tables
- `Profile`: 14 fields (SingleLineText, LongText, MultiSelect, SingleSelect, Email)
- `Tracked Companies`: 9 fields (SingleLineText, URL, SingleSelect, Checkbox, Date, Number)
- `Pipeline`: 28 fields (SingleLineText, URL, LongText, SingleSelect, Date, Decimal, Attachment)
- `Search Queries`: 13 fields (SingleLineText, SingleSelect, Number, Checkbox, Date)
