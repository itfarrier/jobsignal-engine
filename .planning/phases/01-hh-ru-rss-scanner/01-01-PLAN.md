---
phase: 01-hh-ru-rss-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - airtable/AIRTABLE-SCHEMA.md
autonomous: true
requirements:
  - HH-07
  - HH-08
user_setup:
  - service: airtable
    why: "New single-select / multiple-select options must exist in the live base before 01e Create node succeeds"
    dashboard_config:
      - task: "Profile → Target Geography: add Russia, Moscow, Saint Petersburg, Remote Russia"
        location: "Airtable base → Profile table → Target Geography field → Edit options"
      - task: "Search Queries → Source Type: add HH RSS"
        location: "Search Queries table → Source Type field → Edit options"
      - task: "Pipeline → Source: add hh.ru"
        location: "Pipeline table → Source field → Edit options"

must_haves:
  truths:
    - "AIRTABLE-SCHEMA.md documents four RU Target Geography options with hh area IDs"
    - "AIRTABLE-SCHEMA.md documents HH RSS Search Query column usage and JobSpy-only columns"
    - "AIRTABLE-SCHEMA.md lists hh.ru as a Pipeline Source option with manual Airtable setup steps"
  artifacts:
    - path: airtable/AIRTABLE-SCHEMA.md
      provides: "RU geography options, HH RSS query docs, Pipeline Source hh.ru"
      contains: "Russia"
      contains: "HH RSS"
      contains: "hh.ru"
  key_links:
    - from: airtable/AIRTABLE-SCHEMA.md
      to: workflows/01e-scanner-hhru.json
      via: "executor reads GEO_MAP + field semantics before workflow implementation"
      pattern: "Remote Russia"
---

<objective>
Update `airtable/AIRTABLE-SCHEMA.md` so operators and workflow 01e share one canonical contract for RU geographies, HH RSS Search Queries, and Pipeline Source `hh.ru`.

Purpose: HH-07 and HH-08 require schema documentation before workflow JSON can reference new Airtable options without ambiguity.
Output: Extended schema doc with RU geography table, HH RSS subsection, Pipeline Source update, and manual option-add checklist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hh-ru-rss-scanner/01-CONTEXT.md
@.planning/phases/01-hh-ru-rss-scanner/01-RESEARCH.md
@airtable/AIRTABLE-SCHEMA.md
@workflows/01a-scanner-greenhouse.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document RU Target Geography and GEO_MAP reference (HH-07)</name>
  <read_first>
    - airtable/AIRTABLE-SCHEMA.md lines 33–48 (Profile Target Geography)
    - 01-RESEARCH.md section "GEO_MAP Extensions for RU Geographies"
    - 01-CONTEXT.md D-08 through D-11 (area routing + post-filter)
    - workflows/01a-scanner-greenhouse.json Parse & Filter Jobs GEO_MAP structure
  </read_first>
  <files>airtable/AIRTABLE-SCHEMA.md</files>
  <action>
    In Profile table section, extend **Target Geography** multiple-select options from Canada/USA-only to include **Russia**, **Moscow**, **Saint Petersburg**, **Remote Russia** — keep all existing international options unchanged.

    Add a new subsection **Target Geography — Russia (hh.ru)** with a reference table:

    | Airtable option | hh `area` ID | Scanner behavior |
    |-----------------|--------------|------------------|
    | Russia | 113 | RSS `area=113` when any RU geo selected; post-filter via GEO_MAP substrings |
    | Moscow | 1 | Same fetch area (113 per D-08); post-filter Moscow substrings |
    | Saint Petersburg | 2 | Same fetch area (113 per D-08); post-filter SPb substrings |
    | Remote Russia | (none) | `area=113` + post-filter remote RU substrings in region/description/title (D-09) |

    Document that 01e **Parse & Filter** extends `GEO_MAP` with the exact substring arrays from 01-RESEARCH.md (Russia, Moscow, Saint Petersburg, Remote Russia blocks). Note post-filter unions **all** selected Profile geographies (D-11), matching 01a pattern.

    Cross-check existing 01a–1c geography keys in workflow code still appear in the international options list — do not remove or rename Canada/USA/Remote Global/etc.

    Cite locked decisions D-08, D-09, D-10, D-11 in prose where behavior is described.
  </action>
  <acceptance_criteria>
    - Target Geography row lists four new RU options plus all prior international options
    - Reference table maps each RU option to area ID 113/1/2/none with post-filter note
    - GEO_MAP substring lists for RU geographies are documented for 01e implementer
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Russia\|Moscow\|Saint Petersburg\|Remote Russia' airtable/AIRTABLE-SCHEMA.md | awk '$1>=4'</automated>
  </verify>
  <done>HH-07 schema documentation complete; RU geography semantics match CONTEXT D-08–D-11</done>
</task>

<task type="auto">
  <name>Task 2: Document HH RSS Search Queries and Pipeline Source (HH-08)</name>
  <read_first>
    - airtable/AIRTABLE-SCHEMA.md lines 107–125 (Search Queries JobSpy section)
    - airtable/AIRTABLE-SCHEMA.md lines 52–66 (Pipeline Source)
    - 01-CONTEXT.md D-12 through D-17 (Search Queries HH RSS)
    - 01-RESEARCH.md section "Airtable Schema Changes"
  </read_first>
  <files>airtable/AIRTABLE-SCHEMA.md</files>
  <action>
    Rename or retitle Search Queries section header to clarify dual use: **Search Queries Table (JobSpy + HH RSS)**.

    Update **Source Type** row: options include **JobSpy** and **HH RSS** (D-12).

    Add subsection **Search Queries (HH RSS)** documenting per D-13–D-17:
    - Reused columns: Query (label), Query String (`text=` override), Title Keywords (comma-separated), Location (hh **area ID** as plain text `"1"`, `"2"`, `"113"` — not city name per D-14), Source Tag, Enabled, Last Run, Results Last Run
    - Location empty → derive area same as auto-feeds (113 if Profile has any RU-relevant geo per D-15)
    - JobSpy-only columns (JobSpy Sites, Country Filter, Hours Old, Results Wanted) marked **ignored for HH RSS** (D-16)
    - Enabled HH RSS rows merge with Profile auto-feeds in one loop (D-17)
    - Include example row from RESEARCH (Python RU manual / HH RSS / Query String / Title Keywords / Location 113)

    Update Pipeline **Source** row: add **`hh.ru`** to option list alongside LinkedIn, Indeed, Greenhouse, etc.

    Add **Manual Airtable setup (required before first 01e run)** checklist: add four Profile geography options, HH RSS Source Type, Pipeline Source `hh.ru`. Note typecast in n8n may not create new select options — operator must add manually in Airtable UI.

    Link area ID reference: `https://api.hh.ru/areas/113`
  </action>
  <acceptance_criteria>
    - HH RSS subsection exists with column semantics and example row
    - JobSpy-only columns explicitly marked ignored for HH RSS
    - Pipeline Source documents `hh.ru` option
    - Manual setup checklist present for three Airtable field option changes
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'HH RSS|hh\.ru|JobSpy-only|area ID' airtable/AIRTABLE-SCHEMA.md | wc -l | awk '$1>=4'</automated>
  </verify>
  <done>HH-08 documentation complete; operators know how to configure HH RSS rows and Pipeline Source</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc → operator | Schema doc instructs manual Airtable configuration |
| Doc → workflow | 01e implementer trusts documented area IDs and field names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | AIRTABLE-SCHEMA.md area ID table | mitigate | Copy verified IDs from api.hh.ru (113, 1, 2); document whitelist {1,2,113} for Location field |
| T-01-02 | Information disclosure | Example Query String in docs | accept | Example uses generic search terms only; no PATs or personal data |
| T-01-03 | Spoofing | Operator typos wrong area ID in Location | mitigate | Document allowed IDs explicitly; 01e Build Feed List will validate (plan 01-02) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No packages in this plan — docs only |
</threat_model>

<verification>
- `grep` confirms all four RU geography names in schema doc
- `grep` confirms HH RSS, hh.ru, and manual setup checklist present
- International geography options from pre-change doc remain documented
</verification>

<success_criteria>
- HH-07 and HH-08 satisfied in documentation only (no workflow JSON yet)
- Schema doc is sufficient for plan 01-02 executor without re-reading CONTEXT decisions
</success_criteria>

<output>
Create `.planning/phases/01-hh-ru-rss-scanner/01-01-SUMMARY.md` when done
</output>
